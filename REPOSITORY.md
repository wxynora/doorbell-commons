# 公共农场仓库边界

这个目录保存旧 VPS 上公共农场的独立运行包。当前生产实例仍运行在
`/opt/aifarm`，由 `aifarm.service` 管理，并把持久数据单独保存在
`/var/lib/aifarm`；它不属于 Doorbell Commons 社区后端进程，也不复用社区数据库。

## 当前事实源

- 根目录的 `dist/`、`content/`、CLI、同步脚本、公开说明和 `package.json` 于
  2026-08-02 从旧 VPS 的 `/opt/aifarm` 拉取，是迁仓时的生产运行快照。
- `deploy/aifarm.service` 来自旧 VPS 当前启用的 systemd unit，不包含凭据。
- VPS 上没有 `src/`、`package-lock.json`，source map 也没有内嵌
  `sourcesContent`。因此不能声称根目录快照拥有可重建当前生产版本的 TypeScript 源码。
- `source-reference/` 保存迁仓前网关仓库里的 TypeScript 与维护工具，只用于后续逐项
  对照恢复源码。它与线上 `dist/` 不完全一致，不能直接 build 后覆盖生产运行包。

## 公共农场维护流程

1. 每次维护旧 VPS 公共农场，必须先读取旧 VPS `/opt/aifarm` 中本次目标的当前 live
   运行文件；live 文件是本次唯一基线，仓库快照和 `source-reference` 都不能替代。
2. 从 live 文件复制出候选，只叠加辛玥当轮明确授权的变更；不得拿本地脏文件、仓库
   整文件、整目录、构建产物或“已经做完的其他功能”覆盖线上。
3. 写入生产前必须完整审查 live→candidate 差异；只要出现授权范围外的任意差异，
   立即停止并报告，不能顺手对齐、补齐、整理或覆盖。
4. 修改前检查玩家是否正在操作；如有玩家操作，先等待本轮操作结束，并从公共农场
   最后一次请求结束且无在途连接时开始计时。连续静默满 1 分钟后必须立即切入维护
   模式，不得额外延长、等待第二分钟或继续观望；静默期间出现新请求时，从该请求
   结束后重新计时。需要维护模式时只能使用线上已经存在且已确认的维护入口，禁止
   临时新建 Nginx、systemd、脚本或其他维护机制。
5. 只替换验收过的目标运行文件，只重启实际加载该文件的服务；不得顺带重启或部署
   其他服务。
6. 重启后必须核对目标文件事实、服务 active/running、监听/健康和本次启动日志；全部
   通过才算完成。若失败，停止扩散修复并如实报告。
7. 不读取、覆盖或迁移 `/var/lib/aifarm` 存档、凭据和配置；除非辛玥当轮逐项明确授权。
8. 部署成功后，仓库生产快照必须与最终 live 文件保持一致，但只能提交本轮授权差异；
   不能借同步快照夹带线上或本地的其他差异。

## 永不进入 Git 的内容

- `/var/lib/aifarm/world.json` 及任何农场存档；
- 同步凭据、human key、agent key、token、私钥和 `.env*`；
- `backups/`、`*.before-*`、损坏文件备份和临时文件；
- `node_modules/` 与现场依赖目录。

以后修改公共农场时，以本目录中对应的线上快照文件为起点，只部署明确变更的运行
文件；不得用 `source-reference/` 或整仓覆盖 `/opt/aifarm`，也不得触碰
`/var/lib/aifarm`。
