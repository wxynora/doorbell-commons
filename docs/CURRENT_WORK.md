# Current Work

| Task ID | Mode / status | Exact write scope | Known facts and boundaries | Next action | Validation |
| --- | --- | --- | --- | --- | --- |
| doorbell-registration-resident-farm-backend-commit-20260802 | 已完成：后端注册与农场绑定切片已建立本地 commit | 证据读取范围：`AGENTS.md`、`docs/CURRENT_WORK.md`、当前 HEAD/status、已验收后端切片的限定 diff、目标 staged 清单与秘密/生成物扫描；精确写入范围仅为本任务行、Git index 和一个本地 commit；实际提交仅含注册/居民/家园/农场绑定所需的 `apps/server/**`、`packages/protocol/**`、`.env.example`、必要 package/lock、`docs/product-plan.md`、`docs/runtime-architecture.md`、`docs/DEBUG_INDEX.md`、`AGENTS.md`、Connector 提案对应事实及可分离的后端任务行；未包含 `apps/web/**`、UI 方案、人体素材、其他窗口任务、push 或部署 | 本地 commit 共含 23 个文件；`AGENTS.md` 只纳入后端基线 hunk，`CURRENT_WORK` 只纳入本切片及直接决议/验收任务行。工作区保留并排除 AGENTS 社交记忆 hunk、UI/素材任务行和三份 UI 方案；无 `apps/web`、生成物、非空秘密或二进制进入提交 | 本任务完成并硬停止；后续规划、push 或部署需要另行授权 | 复用相关实现未变化前已通过的 server 26/26、protocol/server typecheck/build 与限定 Biome；提交前 cached diff-check、路径、秘密、生成物和二进制扫描通过。完成状态通过一次 amend 纳入同一提交；未 push/deploy |
| doorbell-identity-doc-state-reconcile-20260802 | 已完成：身份与持久化当前事实已对账 | 证据读取范围：`AGENTS.md` 当前基线、`docs/product-plan.md` 已确认身份边界、`docs/runtime-architecture.md` 当前实现、`docs/phase1-connector-contract-proposal.md` 顶部评审门与 D2、`docs/DEBUG_INDEX.md` 当前入口、`docs/CURRENT_WORK.md`；精确写入范围仅为 `AGENTS.md` 当前基线、Connector 提案对应状态/D2 过时句和本任务行；未修改代码、协议、其他方案段落、前端、依赖、Git 或生产 | `AGENTS.md` 已记录现有 QQ 核验、24h 注册码、人类账号/会话、SQLite 及一账号一居民/家园和唯一农场门牌绑定；Connector 提案已注明人类身份基础由产品合同另行落地，并把一个人类账号至多管理一个居民/家园组合标为已确认。D1、D3–D11、D13–D17 与 Connector 实现门保持未确认 | 本任务验收通过并硬停止；后续前端接入、提交、push 或部署均需另开任务 | 两条过时表述零命中、D1–D17 标题共 17 个、目标事实定向扫描与三文件 diff-check 通过；HEAD 保持 `1f280879b6e90626d41a4db348797f7034d107da`。文档任务未运行代码测试或构建，未 stage/commit/push/deploy |
| doorbell-registration-code-session-fix-20260801 | 已完成：注册码与人类会话后端通过定向验收 | 证据读取范围：本任务现有 Git 状态与限定 diff、`apps/server/src/community-database.ts`、`apps/server/src/registration-auth.ts`、`apps/server/src/registration-auth.test.ts`、`packages/protocol/src/index.ts`、身份相关应用组装与既有产品/运行文档；精确写入范围：上述实现与定向测试、由格式化器机械整理的本任务 TypeScript 文件，以及 `docs/product-plan.md`、`docs/runtime-architecture.md`、`docs/DEBUG_INDEX.md` 和本任务行；未修改 `apps/web/**`、农场、Connector 提案、`du-gateway` 或生产对象 | 已修复全部审查阻塞：过期注册码即使随机碰撞也必定换值；SQLite 文件设为 `0600`；账号持久化 `active/inactive` 群资格，确认退群原子撤销全部账号会话，OneBot 故障不误改状态，重新入群可恢复同一账号；测试类型和限定格式错误已清除。测试首次 500 已确认是 protocol 构建产物过旧，按规定先构建 protocol 后主链路通过。Prompt 改动为 0；未连接真实 OneBot/QQ | 本任务验收通过并硬停止；当前改动仍未提交、未 push、未部署，下一步需另开任务接注册前端或提交发布 | protocol/server typecheck 通过；protocol/server build 通过；server 定向测试 14/14 通过；限定 Biome 10 文件通过；`git diff --check` 通过 |
| doorbell-foundation-scaffold-20260731 | 已完成：第一版工程骨架 | `package.json`、`package-lock.json`、`.nvmrc`、`tsconfig.base.json`、`biome.json`、`README.md`、`docs/product-plan.md`、`docs/runtime-architecture.md`、`docs/CURRENT_WORK.md`、`docs/DEBUG_INDEX.md`、`apps/server/**`、`apps/web/**`、`packages/protocol/**`，以及安装验证生成且被 Git 忽略的 `node_modules/**` | 已建立 Node 24 / TypeScript 6 npm workspaces、Zod 共享合同、Fastify 健康接口和 React 人类观察端外壳。身份、持久化、WebSocket 会话、AI 唤起、私人拜访、审核、游戏存档与生产接入仍未实现；未写 AI 提示词；未提交、未 push、未部署 | 已由下列前端切片与 Connector 合同提案承接；骨架任务不再扩展 | `npm run format:check`、`npm run typecheck`、健康接口定向测试（1/1）及 `npm run build` 均通过；本地桌面与 390px 移动端实际渲染通过，移动端无横向溢出、页面无控制台错误，`/api/health` 联调三次均返回 200；开发服务已停止 |
| doorbell-frontend-lounge-observer-slice-20260731 | 已验收：待机室观察端切片 | `apps/web/src/**` | 前端已拆分顶栏、连接状态、导航、待机室场景、公开消息流、活动栏、我的小机、数据边界、健康检查 hook 与本地 view-model；当前只展示真实健康状态和明确空状态，没有伪造居民、消息或活动，没有人类代 AI 发言入口，没有新增依赖、协议解析、正式 Q 版素材或后端行为 | 等 Phase 1 合同经辛玥确认后，另建任务接真实 snapshot、消息和居民数据；当前不得先猜后端字段 | 目标任务记录的前端 typecheck、`npx biome check apps/web/src`、前端 build、1280×720 与 390×844 浏览器验收均通过；本轮验收复核了当前源码、禁止能力与 CSS 入口，未重复执行未变化的已通过测试 |
| doorbell-phase1-connector-contract-proposal-20260731 | 设计交付已验收；合同待辛玥确认 | `docs/phase1-connector-contract-proposal.md` | 提案只覆盖 Connector、居民接入和公共待机室，明确区分已确认事实、候选合同与待确认决策；门牌不承担鉴权，公开消息持久、presence 仅运行期，新消息不唤起模型，环境只提供事实。D1–D17 的推荐项均标为“未确认”，没有进入 Schema、服务端或协议包 | 辛玥需要审阅并选择 D1–D17；选择前禁止实现身份、REST/WSS 合同、snapshot、续接、错误码与停留时间字段 | 目标任务记录 11 个 JSON 示例可解析、17 组决策完整且无尾随空白；本轮逐段核对当前提案与产品基线，未发现私设重试、裁剪、额外期限、模型唤起或 Prompt 文案；未运行服务、构建或项目测试 |
| doorbell-repository-baseline-alignment-20260731 | 已完成：项目规则对齐 | 精确写入范围仅为 `AGENTS.md`、`docs/CURRENT_WORK.md`；未改前后端代码、Connector 提案、依赖、Git 或服务器 | `AGENTS.md` 已参考本地仓库规则补齐人格与表达、事实源、Doorbell 产品边界、Prompt 审阅、三种工作模式、Task Ticket、多窗口隔离、根因与改动纪律、定向验证以及 Git/生产边界；已删除过期的“仅有文档、技术栈未批准”状态，并明确当前骨架与尚未实现项；R2、Android、du 服务等项目专用条款未迁入 | 本任务在规则验收后停止；`docs/DEBUG_INDEX.md` 收束与 Git 首次提交仍需各自明确授权 | 已定向核对关键规则、章节结构与行尾空白；不相关专用词无命中；文档任务未运行代码测试、未提交、未 push、未部署 |
| doorbell-qq-group-admission-20260731 | 已完成：QQ 群准入与退群退出规则 | 精确写入范围仅为 `AGENTS.md`、`docs/product-plan.md`、`docs/CURRENT_WORK.md`；未改代码、QQ 机器人、Connector、数据库或服务器 | 产品基线和仓库规则已明确：Doorbell Commons 只绑定 QQ 群 `515831305`；只有该群现役成员能申请入住和注册，人类伴侣是资格主体；加群不自动创建居民；退群会立即撤销入住资格、终止现有连接、拒绝新访问，并启动既有的一个月保留删除流程；可信验证方式仍是待确认项 | 本文档任务到此停止；确认第一版群成员验证方式后另建身份接入施工任务 | 已定向核对一句话定义、身份章节、退出生命周期、安全清单、验收条目、待确认项和 AGENTS 产品边界；7.1–7.3 小节编号唯一；未运行代码测试、未提交、未 push、未部署 |
| doorbell-qq-registration-auth-backend-20260731 | 本地施工完成、定向验证通过；Git 首次基线缺失导致提交/push 阻塞 | 证据读取范围：`AGENTS.md`、`docs/product-plan.md` 第 1/5/7/17/19/21/22 节、`docs/runtime-architecture.md`、`docs/DEBUG_INDEX.md`、`docs/CURRENT_WORK.md`、`docs/phase1-connector-contract-proposal.md` 的人类身份边界、`apps/server/**`、`packages/protocol/**`、根 package/TypeScript/Biome/环境示例配置；精确写入范围：`apps/server/src/**` 中本任务的 OneBot 只读群资格边界与路由、`apps/server/src/**/*.test.ts` 定向测试、`packages/protocol/src/**` 中仅本次 QQ 群资格合同、`.env.example` 的空配置、`docs/runtime-architecture.md` 对应小节、本任务行、`docs/DEBUG_INDEX.md` 稳定条目；不修改根依赖 | `POST /api/registration/qq-group-eligibility` 只接受 QQ 号并固定调用 OneBot `get_group_member_list`（`no_cache: true`）查询群 `515831305`，精确命中即通过，未命中与 OneBot 不可用分开；没有 challenge、群历史、QQ 发送、所有权证明、身份持久化、Cookie、resident/home/Connector。登录凭据和网站会话仍无已确认合同，不实现。限定范围审计无真实秘密和发送调用 | 阻塞证据：本地 `main` 没有 `HEAD`，工程基线全部未跟踪；`origin` 存在但 `git fetch origin main` 返回远端无 `main`，`git ls-remote --heads origin` 为空。只提交本任务会得到缺少根 workspace 和包定义的残缺首提交，纳入其他任务基线又违反本轮精确提交范围。等待授权统一提交当前已完成基线，或等待基线任务先建立并 push `origin/main`；阻塞解除后再只提交本任务并核查独立 Doorbell 生产对象 | Node `v25.8.2`（仓库要求 `>=24`）下：protocol/server typecheck 通过；server 定向测试 5/5 通过；限定 Biome 首次仅报格式差异，机械格式化后复查通过；protocol 与 server build 通过。未连接真实 OneBot/QQ，未运行全仓或前端验证；未 stage、未 commit、未 push、未部署、未重启 |
| doorbell-initial-baseline-commit-20260731 | 已完成：Doorbell Commons 首次本地基线 commit | 证据读取范围：`AGENTS.md`、`docs/CURRENT_WORK.md`、`.gitignore`、当前全部非忽略工程文件的路径清单、Git HEAD/branch/remote/index 状态；精确写入范围：仅本任务行、Git index 和一个首次本地 commit（完成状态已 amend 一次）；未修改代码、配置、产品/运行机制或 `docs/DEBUG_INDEX.md` 内容 | 首次 root commit 统一包含当前已完成的工程骨架、项目规则与产品/运行文档、前端观察端、待确认 Connector 提案和 QQ 群资格接口，共 43 个工程文件；`.DS_Store`、`dist/`、`node_modules/` 等生成物保持 ignored，`.env.example` 仅含空 OneBot URL/token 模板。已知账号/会话/持久化/注册页、README 环境注入说明和生产部署缺口均原样保留 | 本任务已完成并硬停止；未 push、未部署、未重启。后续规划或 push 需要另行授权 | 复用既有独立验收，未重复运行测试。秘密模式、非空 env secret、生成物路径和二进制扫描均零命中；`git diff --cached --check` 仅报告既有 3 个 Markdown 双空格硬换行和 `.gitignore` 末尾空行，本任务未越权格式化。首次 commit 使用本机自动身份，完成状态已通过一次 amend 纳入同一 root commit；最终 HEAD、工作树和 origin 由交付前只读核对确认 |
| doorbell-registration-code-session-20260731 | 已完成：已由 `doorbell-registration-code-session-fix-20260801` 接管并完成验收 | 本历史任务原证据与写入范围不再继续使用；状态收束仅修改本任务行，不修改实现、测试、其他文档、Git 或生产 | 后续 fix 任务已完成注册码与人类会话后端，记录的稳定事实包括：过期注册码轮换必定换值、SQLite 文件权限为 `0600`、账号持久化群资格、退群原子撤销会话、OneBot 故障不误改状态、重新入群恢复同一账号；Prompt 改动为 0，未连接真实 OneBot/QQ | 本历史任务关闭并硬停止；后续以 `doorbell-registration-code-session-fix-20260801` 的完成状态与验证记录为准 | 复用接管任务已经记录的验收：server 定向测试 14/14、protocol/server typecheck 与 build、限定 Biome、`git diff --check` 均通过；本状态收束不重复运行代码验证 |
| doorbell-registration-task-state-reconcile-20260801 | 已完成：注册鉴权重复任务状态已收束 | 证据读取范围仅为 `AGENTS.md` 及 `docs/CURRENT_WORK.md` 中 `doorbell-registration-code-session-fix-20260801`、`doorbell-registration-code-session-20260731` 两行；精确写入范围仅为旧任务行与本任务行；未修改代码、其他文档、测试、Git 或生产 | `doorbell-registration-code-session-fix-20260801` 已记录完整实现和 14/14 定向测试、protocol/server typecheck、build、限定 Biome、diff-check 全部通过；旧任务行仍误留在施工中，现已明确关闭并指向完成的接管任务 | 状态对账完成并硬停止；注册鉴权后续工作以 fix 任务为唯一完成事实源 | 定向复核两个任务 ID 与完成/接管关系；状态文档任务未运行代码测试或构建，未 stage、未 commit、未 push、未部署 |
| doorbell-d2-d12-decision-record-20260802 | 已完成：D2-A 与 D12-B 已正式记录为产品决定 | 证据读取范围仅为 `AGENTS.md`、`docs/CURRENT_WORK.md`、`docs/product-plan.md`、`docs/runtime-architecture.md`、`docs/phase1-connector-contract-proposal.md` 中 D2/D12 及直接相关边界；精确写入范围仅为三份方案/运行文档的 D2/D12 对应段落和本任务行；`docs/DEBUG_INDEX.md` 因尚无实现未写；未修改代码、协议、前端、测试、依赖、Git 或生产 | 三份文档现已一致记录：Phase 1 一个居民绑定一个家庭和一个有效 Connector；人类/陪伴者使用独立人类会话读取公共社区，并只额外读取自己 AI 状态；人类 Cookie 与 Connector 凭据严格分离，禁止匿名公共读取。D1、D3–D11、D13–D17 均保持未确认；一个 human account 可管理几个居民/家庭没有被推断；当前没有新增实现入口 | 本任务验收通过并硬停止。下一居民/家庭施工票仍缺的最小用户决定是：Phase 1 是否固定一个已登录 human account 只管理一个 resident/home 组合；公共休息室读取的其他可见范围与实时合同继续留在对应 D 项 | D2-A/D12-B 三份文档定向扫描通过；提案 D1–D17 标题完整且只有 D2-A、D12-B 标为已确认；限定 `git diff --check` 通过；文档任务未运行代码测试或构建，未 stage、未 commit、未 push、未部署 |
| doorbell-registration-resident-farm-binding-audit-20260802 | 已完成：注册内居民/家园/现有农场一次绑定事实核对 | Doorbell 证据读取范围：`AGENTS.md`、`docs/CURRENT_WORK.md`、`docs/product-plan.md`、`docs/runtime-architecture.md`、`docs/DEBUG_INDEX.md`、当前 `apps/server/**`、`packages/protocol/**` 的注册/账号/数据库直接实现；跨仓只读 `/Users/doraemon/Downloads/du-gateway/AGENTS.md`、`docs/DEBUG_INDEX.md`、农场相关方案文档及索引指向的排行榜、身份、存档、API 直接实现；精确写入范围仅本任务行；未修改任一仓代码、协议、方案正文、测试、依赖、Git 或生产，未访问生产或写 R2 | 已确认：农场公开门牌是 `Farm.id`（6 位、唯一），农场名是 `Farm.name`；`aiName`/`humanName` 是另两个可选昵称而非主体键；排行榜行以 `code=f.id`、`name=f.name` 关联，存档以 `Farm.id` 建内存索引并保存完整 Farm，公共服稳定索引记载世界存档为 `/var/lib/aifarm/world.json`。当前本地农场实现已有 `GET /c?a=visit&farm=<门牌号>&detail=true` 只读查询：存在时返回 `farm.id/farm.name/plots`，不存在时返回 400；未实测生产。门牌号加名字确认只证明目标存在和人类确认，不证明农场归属；Doorbell 当前无 farm client、resident/home/farm 表或注册合同；重复绑定、解绑/重绑均无既定事实 | 已按辛玥确认收束最小流程：注册表单先提交 `farm_doorplate` 做只读查询，前端展示返回的 `farm_name`，人类明确确认后再把 QQ、注册码、门牌号与所确认名字一次提交；后端必须重新只读核对当前 `Farm.id/Farm.name`，随后在同一 SQLite 事务中创建/恢复 human account、创建 resident/home、写 farm binding、创建 session，任一步失败均不落部分绑定。查无结果停在确认前；农场服务不可用不得当作查无结果或继续注册；检测到既有绑定时不得静默覆盖，但同账号重登如何幂等、同一农场能否绑定多个账号、归属证明及解绑/重绑均需辛玥明确规则后才能施工。实现精确后端范围为 `packages/protocol/src/index.ts`、`apps/server/src/app.ts`、`apps/server/src/index.ts`、`apps/server/src/config.ts`、`apps/server/src/registration-auth.ts`、`apps/server/src/community-database.ts`，新增 `apps/server/src/farm-directory-client.ts`，并定向更新 `apps/server/src/app.test.ts` 与 `apps/server/src/registration-auth.test.ts` | 只读核对完成；没有调用生产、没有写 R2、没有运行测试。仅更新本任务行；`git diff --check -- docs/CURRENT_WORK.md` 已通过，HEAD 未变化，随后硬停止 |
| doorbell-resident-home-farm-registration-20260802 | 已完成：本窗口接管复核通过 | 证据读取范围：`AGENTS.md`、`docs/CURRENT_WORK.md`、已确认的 `docs/product-plan.md`、`docs/runtime-architecture.md`、`docs/DEBUG_INDEX.md`、`docs/phase1-connector-contract-proposal.md` 对应边界、当前 `apps/server/**` 与 `packages/protocol/**` 的注册/会话/数据库/配置/路由及直接测试，只读 `/Users/doraemon/Downloads/du-gateway/vendor/aifarm-oss/src/server.ts` 及其直接引用的只读 visit 合同；精确写入范围：`packages/protocol/src/index.ts`，`apps/server/src/app.ts`、`index.ts`、`config.ts`、`registration-auth.ts`、`community-database.ts` 及直接定向测试，新增 `apps/server/src/farm-directory-client.ts` 及测试，`.env.example`，对应 `docs/product-plan.md`、`docs/runtime-architecture.md`、`docs/DEBUG_INDEX.md` 与本任务行；本次接管未修改实现，只更新本任务行；未写 `du-gateway`、`apps/web`、其他 Connector D 项、素材、Git 或生产 | 当前文件证实：严格两种 session 请求形态、只读 farm lookup、resident/home/farm binding 三表及唯一约束、首次注册与历史账号补全的 immediate 单事务写入、既有组合精确恢复、会话摘要存储和 POST/GET 完整响应均已落地；农场门牌字符集与不存在响应已和上游源码逐项吻合，调用链无 QQ 发送动作，`farm_name` 不落库。产品方案、运行文档和 DEBUG_INDEX 与实现一致；`AGENTS.md` 末尾“身份/持久化未实现”以及 Connector 提案中“一个人类账号可管理几个居民/家庭尚未确定”仍是范围外旧表述，本轮未覆盖 | 接管验收完成并硬停止；当前后端无需重做。若要消除两处旧文档表述、接前端注册流程、提交或部署，均另开独立任务 | 本窗口实跑：server 定向测试 26/26、protocol/server typecheck 与 build、限定 Biome 13 文件、目标 diff-check 全部通过；HEAD 保持 `1f280879b6e90626d41a4db348797f7034d107da`，未连接真实 QQ/农场、生产或 R2，未 stage/commit/push/deploy |
