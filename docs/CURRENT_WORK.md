# Current Work

这里只记录尚未完成、正在执行或已经明确排队的工作。已经完成的实现进入
`docs/DEBUG_INDEX.md`，产品进度与未决项进入 `docs/product-plan.md`；完成任务从本文件直接删除，
不追加过程补记、完成补记或历史归档。

## BUILD-PWA-BROWSER-ACTIVITY-NOTIFICATIONS-20260829

- 模式／状态：施工／真实 Web Push 基础、两个设置开关与首个职业考试到点生产者已随 `main` commit `54629db` 推送；菜成熟和流光原野冷却生产者仍等待权威调度事实。全部尚未部署。
- 证据读取范围：本小节；统一 PWA 产品事实；`apps/web/public/{manifest.webmanifest,service-worker.js}`、`apps/web/src/{main,pwa,app}.tsx?` 与设置页；server 当前家庭设置、会话、定时提醒服务和 farm／流光只读投影；协议与定向测试。不得读取生产凭据、真实订阅、玩家数据、农场存档或其他任务 diff。
- 精确写入范围：本小节；统一 PWA 的 service worker、通知客户端与定向测试；`packages/protocol/src/index.ts`；server 的浏览器推送订阅、家庭设置、持久化、投递服务、`config.ts`／`index.ts` 装配与定向测试；`apps/server/package.json`、根 `package-lock.json` 只增加 Web Push 运行依赖；`.env.example` 与 `deploy/env/doorbell-commons.env.example` 只登记必需的 Web Push 部署配置名；设置页两个开关；`docs/{product-plan,runtime-architecture,DEBUG_INDEX}.md` 的本票事实。若菜熟／流光冷却缺少可验证的权威到点事实，只在本小节和方案记录精确阻塞，不修改 farm 或伪造事件。
- 已确认方向：浏览器通知是人类端系统通知，与 Bell 唤醒小机分开；“浏览器通知”是总开关，开启必须真实请求浏览器权限并建立可撤销订阅；“活动提醒”只控制菜成熟、流光原野冷却结束等个人到点提醒。关闭任一相关开关后不得继续投递对应 Web Push。
- 已完成并推送／直接证据：schema v9 保存默认关闭的浏览器通知／活动提醒及居民订阅；设置页开启总开关时真实请求权限并订阅，关闭时撤销；服务端每次投递重新核验 QQ 资格与两个开关，失效 endpoint 自动删除，其他失败 fail-soft；Service Worker 展示严格活动 payload 并点击回同源；既有职业考试 `13:55` 权威提醒同时成为首个 Human Web Push 生产者。隔离发布树 protocol build、server／web typecheck、server 定向 42/42、web 定向 25/25 通过；commit `54629db` 已与 GitHub `main` 精确一致。
- 阻塞／下一动作：农场 field 已读得到 `matures_at`，但当前社区没有覆盖后续农场动作、重启与时间变化的持久调度／撤销链；流光原野严格读取也没有权威 `ready_at`。取得这两类上游事实后再接对应服务端生产者；此前不得用页面计时器、旧响应缓存或估算时间冒充通知。

## FIX-CONSTABLE-INTERVIEW-CLOSURE-20260828

- 模式／状态：施工／Farm 修复与定向验证已完成并推送；Main 实现与文档已在隔离 clean clone 验证，因 `origin/main` 前移且命中本票三份事实文档，按并发规则停在刷新点，未覆盖也未推送 Main。
- 证据读取范围：本小节；现行治安官考试／面试／公示产品事实；刚推送的 Main/Farm 面试协议、状态机、人类路由、`go.school` 投影和直接测试。不得读取前端、日报、其他职业、生产配置或玩家数据。
- 精确写入范围：本小节；Main 的治安官面试共享协议、Farm client、人类面试服务／路由及直接测试；Farm 的面试排期／评分／公示状态机、`go.school` 公示投影、直接测试；对应 `docs/{product-plan,runtime-architecture,DEBUG_INDEX}.md`；Git 只精确提交并推送 `main` 与 `farm`。
- 已知事实／下一动作：Farm commit `7742466` 已推送 `origin/farm`；Farm 面试／内部 HTTP／`go.school` 22/22 通过。Main protocol build 与 client／route／mail 19/19 通过，定向 Biome／diff-check 通过；server typecheck 只剩范围外 Together fixture 缺 `archives`。`origin/main` 从 `123e7f0` 前移到 `ab364a2`，变更路径命中 `docs/{product-plan,runtime-architecture,DEBUG_INDEX}.md`；下一动作需在新 Main 基线上重新落本票三处事实文档并复用已验证代码，之后才能 commit／push Main。

## DESIGN-RANCH-BRAND-LIMITED-SKINS-20260828

- 模式／状态：设计／辛玥已授权先画一批牧场限定皮肤概念图，并确认正式限定皮肤需要真实角色动画。布丁狗旧动作设定图及其机械切分的移动预览已经被判定完全不可用；随后单独生成的四帧走路稿虽然分帧正确，但手臂没有随腿形成对侧摆动，同样作废且未保存。当前补充手臂与腿必须反向配合的逐帧要求，待辛玥确认修订提示词后再生图。
- 证据读取范围：本小节；`docs/{product-plan,runtime-architecture,DEBUG_INDEX}.md` 中牧场当前边界；当前权威 farm 动物／宠物目录；`old-vps/farm/assets/animal-codex-atlas.png` 与社区副本 `apps/web/public/farm/animals/animal-codex-atlas.png` 仅作现有像素画风、像素密度、配色质感和透明背景处理参考，不把其中动物的比例、轮廓、姿势或细节当作新角色底模。不得读取玩家存档、生产配置、凭据、其他任务 diff 或外部服务。
- 精确写入范围：本小节；`docs/product-plan.md` 仅记录已确认的限售周期、牧场金币、偏高价、一动物一款、逐项角色对应和动画方向；提示词确认后可在 `docs/design-assets/ranch-limited-skins/` 新增 `pompompurin-walk-four-frame-source.png`，并在四帧验收后替换 `pompompurin-walk-preview.gif`；此前 `pompompurin-motion-concept.png` 与 `pompompurin-exercise-preview.gif` 本轮不再修改。后续三款概念图仍须逐张提示词确认。不得修改动物／宠物 JSON、现有动物图集、商店、价格、正式动画速度、售卖期限、掉落／产出、前端、后端、Prompt 代码、Git、部署或生产。
- 已知事实／候选／下一动作：当前权威目录为 16 种生产动物与小猫、小狗两种宠物；每种动物本批最多一个皮肤位。已确认小狗唯一对应布丁狗；吉伊卡哇只对应未来仓鼠，不套鹌鹑；鹌鹑暂以角落生物的小麻雀为候选；鼹鼠可乐饼不对应猪，也不强塞给其他现有动物，猪本批暂时空缺。正式动物图集只借用像素视觉语言，新角色按自身辨识度重画。正式限定皮肤不能用一张静态图整体位移冒充动画：移动循环的相邻帧必须实际换腿，包含落脚／经过／相反落脚／再次经过等不同步态；手臂必须与腿形成对侧协调，左腿向前时右臂向前、右腿向前时左臂向前，两个经过帧的手臂相应回到中间，同时身体重心、耳朵、毛发或随身特征随步态变化；允许角色自然转动观察角度。首批只做布丁狗、小八、乌萨奇、甜心皮亚诺四款；生图只使用角色名并为角色单独提供官方形象参考。旧布丁狗动作设定图不能再用于机械切帧，首次独立四帧稿也因摆臂没有配合腿部而作废。下一动作提交修订后的单行四帧布丁狗走路循环完整提示词：四帧各自落在互不相交、尺寸相同的独立格中，角色中心与脚底基线一致，并逐帧明确对侧摆臂；未确认前不生图、不进入小八。

## REDESIGN-WEREWOLF-UI-V2-20260828

- 模式／状态：施工／狼人杀已经改为手机竖屏主界面并完成定向技术检查，本地预览服务已恢复；等待辛玥刷新后的实页视觉验收，不沿用上一版横屏画布或缩小验收结论。
- 证据读取范围：本任务小节；`docs/{product-plan,runtime-architecture,DEBUG_INDEX}.md` 中狼人杀、Game Adapter、银币与横屏预览边界；`apps/web/src/games/werewolf/{werewolf-page.tsx,werewolf-page.css,werewolf-interaction.ts,werewolf-interaction.test.ts}`；本机 `5192` 隔离预览的可见页面、无隐私 DOM、控制台与截图。不得读取玩家存档、身份、凭据、其他游戏脏 diff、生产数据或外部服务。
- 精确写入范围：本任务小节；`apps/web/src/games/werewolf/{werewolf-page.tsx,werewolf-page.css}`；按真实结果更新 `docs/{product-plan,runtime-architecture,DEBUG_INDEX}.md` 的狼人杀 UI 现状。不得修改 `games/werewolf/**` Python 规则、交互动作语义、玩家人数／身份合同、其他游戏、正式休息室、社区数据库／协议／路由、Connector、「铃」、Prompt、银币金额、Git、部署或生产。
- 已知事实／实现：规则仍为 `doorbell.werewolf.simple.v1` 的 `6～12` 人简版；受信座位支持全人类、全小机与任意混局，预览只允许改人数。原 `844×390` 横屏空场已删除，辛玥确认的竖屏决定已经同步产品与运行事实源。`6～7` 人使用三列、`8～12` 人使用四列；玩家改为无人物卡底框的轻量圆形头像，保留编号、姓名、本人／人／机、当前行动和合法目标反馈。第一版同脸小人身体、假村庄剪影和深色大公告板已按辛玥“诡异”反馈删除；身份、阶段和动作现在进入连续浅色底部操作区，不做重复目标卡或逐步弹窗。宽屏只居中承载同一竖屏版；规则、身份合同与服务端未改。
- 验证／下一动作：前端交互 4/4、Web typecheck、两文件 Biome、18 modules 专用构建和限定 diff-check 通过；`8771` API 健康，`5192` 页面返回 200，两个本地预览服务当前运行。浏览器自动读取本机页面被当前地址策略拦截，因此没有冒充 `337×694` 实页已验收；下一动作仅等待辛玥刷新后指出具体视觉问题，再在本任务两文件内继续修。未提交、推送、部署。

## TUNE-OWNER-PROFILE-RELATIONSHIP-EDIT-20260827

- 模式／状态：施工／关系图右侧 `Edit` 贴纸已按辛玥标记上移，定向回归和 `348×694` 实页核对通过，等待辛玥视觉验收。
- 证据读取范围：本任务小节；`apps/web/src/preview/{candidate-two-preview.tsx,candidate-two-preview.test.ts}` 中只限 `profile-relationship-edit` 的 DOM、CSS 与现有断言；辛玥当轮标记截图。不得读取范围外脏 diff、生产数据或外部服务。
- 精确写入范围：本任务小节；`apps/web/src/preview/candidate-two-preview.tsx` 仅限 `.candidate2-relationship-edit` 的垂直定位值；`apps/web/src/preview/candidate-two-preview.test.ts` 仅限对应位置断言。不得移动 Design／Edit Profile、身份卡、关系图、活动纸、More、底栏或其他页面，不得改逐字文案、行为、Git、部署或生产。
- 实现／验证：只把 `.candidate2-relationship-edit` 从 `bottom:124px` 改为 `164px`；`right:-16px` 与其他样式、DOM、文字和行为均未改。Candidate Two 15/15、Web typecheck、两文件 Biome 通过；`348×694` 实页中按钮顶部约 `368px`、底部约 `392px`，与约 `415px` 开始的活动纸分开。下一动作仅等待辛玥视觉验收。

## TUNE-FARM-NEIGHBORHOOD-TAB-HEIGHT-20260827

- 模式／状态：施工／「排行榜／留言板／原创作物」三个页签已收回米色顶栏范围，定向回归与 `348×694` 实页核对通过，等待辛玥视觉验收。
- 证据读取范围：本任务小节；辛玥当轮标记截图；`apps/web/src/farm/{farm-page.css,farm-page.test.tsx}` 中只限 `.farm-neighborhood` 网格首行、`.farm-neighborhood__tabs` 与 `.farm-neighborhood__link` 现有样式及对应源码断言。不得读取范围外脏 diff、生产数据或外部服务。
- 精确写入范围：本任务小节；`apps/web/src/farm/farm-page.css` 仅限 `.farm-neighborhood__tabs` 的纵向尺寸／对齐；`apps/web/src/farm/farm-page.test.tsx` 仅限对应回归断言。不得修改 JSX、三个页签宽度／颜色／文字／交互、内容面板、背景、底部导航、其他农场页面、数据合同、Git、部署或生产。
- 实现／验证：保留外层网格首行 `11.7cqw` 和内容面板起点，只给 `.farm-neighborhood__tabs` 增加 `align-self:start` 与 `height:9.8cqw`，三个页签宽度、颜色、文案、交互及其余页面均未改。新增尺寸断言在修复前按预期失败、修复后通过；farm source 42/42、Web typecheck、两文件 Biome 通过；真实 `348×694` 页面中三枚页签均完整落在同一顶栏内，控制台无错误。下一动作仅等待辛玥视觉验收。

## BUILD-FARM-WEATHER-NATURE-BACKEND-20260826

- 模式／状态：施工／P4 权威核心已经由 farm commit `edbcb33` 提交并推送至 `origin/farm`；共享 scheduler、玩法 adapters／authority、runtime、玩家入口、社区协议和页面仍未接线，当前不能冒充玩家已经能看到天气、经历灾害或使用职业处理。尚未 deploy 或操作生产。
- 证据读取范围：本任务小节；`docs/{lingye-upgrade-plan,product-plan,runtime-architecture,DEBUG_INDEX}.md` 的 P4、农场发布线和世界存档边界；`old-vps/farm/{REPOSITORY,README}.md`；`old-vps/farm/dist/{config,time,content,season-events,store,engine,game,fishing}.js`；`old-vps/farm/content/{seasons,season-events,fishing,animals}.json`；现有农场定向测试习惯。不得读取生产配置、凭据、真实玩家存档、其他任务 diff 或社区实现代码。
- 精确写入范围：本任务小节；权威 farm 工作区的新增 `dist/nature.js`、修改 `dist/{time,store}.js` 与新增 `tools/farm-weather-nature.test.mjs`。不得修改旧随机季节事件代码／内容、作物／牧场／钓鱼结算、Human UI、社区协议／服务／前端、产品／运行／索引事实文档、Git、部署或生产；需要这些共享入口时只向总规划列出精确接线需求。
- 已确认合同：P4 启用日由发布运维显式设置为北京时间春季第 1 天，启用前不得自动猜日期；启用后四季各 14 个自然日并全服同步。权威世界种子按日期稳定生成并持久保存今天及未来三日天气；农场和牧场必须读取同一事实，禁止按玩家、场景、切页、重启或背景分别抽取／缓存／重掷。天气权重、虫灾／洪水／干旱触发、单事件生命周期、7 个完整自然日冷却及已确认概率／时长已经进入核心；旧随机季节事件代码与内容保持原样，并在 P4 激活后通过现有 `currentSeason()` 自动读取同一生态季，不升级成持久状态或公共灾害。
- 待确认／接线边界：春季疾病提高、冬季基础产量降低的精确对象／概率／幅度与秋季持续效果仍不得私定；P3 农事异常、动物病例、农艺师／医生、天气钓鱼执行、洪水鱼、作物暂停／品相和 Human／MCP Schema 尚未接入。本票只保存稳定灾害事实、影响登记与显式恢复／结算栅栏，不能凭空创建尚不存在的 P3 病例或物资。
- 已实现：`dist/nature.js` 提供显式激活、稳定天气计划、生态季投影、公共灾害状态机、影响事实幂等登记、权威处理引用和显式恢复／结算栅栏；持久 `lastAdvancedDay` 让跨多个北京时间日界线的停机按权威日逐日补推进，倒退时钟明确 fail closed，不再静默跳过停机期间的预警／灾害；`flood`／`drought`／`pest` 各自只接受合同允许的 impact kind，注册与读档归一都会拒绝串灾害事实。`dist/time.js` 在自然 authority 激活后把既有季节消费者切到 14 日生态季，未激活旧世界继续使用原加速季节；`dist/store.js` 只增加 nature 内存槽、provider、原子保存／读取／提交／推进接点，缺少 nature 字段的既有 version-1 世界安全归一为 inactive。
- 下一动作／接线清单：从同一农场 authority 装配北京时间日界线推进与只读 snapshot；再分别把权威天气接到钓鱼拒绝／标签权重、农场与牧场同源显示，把灾害 impact 接到 P3 农事异常／动物病例、地块生长／品相、洪水鱼和职业处理，并接入实际 runtime／玩家入口与社区受控投影。精确模型可见 Schema／文案仍须审阅。任务在上述闭环、事实文档和发布均未完成时保留本小节。
- 定向验证：权威 farm 工作区四组自然核心定向测试合计 27/27，既有 request／store 存档安全测试 2/2；目标运行文件语法与限定 diff 检查通过。核心已随 `edbcb33` commit／push；没有启动农场服务、访问上游、读取真实存档、deploy 或操作生产。

## INTEGRATE-BANK-CAREER-WEATHER-BACKEND-20260826

- 模式／状态：施工／银行、职业、天气权威核心与独立 `lingye-world.sqlite` 同库容器已作为 farm commit `edbcb33` 推送到 `origin/farm`；社区 SQLite 保持原 schema v7，不承载这些权威数据。共享 `main` 中本轮用于机械迁移的 farm 运行副本已精确删除，`time/store` 的本轮增量也已撤回。当前仍未接农场启动装配、玩家入口、旧余额迁移、玩法 adapter 或生产，不能冒充完整系统已经可用。
- 证据读取范围：本任务小节；`docs/{product-plan,lingye-upgrade-plan,runtime-architecture,DEBUG_INDEX}.md` 的 P2／P3／P4 与 farm 发布线边界；两个 worktree 的 `old-vps/farm/{README.md,REPOSITORY.md,package.json}` 及本轮目标文件；本轮新增 `dist/{economy,career}/**` 的公开接口、`dist/nature.js` 的只读公开面与各自定向测试。不得读取生产配置、凭据、真实玩家数据、其他任务 diff 或范围外模块。
- 精确写入范围：本任务小节；已推送的 farm 目标为 `dist/{economy,career}/**`、`dist/{nature,lingye-world-database}.js`、`dist/{time,store}.js` 的本任务增量与四份定向测试。后续不得修改社区服务／协议／前端、农场 `server.js`／`index.js`、Prompt／MCP description／HELP、未确认数值、部署或生产；运行接线需先原位扩展本票精确范围。
- 接线原则：经济账户主体只能是 `resident_id`；银行与职业必须复用独立铃野世界 SQLite 的同一连接和事务边界，不复制第二套余额、居民或工作状态；职业收费／工资只能引用同一账本生成的权威回执，未接账本时不得假装已经扣款或发薪；天气继续由现有农场 `world.json` 的自然 authority 保存，SQLite 只承载机构／经济等独立数据域，不复制天气真相。
- 未确认／模型可见边界：治安官复核门槛与临时缺席处理、贷款信用最低存续日、高消费日累计上限仍须显式 fail closed；HTTP／MCP canonical op、strict Schema、错误语义和所有模型可见逐字文案尚未审阅，本票不得自行开放工具或占位接口。
- 已完成集成：`dist/lingye-world-database.js` 使用 Node 24 内建 `node:sqlite` 打开独立铃野世界库，只保存稳定 `resident_id` 绑定引用并在同一连接／事务中装配经济与职业服务；职业收费使用真实经济 journal 回执，若后续职业写入拒绝，外层事务会连同资金变化整体回滚。天气继续只进入现有原子 `world.json` 的 `nature` authority，不复制到 SQLite。共享 main 的 CommunityDatabase 本轮未升级、未接入这些表。
- 下一动作：在权威 farm 工作区补农场启动装配、真实身份／旧余额维护迁移、P3／P4 玩法 adapters 与经逐字审阅的玩家入口；未确认数值继续 fail closed。部署和生产激活须另行授权。
- 定向验证：权威 farm 工作区银行／职业／天气／同库四组测试 27／27，原 request／store 安全回归 2／2；目标 JS 语法与限定 diff-check 通过；`HEAD` 与 `origin/farm` 均为 `edbcb33`。未运行全仓测试、启动服务、访问上游、读取玩家存档、deploy 或写生产。

## BUILD-BANK-ECONOMY-BACKEND-20260826

- 模式／状态：施工／银行与统一经济核心已经随 farm commit `edbcb33` 提交并推送至 `origin/farm`，权威资金回执、高消费日累计回退及同库集成验证均已完成。当前仍未接农场运行时／玩家入口，未部署或启用生产。
- 证据读取范围：本任务小节；总集成已完成的权威 farm worktree 机械迁移、定向验证与 commit／push 结果。不得读取玩家数据、生产配置／凭据、其他任务 diff或范围外代码。
- 精确写入范围：本轮只原位更新本任务小节，不再修改共享 main 迁移源、权威 farm worktree 代码、其他任务票、产品／运行／索引文档、Git、部署或生产。后续运行时／玩家入口接线须由对应集成任务按精确范围施工。
- 已确认合同：唯一经济主体为 `resident_id`；金币／银币统一整数账本且余额不可为负；不可变账目、稳定幂等键、同键异参数冲突；真实玩家交易按待确认、冻结、结算／取消及退款原子推进；银币锁定只限制小机新的自主可选消费，不阻断既有冻结结算、退款或债务还款；金币活期与大额定期存款、500 金兑 1 银及月度累进手续费／个人与全服额度、系统金币贷款、真实玩家银币借款、到期冻结利息、三日宽限、失信限制与信用点规则均按已确认方案实现。职业财务只能消费经济层权威落库且以实际 `journal_id` 为 `receiptId` 的封闭回执；六种 kind 固定为 `system_gold_charge`、`system_gold_credit`、`system_gold_reserve`、`system_gold_settle`、`system_gold_release`、`player_silver_settle`，调用方不能提交或覆盖回执字段。
- 未确认边界：系统贷款产生信用点的统一最低存续天数、失信居民其他非必要系统金币消费与非还款玩家银币消费的北京时间当日累计上限仍不得私定；模块构造时必须由接线方显式提供，缺失时相关能力 fail closed。银行 HTTP／MCP canonical op、strict Schema、逐字文案与错误映射也不在本票内。
- 生产激活前合同洞：不可变财务账目当前使用 `resident_id` 作为可核对主体并以 `ON DELETE RESTRICT` 防止级联抹账；这与退群一个月后删除社区管理范围个人数据之间需要另行确认结清、保留／匿名化和公开范围。未确认前不得把居民删除实现成级联删除账本，也不得以账本不可变为由永久保留未获批准的身份资料。
- 已完成 leaf：经济 schema v3 已登记系统金币 reservation、不可变权威财务回执和交易权威冻结时间；`reserveSystemGold／settleSystemGoldReservation／releaseSystemGoldReservation` 分别原子推进 available↔frozen、frozen→treasury。金币系统收费／发放、三种 reservation 和真实玩家银币交易结算返回实际 journal 回执，伪造输入 fail closed。高消费日累计在同一北京时间自然日的 reservation release／trade cancel 中原子扣回；跨日释放／取消保留原日事实且不污染当前日，settle 后不回退。原有账本、锁定、存款、兑换、交易、贷款和跨日停机补结行为保留。
- 下一动作：由总集成另行完成农场运行时／玩家入口、权威旧余额迁移和模型可见协议接线；相关入口、Schema、逐字文案、未确认数值与生产激活仍不得由本票擅自推进。完成接线、事实文档同步与发布边界核对前保留本小节。
- 定向验证：权威 farm worktree 的银行／职业／天气／同库四组核心测试合计 27／27，存档安全回归 2／2；本票原经济定向回归已包含资金回执、reservation、高消费同日回退／跨日保留、settle 不回退及外层事务整体回滚。核心已由 commit `edbcb33` 推送 `origin/farm`；没有启动服务、访问上游、读取玩家数据、部署或生产写入。

## BUILD-CAREER-WORK-BACKEND-20260826

- 模式／状态：施工／职业 leaf、同库经济 authority／journal／ledger 验真及 `lingye-world.sqlite` 容器／外层事务集成已经进入 farm commit `edbcb33` 并推送至 `origin/farm`；当前仍未接入农场 runtime、启动装配或任何玩家入口，未部署、未进入生产，也未触碰真实玩家数据，不能冒充已可用。
- 证据读取范围：本任务小节；总集成已回报的权威 farm 工作区机械迁移结果与定向验证结果。不得读取玩家数据、生产配置／凭据、其他任务 diff或范围外代码。
- 精确写入范围：只更新本任务小节；本轮不再修改共享 main 迁移源、权威 farm 工作区代码、economy、shared package、`dist/{store,index,server}.js`、天气、产品／运行／索引事实文档、前端、Git、部署或生产。
- 已确认合同：首批五职业为料理师、农艺师、动物医生、记者、治安官；每职业四级、每级三门顺序课程和 5 题练习至少答对 4 题；第一职业不可替换，第一职业高级后可开独立第二职业且最多两个；普通考试每日北京时间 12:00／20:00、20 题至少 18 题通过；三类公共机构各 2 个正式席位，正式受聘且未请假／停职者幂等生成次日 `duty_day`；日报社／动物医院／治安署按等级结算金币底薪与真实完成工作的绩效，料理师／农艺师不虚构机构底薪；五职业共用真实对象、负责人、锁、决定记录、物资／付款引用和唯一终态的工作状态机。
- 未确认边界：治安官 24 小时公示的复核门槛、复核人员组成／结果／时限及考官确认后临时缺席处理仍不得私定；相关阶段保存为明确的 `pending_review_configuration`／不可推进。职业 HTTP／MCP canonical op、strict Schema、课程／题目内容、逐字文案、日报模型审核 Prompt、信箱／铃文案以及世界对象具体处理选项不在本票内，不得先注册占位或调用模型。
- 已完成 leaf：权威 farm 工作区的 `dist/career/**` 已覆盖五职业／四级数值、顺序课程／练习／考试／证书、治安官人类考官与匿名公示、三机构两席／排班／底薪绩效，以及真实来源工作、对象锁、决定、转交／失效／终态；schema 与 service 接受外部 `DatabaseSync`，事务适配兼容调用方已有事务，不占用数据库版本。财务回执不再由调用方对象自证：职业按 `receiptId` 查询同库不可变 `economy_financial_receipts` 与实际 journal／全部 ledger entries，权威六字段必须同时匹配调用引用和职业期望；金币收费／工资、考试冻结／结算／释放及玩家银币结算分别验证真实 resident、system 或真实交易对手的双边账目，动态 charge／credit businessType 不由职业侧猜测。旧社区位置已删除。
- 下一动作：只在权威 farm 工作区完成农场 runtime 与启动装配，并接入铃野身份、真实 source／object／option／result、信箱／铃和经审阅的玩家入口；已验证的同库容器不得再接入 CommunityDatabase。完成运行接线、公开合同、事实索引和授权后的 Git／部署动作前保留本小节。
- 定向验证：总集成在权威 farm 工作区回报四组核心测试合计 27／27、存档安全测试 2／2；核心已随 `edbcb33` commit／push 至 `origin/farm`，仍未部署、未进入生产，也未进行 runtime 或玩家入口验收。

## BUILD-LOUNGE-DOUDIZHU-20260823

- 模式／状态：施工／斗地主横屏 UI 与电脑窄预览适配已完成定向技术检查，Python 规则与隔离边界未改；当前保持本地预览并等待辛玥本人视觉验收。
- 证据读取范围：本任务小节；`docs/{product-plan,runtime-architecture,DEBUG_INDEX}.md` 的小机活动室、Game Adapter、存档和视觉边界；`apps/web/src/games/doudizhu/` 页面与交互；上游 `29-Cu/bisca` 斗地主规则证据。不得读取玩家存档、身份、凭据、范围外任务 diff、其他仓库或生产数据。
- 精确写入范围：本任务小节；`apps/web/src/games/doudizhu/{doudizhu-page.tsx,doudizhu-page.css}`；按真实结果更新 `docs/{DEBUG_INDEX,product-plan}.md` 的斗地主现状。不得修改 `games/doudizhu/**` Python 规则、其他游戏、正式休息室、社区数据库／协议／路由、Connector、「铃」、MCP、Prompt、模型调用、银币金额、Git、部署或生产。
- 已确认合同：`doorbell.bisca.doudizhu.v1` 固定三人、17 张加 3 张底牌，按 `0／1／2／3` 叫分并使用已确认牌型、炸弹／王炸、春天／反春和零和结算；不私加明牌、加倍、抢地主、托管或超时。受信座位支持全人类、全小机和混局，预览固定一人类两小机且页面不能选择身份。参与货币为银币，金额、扣退和奖励未确认。
- 设计与实现：整屏保持明亮 Q 版绿色牌桌，不使用人物大卡或交接弹窗。游戏始终使用 `844×390` 横屏逻辑画布，可连续多选后一次提交；电脑预览区域即使是窄高窗口，也直接把完整横屏画布等比缩小并居中显示，不再显示旋转拦截层，不另做竖屏玩法。普通出牌阶段不重复显示事件条。
- 未决限制：电脑动作当前固定 `520ms` 后提交；已向辛玥提出改为 `900ms`，在具体数值获确认前不得改变。
- 下一动作：保持本地预览，等待辛玥本人视觉验收；只按她后续点名的斗地主区域继续修改。若确认电脑动作改为 `900ms`，再单独调整并复测。
- 定向验证：四游戏前端交互合计 `13/13`、定向 Biome、专用 Vite 构建通过。真实 `390×844` 电脑窄预览可看到完整横屏牌桌并叫 3 分取得底牌；`844×390` 横屏回归完整可见，两种窗口都没有旋转拦截层。技术检查完成，等待辛玥本人视觉验收。

## PROTOTYPE-LINGYE-DAILY-UNFOLD-ANIMATION-20260823

- 模式／状态：施工／独立《铃野日报》报纸展开动画样张已生成，等待辛玥本地观看与视觉验收；尚未接入正式页面。
- 证据读取范围：本任务小节；现有 `apps/web/lingye-daily-preview.html`、`apps/web/src/daily/lingye-daily-page.css` 和直接页面结构；只读取其现成日报样式与排版。不得读取真实群聊、日报数据、生产状态、凭据或范围外任务 diff。
- 精确写入范围：本任务小节；新建 `apps/web/lingye-daily-unfold-preview.html`。不修改现有日报组件／CSS／预览、Prompt、协议、Schema、接口、社区接线、Git、部署或生产。
- 已知事实：样张沿用现有《铃野日报》的纸色、字色、报头与栏目视觉；动画表现一份横向对折的报纸沿中间折痕向下展开，并提供重播按钮。样张明确标注为非正式日报，没有放入虚构社区事件或天气；内部使用同一 600×780 逻辑纸张，并根据视窗宽高统一等比缩放。
- 未知／边界：正式入口何时触发、是否只播放一次、以后与真实日报加载如何衔接尚未确认；本轮不替正式交互定案。应用内浏览器的文件 URL 策略阻止自动打开该本地样张，因此首帧、展开过程、终态和三类视口仍未完成实机视觉验收。
- 下一动作：由辛玥直接打开独立样张观看；根据视觉反馈只调整该样张。确认动作后再另行决定是否接入正式日报页面。
- 定向验证：Biome 定向检查通过，限定 diff-check 通过；核心展开动画只变换 transform／opacity，包含 `prefers-reduced-motion`，重播按钮与 600×780 等比缩放逻辑已做静态检查。未冒充浏览器视觉验收通过。

## BUILD-LOUNGE-LEAF-GAME-20260823

- 模式／状态：施工／JSONL worker 最后一手超时死局、横屏预览与电脑窄预览适配均已修复并通过定向技术检查；独立规则和本地预览继续保留，等待辛玥本人视觉与玩法反馈。
- 证据读取范围：本任务小节；`docs/{product-plan,DEBUG_INDEX}.md` 的叶子戏与 Game Adapter 边界；`games/leaf-game/src/doorbell_leaf_game/{engine,worker,preview_server}.py` 及定向测试；`apps/web/src/games/leaf-game/` 页面与交互。不得读取玩家存档、身份、凭据、范围外任务 diff、其他仓库或生产数据。
- 精确写入范围：本任务小节；`games/leaf-game/src/doorbell_leaf_game/worker.py`、`games/leaf-game/tests/test_worker.py`；`apps/web/src/games/leaf-game/{leaf-game-page.tsx,leaf-game-page.css}`；按真实结果更新 `docs/{DEBUG_INDEX,product-plan}.md` 的叶子戏现状。不得修改其他游戏、正式休息室、社区数据库／协议／路由、Connector、「铃」、MCP、Prompt、银币金额、Git、部署或生产。
- 已确认合同：固定四人、52 张牌各持 13 张；保留盖牌报点、跟牌／质疑／认罚、收牌罚饮、递增毒酒概率、醉倒和最终胜负。单次最多 16 张；桌面醉意为牌数乘 `1.5%` 后向上取整，毒酒概率从 `25%` 起递增；最后一手约 `3` 秒质疑窗由牌局服务自动收敛。受信座位支持全人类、全小机和混局，预览固定一人类三小机。参与货币为银币，金额、扣退和奖励未确认。
- 视觉与交互：整屏使用 Doorbell 明亮 Q 版绿色横向牌桌，不复制原作资源，不使用人物大卡或交接弹窗。四席贴横屏画布四边，直接连续点选一张或多张手牌，再点中央牌堆提交；电脑窄高预览把完整横屏画布等比缩小并居中显示，不显示旋转拦截层，也不另做竖屏玩法。
- 下一动作：保持本地预览，等待辛玥本人视觉与玩法验收；后续只修改她点名的叶子戏区域。
- 定向验证：既有 worker 专项回归、Python 定向测试与最后一手真实 JSONL 到期收敛保持有效；四游戏前端交互合计 `13/13`、定向 Biome、专用 Vite 构建通过。真实 `390×844` 电脑窄预览可看到完整横屏牌桌并提交小机推进；`844×390` 横屏回归完整可见，两种窗口都没有旋转拦截层。技术检查完成，等待辛玥本人视觉验收。

## SIMPLIFY-LEGACY-FARM-RIPEN-AND-GLIMMER-CATCH-20260822

- 模式／状态：施工／旧 farm 补齐已作为 `5ae3655321e83bbb68f6c91fdf54d77d0159bec9` 提交并 push 到 `origin/farm`，尚未部署。未来 Doorbell 补齐已整理为仅含 registry、定向回归和产品合同的本地 main commit `dde9d62658e40000bd1beb4821b8a9f332e25ce2`；push 前 `origin/main` 被并行推进到 `35b606970418791f92215a5a2dc4a79c60d88557`，且同样修改 `docs/product-plan.md`，非快进 push 已被 Git 拒绝，未 force、未覆盖远端。新农场 UI 完成前继续使用现有单一 `farm` MCP，Doorbell 仍不开放、不迁移。
- 证据读取范围：本任务小节；`docs/{product-plan,runtime-architecture,DEBUG_INDEX}.md` 的现有 farm／Doorbell 迁移边界；社区 `apps/server/src/{doorbell-farm-op-registry.ts,mcp-runtime.test.ts}`；独立 farm 发布线的 `dist/game/{actions/field.js,help.js,presentation/farm.js}`、`dist/mcp.js` 与 `tools/legacy-farm-simplified-calls.test.mjs`。不得读取玩家存档、身份、凭据、其他仓库或范围外任务 diff。
- 精确写入范围：本任务小节与 `docs/product-plan.md` 对应已确认合同；社区只修改 `apps/server/src/{doorbell-farm-op-registry.ts,mcp-runtime.test.ts}` 中 `farm.ripen` 的 registry／回归；独立 farm 只修改 `dist/game/{actions/field.js,help.js,presentation/farm.js}`、`dist/mcp.js` 与 `tools/legacy-farm-simplified-calls.test.mjs` 中 `ripen` 公开自动分支、模型可见提示和定向回归。不得修改 Doorbell transport、认证、领取／迁移开关、药水价格／套装／每日限购／库存与结算逻辑、存档字段、身份、Human UI、其他无关 action、Git 或生产。
- 已确认合同一：当前 `farm` MCP 的 `ripen` 是严格二选一：`farm({action:"ripen",plots:[1,3,5]})` 只催熟明确列出的地块；`farm({action:"ripen",auto:true})` 复用原自动逻辑，按现有金币和官方商店当日剩余限购自动补药，并尽量催熟全部生长地块。`plots` 与 `auto:true` 不能同传，`auto:false` 不接受。旧 `use` 和 `run.potion` 仅作为隐藏兼容继续执行，不再出现在 HELP、状态提示或工具 description。
- 已确认合同二：当前 `farm({action:"glimmer",op:"catch",animal:2,dish:"料理名称"})` 的 `animal` 接受当天动物列表的数字代号，也继续接受动物名称；无参数流光状态展示当天实际动物的顺序编号，提交哪个代号就解析到当天对应动物，不改变当天列表、捕捉规则或料理参数。
- 迁移边界：本轮不开放或切换 Doorbell；现有玩家继续使用旧 `farm` MCP。未来 `doorbell({op:"farm.ripen",args:{plots:[1,3,5]}})` 与 `doorbell({op:"farm.ripen",args:{auto:true}})` 使用同一严格二选一，等新农场 UI 完成后只处理新连接和逐户切换，不再重做该操作。
- 施工假设：显式 `plots` 表达一次完整目标集合；任一地块不存在、为空、已经成熟、重复，或药水不足时整次不执行，避免模型以为整组已经按指定完成。若真实现有批量结算边界与此冲突，先停下回报，不自行改成部分成功。
- 已实现：旧 farm 的 `ripen` 现可二选一接收 `plots` 或 `auto:true`；自动分支直接调用既有 `autoPotion`，模型可见 HELP、状态催熟提示和 `farm` 工具 description 同步公开两种写法。混传、`auto:false` 和缺少两种参数均拒绝；旧 `use`／`run.potion` 继续隐藏兼容。未来 Doorbell 的 `farm.ripen` 改为两个 strict args 分支并保留统一 adapter；没有修改药水价格、套装、当日限购或结算。
- 下一动作：等待当前 main 文档并行写入收束后，重新核对 `origin/main` 的同文件变更并把本任务干净提交移到最新基线；未协调前不强推、不覆盖。farm 8091 部署仍需辛玥另行授权，Doorbell 不得提前开放迁移。
- 定向验证：独立 farm 定向测试 `4/4` 通过，覆盖精确原子催熟、自动买药并催熟、错误二选一、旧兼容、HELP／状态／工具提示与既有流光代号；四个 runtime 文件和测试 JavaScript 语法检查通过。社区 `mcp-runtime.test.ts` `9/9` 通过，覆盖 `auto:true`、`detail:true`、`auto:false`／混传／空参数拒绝和 adapter 映射；两条发布线限定 diff-check 通过。未运行全量测试，未访问生产玩家。

## BUILD-LINGYE-DAILY-COPIED-UI-SHELL-20260815

- 模式／状态：施工／React 页面壳与可直接打开的静态预览均完成，社区接线待前端重叠任务释放。辛玥明确要求直接照搬下载 UI 合集《铃野日报》的现成 CSS 与排版结构，不做风格概括或重新设计。本窗口误写的产品方案风格段已撤销。
- 证据读取范围：本任务小节；`/Users/doraemon/Downloads/ui合集/铃野日报_files/4ac3f295-57c1-45ee-a01e-87208cc35efc.html` 的 HTML／CSS；`apps/web/package.json`、Vite 配置、现有独立 HTML 入口及其直接组件／测试，只用于复用本仓独立预览接线方式；`docs/product-plan.md` 的《铃野日报》已确认栏目、真实素材和展示边界。不得读取生产群聊、凭据、其他下载内容、外部仓库或范围外任务 diff。
- 精确写入范围：本任务小节；删除本窗口刚写入 `docs/product-plan.md` “报纸展示”段的风格概括；新建 `apps/web/lingye-daily-preview.html` 与 `apps/web/src/daily/**`，用于直接复刻下载页面 CSS／排版和提供诚实空状态／隔离测试。因当前 `BUILD-FARM-HUMAN-UI-UPGRADE-AND-SPLIT-20260815` 正在修改 `apps/web/src/{app.tsx,preview/**}`，本票不得修改这些文件，也不得修改 `apps/server/**`、`packages/protocol/**`、Prompt、真实日报内容、Git、部署或生产。
- 已知事实：`apps/web/src/daily/lingye-daily-page.css` 已直接迁入参考页的 `#f6f0df`／`#3a4b5c` 色值、最大 600px 纸张、`1fr 140px` 主辅栏、栏目标签、引语框、投稿框与 480px 单栏规则；React 页面只接收外部 issue 字段，没有内置虚构日报。`lingye-daily-preview.html` 已移除需要 Vite 转译的 TSX module script，改为相对加载同一份日报 CSS 并内置与 React 一致的诚实空状态，因此 `file://` 不再依赖开发服务。参考页里的《人类观察报》、2023 日期、能量豆、心情天气、虚构统计、无人机和假农场数据没有进入生产组件或静态预览。
- 未知／阻塞：真实日报 API、群聊采集、期次存储和模型生成仍未实现；当前前端主壳与预览文件正被农场任务修改，因此尚未把页面接入社区阅读入口。不能把静态预览冒充真实日报链路或社区内已可阅读。
- 下一动作：社区接线继续等待 `BUILD-FARM-HUMAN-UI-UPGRADE-AND-SPLIT-20260815` 释放 `apps/web/src/{app.tsx,preview/**}`，之后重新读取实时入口并接入现有 React 日报组件，不重画 CSS；真实数据与 API 需另行建立后端／协议施工范围。
- 定向验证：定向测试 4/4 通过，证明七个栏目、诚实未出版状态、原色值、600px、`1fr 140px`、480px 单栏，以及静态预览存在相对 CSS、真实可见 DOM 且不再引用 Vite／TSX；`@doorbell/web` typecheck 通过；限定 Biome 与 diff-check 通过。此前 Vite 本地真实渲染确认桌面纸张为 600px、背景／正文色正确，390px 时页面宽 390px、无横向溢出并显示诚实空状态；生产组件与静态预览限定检索没有参考页虚构日期、价格、数值、心情、穿衣建议或观测站设定。未运行全仓测试、未接社区主壳、未修改后端／协议／Prompt／Git／部署／生产。

## BUILD-FARM-HUMAN-UI-UPGRADE-AND-SPLIT-20260815

- 2026-08-28 发布授权：辛玥已明确要求把本轮纪念册 UI 与当前玩家七夕信笺／灯笼受控读取接线全部 push。发布仍严格拆为 Doorbell `main` 与农场 `farm` 两笔独立提交／推送，只纳入本任务精确文件和 hunks，不夹带共享工作树中的职业考试、铃野共行归档、农场其他窗口或其他无关改动；不 force-push，不部署、不重启、不操作生产数据。沿用辛玥正在编辑期间的限制，本次发布不运行测试、typecheck、构建、lint，也不刷新或操作当前浏览器页面；推送状态与运行／视觉验收分开报告。
- 2026-08-28 七夕纪念册正式数据接入：辛玥确认当前七夕内页视觉与自由排版收口，并明确正式页面必须按当前登录玩家替换四个槽位，不得继续绑定「辛玥／渡」示例。农场权威存档 `qixiLantern2026.lamps.human/ai` 已分别保存人类与本户 AI 放出的灯笺正文和五项成品灯外观；本轮已完成服务间只读投影、Doorbell 登录态受控读取与现有四槽位动态替换接线，浏览器只获得本户的 `human_name`、`ai_name`、两封正文和两组外观，不获得 Human key、其他农场数据、活动答题／奖励／打捞状态或完整七夕私有存档。空态逐字确认：某侧没有正文时显示「无」；某侧没有保存灯时显示默认四角宫灯（月白、无纹样、无挂件、无封签）。前端保留现有四个可排版槽位、坐标、角度、缩放、层级和视觉样式，只在打开纪念册时读取并替换姓名、正文与灯外观；演示编辑稿继续使用本地示例，不把真实私信写入仓库、公共资产、布局 URL、页面存储或日志。精确写入为主线 `packages/protocol/src/index.ts`、`apps/server/src/{qixi-memorial-client.ts,registration-auth.ts,app.ts,index.ts}`、`apps/web/src/{app.tsx,auth/lingye-client.ts,preview/candidate-two-preview.tsx}`、`docs/{product-plan.md,qixi-2026-event-plan.md,runtime-architecture.md,DEBUG_INDEX.md,CURRENT_WORK.md}`，以及农场发布线 `/Users/doraemon/Downloads/doorbell-commons-farm-qixi/dist/server/{qixi-memorial-structured.js,doorbell/human-farm.js,doorbell/router.js}`。不改七夕权威存档结构、活动玩法、模型工具 Schema、Prompt、生产数据、当前编辑 URL、其他活动、Git、部署或服务；按辛玥当轮要求未运行测试、typecheck、构建、lint，也未刷新或操作她正在编辑的页面。当前仅为本地未提交、未 push、未部署状态。
- 墨趣字体实页回退根因与修正：七夕预览运行在 `sandbox="allow-forms allow-scripts"` 的 `srcdoc` 隔离画布，字体样式已声明为 `MoQuGuFengTi`，但画布来源为 `null`，本地 TTF 响应没有 `Access-Control-Allow-Origin`，因此 `document.fonts.check` 为 false 并回退宋体。不得为字体放开 `allow-same-origin` 或给整个开发服务追加全局 CORS；改为生成七夕专用 `moqu-gufeng-ti.css`，在该样式内以 data URL 携带原始 TTF，只让两封信正文与署名使用。保留当前坐标、正文临时注入和编辑状态；不刷新或测试页面。
- 最新信笺字体纠正：辛玥要求七夕详情两封信笺正文与右下署名都从京华老宋体繁体改为墨趣古风体，正文同时由 `11px` 收小为 `9px`、署名由 `10px` 收小为 `8px`；仅接入该字体并修改两处字号／字体声明及直接断言，不改信纸、行距、坐标、用户当前编辑布局或其他页面；按辛玥要求不做页面测试。
- 最新七夕纪念素材纠正：七夕内页副照片不再使用与主图同类的生成灯河图 `qixi-2026-lantern-night-alt.jpg`，改为从当期历史正式素材恢复的归还旧物场景 `qixi-2026-objects-return-bg-v3.jpg`；这是原活动 `objects-return-bg-v3.jpg`，不是新生成图片。两封横向信笺和两盏成品灯同时移除 CSS `drop-shadow`，保留信纸图片本体、正文、署名、灯笼合成层及辛玥当前 URL 中的位置、角度、缩放、层级和删除状态不变。本轮只改 Candidate Two 对应素材引用／样式、直接断言、新增该历史活动图片副本与事实文档；不刷新或测试辛玥正在编辑的页面，不写布局 URL，不改其他素材。
- 最新七夕内页一次性编辑器及存档纪念物纠正：辛玥要求把七夕活动详情页的现有内容接入可视化自由排版，而不是继续由实现侧猜位置；进一步逐字澄清“全部七夕素材”重点不是灯型、纹样、挂件等制作零件，而是活动结束后已经保存的完整成品灯、对应原始信件，以及七夕角色翘翘。当前排版须明确提供四个完整纪念物槽位：“辛玥的灯／渡的灯／辛玥的信／渡的信”；先从农场权威存档只读取得两盏灯的真实 `appearance` 和两封对应私密原文，临时注入当前本地编辑预览供辛玥定位置，后续正式受控接口只替换槽位数据而不改布局。真实信件正文不得写入仓库、公共素材、布局 URL 或长期浏览器存储；代码只保存四个槽位并复用活动原灯笼合成渲染。两封信的底板改为本轮独立生成的无字横向长方形暖白信纸 PNG `qixi-letter-card-v1.png`：女性向七夕手帐质感、淡紫细装饰，纸面本体实色且外部为真实 alpha，不得画棋盘格，也不得把正文、署名或其他文字烘焙进图片；正文继续临时注入，正文与右下「辛玥」「渡」署名统一使用京华老宋体繁体网页字体，现有位置、角度、缩放和层级参数不变；不再使用旧七夕花边灯笺素材或 CSS 叉号；另提供翘翘完整素材。仅在开发构建且 URL 明确带 `editor=memorial-entry-layout` 时开放；所有元素可在同一活动纸面内自由拖动、旋转、等比例缩放、调整真实遮挡层级、复制与删除，结果只写回当前开发预览 URL 的 `memorialEntryLayout`，不得覆盖辛玥当前已排参数、写服务端或改变活动权威数据。精确读取扩至当前生产农场的七夕存档，只读定位辛玥与渡的两盏灯和两封信；精确写入限 `apps/web/src/preview/{candidate-two-preview.tsx,candidate-two-preview.test.ts}`、新目录 `apps/web/public/lingye/memorial/qixi-archive/**` 与 `docs/{product-plan.md,CURRENT_WORK.md}`；不改七夕原活动资产、生产存档、其他活动内页、目录默认稿、后端／协议、Git、部署或生产。
- 最新返回／目录标题尺寸纠正：辛玥实页确认目录与七夕内页两个返回图形仍整体过大；本轮把两层返回的可见菱形底牌、箭身与箭头统一再缩小，继续保留 `40px` 点击热区、现有屏幕锚点和无障碍名称。目录顶部整张 `Album／纪念册／往期活动回顾` 标题组同步缩小但不改变辛玥已定的中心位置、图片内容或层级；不扩散修改主体纸页、票券、分类、其他页面或返回控件。
- 最新七夕内页排版纠正：辛玥实页指出当前稿是“大标题海报＋横幅主图＋说明书段落”的直排，主题便笺和第二张活动图被缩成右侧附件，页面下半段大面积空白，未形成参考图的手帐层叠关系。七夕详情不再展示实现侧补写的路线说明段落，也不新增任何活动文案；只保留已经确认的「灯河有信」、真实日期与主题句「愿今夜所有思念，都能顺水抵达归处。」。页面必须只用已经在项目内的两张正式七夕活动图、`qixi-letter-note.webp` 灯笺纸与本轮七夕专属标题／装饰 PNG 重新排成错落拼贴：主图改回照片卡而非商品横幅，第二张图明显放大，灯笺纸与照片形成压边／叠放，内容结束后纸页收口，不保留当前无内容的大段空白。参考图底部「活动回顾」「七夕纪念手帐」入口仍不接入，其他活动继续单独设计。
- 最新活动内页复用边界与七夕专属设计：辛玥提供新的手帐活动页参考并明确，所有活动内页只共用连续淡紫白纸背景、返回锚点与基础照片容器；标题字图、装饰贴纸、照片构图和文字排版必须按每一期活动单独设计，不能把七夕装饰变成通用模板。本轮只为现有七夕「灯河有信」生成并接入两张透明 PNG：`apps/web/public/lingye/memorial/qixi-title-lockup-v1.png` 使用项目现有文案「灯河有信」「灯河相逢 · 愿思念抵达归处」，`apps/web/public/lingye/memorial/qixi-stickers-v1.png` 使用七夕吊签、流苏结、河灯、星点／蝴蝶等无额外文案的专属装饰；参考图只用于女性向游戏手帐的层级与材质，不照抄其标题、日期、奖励、按钮或其他活动内容。代码必须以七夕专属 modifier 隔离这些资产，未来其他活动不得自动复用。
- 最新活动内页点名纠正：辛玥实页指出内页返回键与上一层目录返回不在同一位置，且按钮外围带了不应出现的底色；两层返回必须使用同一屏幕锚点和同一透明点击区，只保留轻量返回图形本身。内页正文不再单列大号「活动回顾」栏目标题，保留真实路线叙事文字直接排在纸面；第二张正式活动图不得被整段正文推到视窗最下方只露一角，须与叙事文字组成同一组手帐拼贴、在首屏后半段可完整辨认。辛玥所说的“背景”明确指活动内页整张底图，不是目录活动票券；前一轮误改的票券双层纸背景立即撤回。旧 `memorial-album-shell-v2.jpg` 的纸页在正文中途出现下边缘，使内容掉到桌面花饰区；本轮新增 `apps/web/public/lingye/memorial/memorial-entry-paper-v1.png`，保留淡紫白纸、顶部花／丝带和左侧装订，只把纸面及细边框连续延伸到底，去除中途页底、回形针、底部桌面／信封／花／散落丝带且不含文字、卡片、照片或按钮。主图、主题便笺、真实文字和图片资产不变。
- 最新返回控件点名：当前纪念册返回键虽用了菱形，但尺寸、底牌比例和箭头形态仍未贴近辛玥提供的示意图，且整体过大。目录返回铃野地图与活动内页返回目录统一收为小号半透明深紫菱形底牌，使用更细长的白色左箭头；只缩小可见图形，保留原点击目标、无障碍名称与触控热区，不扩散修改流光原野、铃野共行或其他页面的返回样式。
- 最新标题组点名返工：辛玥明确选定较早生成的华丽白色标题稿 `exec-96074ff1-5fe8-45de-b0a1-0fe1ab1e5877.png`，其中花体英文 `Album` 位于装饰体中文「纪念册」上方，副题在下方居中并配两侧细线／星点；之后生成的简化字稿属于误覆盖，立即废弃。项目资产只从这张已选稿抠除纯黑底转为真实 alpha，不改变字形、装饰或组内排版；页面显示尺寸同步缩小，不能再让标题组占满顶部。写入范围仍限 `apps/web/public/lingye/memorial/memorial-title-lockup-v1.png`、Candidate Two 纪念册引用／样式与直接断言及本任务／产品事实；本轮不改主体纸页、活动票券、数据、内页或其他页面。
- 最新标题位置点名：辛玥确认不再由实现侧猜标题坐标，要求自己拖动定稿。显式开发参数 `editor=memorial-layout` 临时重新开放，并把整张 `memorial-title-lockup-v1.png` 标题组作为一个可拖动／缩放／旋转／调层级的编辑元素接回现有一次性编辑器；不拆开或改写图片内文字，不改变纸页、活动票券、底层形状及其当前参数。改完直接打开本地编辑器供辛玥调位，普通 `screen=memorial` 仍不显示编辑工具。
- 最新定稿参数：辛玥已完成标题调位并逐字提供最终布局 JSON。默认稿必须按该 JSON 回填：`title-lockup scale=0.97/x=-22.9/y=-16.4/z=10`，主体纸页 `x=49.5/y=27.4`，其余分类、票券、图片和底层形状参数保持所给值不变；随后从当前本地 URL 移除 `editor=memorial-layout` 与临时 `memorialLayout`，关闭一次性编辑器并查看普通纪念册。不得再对最终坐标做实现侧微调。
- 最新分类文字点名：纸页上沿露出的左、右两个方签必须分别显示「全部」「节日」。当前右签默认文本被上一份布局 JSON 保存为空，左签文字也随方签下半部被纸页遮住；本轮只把右签默认文本恢复为「节日」，并把两签文字对齐到露出的上半截。方签坐标、尺寸、颜色、旋转、缩放、负层级和“纸页遮住下沿”的既有遮罩关系全部不动，改完退出编辑器查看普通纪念册。
- 最新分类字形纠正：普通纪念册实测中两个方签被做成普通横排小宋体，且左侧「全部」在浅色签上仍使用白字，既看不清也不符合参考。辛玥明确纠正：不要竖排、不要生成字图，「全部／节日」都保持横排，直接换成字号更明确、字重更粗、颜色更深的宋体。两签改为可点击的「全部／节日」方签按钮；方签本体坐标、尺寸、颜色和纸页遮罩关系不动。点击「全部」显示全部已有档案，点击「节日」只显示已有节日档案并更新选中态，不虚构其他分类或活动。
- 最新方签状态纠正：未选中的「节日」不能通过整块 `opacity` 变成半透明；两张方签始终保持用户定下的不透明填色，只用克制的内描边与字色区分选中态。横排粗宋体字号从当前过大的效果收小，保留清晰字重但不显笨重。
- 最新活动票页字阶纠正：七夕活动卡当前编号、标题和图片操作牌都过重，三栏挤成普通功能列表，缺少纪念册票页的精致层级。本轮只在既有卡片位置、宽度和正式图片内收细排版：编号与活动标题缩小，类别、主题、年份和日期拉开层级并使用较轻衬线／宋体，列间增加克制虚线分隔，图片白边与「查看回忆」压轻；不改活动真实文字、日期、图片、卡片坐标或进入内页行为。
- 最新活动内页返工：浏览器实测当前内页抬头过轻、日期挤在右侧，主图过大，主题便笺压在主图下缘，回顾正文又套了一块生硬白卡，第二张照片从视窗底部硬截出来。按已经确认的手帐内页方向，只重排为连续竖向阅读流：活动抬头与日期组成一组、主图独立照片卡、主题便笺独立贴纸、回顾正文直接排在整页纸面、第二张照片完整进入后续流；保留现有真实标题、日期、主题句、回顾正文和两张正式图片，不改数据或新增假内容。
- 最新点名微调：普通纪念册预览关闭一次性编辑器后，底层淡紫 `shape-1` 在纸页下方仍多露出约 17.5px。辛玥要求只把该底层形状的纵向长度从下边缘向上收短，直到其下边缘与上层主体纸页的下边缘重合；横向宽度、位置、颜色、旋转、层级及其余目录排版全部不动。实测当前视窗中底层形状下边缘为 `675.40px`、上层纸页下边缘为 `657.94px`，本轮只对 `shape-1` 写入对应的纵向基础尺寸并做同视窗边缘复测。
- 最新确认：辛玥已确认当前纪念册目录布局可以收口，并要求先关闭一次性编辑器。关闭只移除开发调位工具与选择框，不得丢失当前 URL 中已经保存的布局或回退旧版；当前稿须成为普通 `demo=full` 纪念册目录的默认布局，后续不带 `editor=memorial-layout` 仍按同一参数渲染。当前 accepted 参数从用户现有预览 URL 原样读取，不凭聊天中的旧 JSON 覆盖；仍只修改 Candidate Two 纪念册预览、直接断言与本任务／产品事实，不碰内页、后端、协议、Git、部署或生产。
- 模式／状态：施工／顶部标题资产已恢复为辛玥点名选定的华丽白色 `exec-96074ff1-5fe8-45de-b0a1-0fe1ab1e5877.png` 版本，项目 PNG 只做纯黑底到真实 alpha 的转换；误覆盖的后续简化字稿已废弃。目录标题显示宽度已继续从 `152px` 收到 `128px`，中心位置与用户定稿参数不动；目录与内页返回的可见菱形统一收至 `18px`、箭身收至 `11px`，`40px` 点击热区和现有锚点保留。辛玥本人完成的最终 JSON 已写入默认稿：`title-lockup scale=0.97/x=-22.9/y=-16.4/z=10`、主体纸页 `x=49.5/y=27.4`，其余位置、缩放和层级参数保持同份 JSON；不再做实现侧微调。两个方签默认文字固定为左「全部」／右「节日」，改为横排 `10px` 粗宋体可点击按钮并始终保留不透明填色，只用内描边与字色区分选中态；浏览器实测两种筛选的 `aria-pressed` 切换正确，现有七夕节日档案在两种筛选下均按规则可见。七夕活动票券在不改变用户定稿坐标、`scale=0.66` 和进入行为的前提下重排为 `45px / 正文 / 118px` 三栏；误把“背景”理解成票券后增加的双层纸效果已撤回。活动内页已真正换用新生成的连续淡紫白纸面 `memorial-entry-paper-v1.png`（`853×1844`），背景按页面实际高度完整铺满，正文下方不再出现旧图的页底、回形针、桌面花饰或散落丝带；内页返回与目录返回落在同一屏幕锚点，点击区及菱形内部透明。大号「活动回顾」标题与实现侧补写的路线说明段落均已删除；七夕内页只保留确认标题、日期与主题句，项目已有主图、第二张正式活动图和 `qixi-letter-note.webp` 灯笺纸重新排成主照片卡与下方错落叠放的拍立得／便笺，第二张图由 `82px` 放大至 `128px`，七夕页最小高度从 `820px` 收到 `650px`，不再保留下半页大段空白。七夕内页专属 modifier `candidate2-memorial-entry-view--qixi` 只在该期接入真 alpha、实色本体的 `qixi-title-lockup-v1.png`（`1555×571`，项目原文「灯河有信」「灯河相逢 · 愿思念抵达归处」）与 `qixi-stickers-v1.png`（`866×1572`，七夕吊签／流苏结／流灯／紫藤／蝴蝶）；生成器原始假棋盘格已清除，标题及装饰本体不透明，外部背景才透明。照片继续使用项目已有 `qixi-2026-lantern-night.jpg` 与 `qixi-2026-lantern-night-alt.jpg`，未生假活动图；参考图底部「活动回顾」「七夕纪念手帐」入口未接入。浏览器当前实页已刷新到七夕内页，两张 PNG 自然尺寸正确且计算 `opacity=1`，无 warning/error；其他活动只继承连续背景、返回锚点与基础照片容器，不继承七夕标题、贴纸、照片构图或文字排版。当前本地标签已移除编辑参数，工具栏隐藏，并停在新版七夕活动内页供视觉检查。对应 Candidate Two 定向测试 1/1、两文件 Biome 与限定 diff-check 通过；目录标题资产为 `1200×680` 真 alpha PNG。未 commit、push、deploy 或操作生产。
- 2026-08-28 铃野纪念册一次性自由编辑器继续点名修正：辛玥确认所有目录元素必须叠在同一张通用底图画布内，底图是唯一显示边界；标题、纸页、票券、分类块和新增形状仍可不受限制地自由拖动、缩放和旋转，超出底图范围的部分只由画布 `overflow` 遮罩裁掉，不得通过坐标、中心点或“总溢出量”算法限制操作。前版只提供“复制布局”而未自动保存，刷新会丢失辛玥已经调好的参数，属于明确缺陷；本轮以辛玥最后粘贴的 JSON 为默认稿（标题 `x=-27.7/y=-20.1`；纸页 `color=#f8f1f5/x=58.8/y=26.9/scale=1.44/rotate=2.1`；两个分类块统一 `#dfc6e2`；活动卡 `color=#ffffff/x=-51.4/y=-56.9/scale=0.66/rotate=0.6`），每次改字、改色、拖动、缩放、旋转、层级或形状增删后通过受控 iframe action 自动写回当前预览 URL，刷新从 URL 恢复时不得用默认值覆盖已保存稿；不得为该施工编辑器引入页面存储或直接访问沙箱父页面。原生 `EyeDropper` 不可用时，由外层同源页面读取点击位置的底图真实像素，再通过受控消息只回传 `#RRGGBB`；不得放宽 iframe 沙箱。层级按钮一次必须跨过相邻的真实 `z` 层，不能再把 `10` 机械改成视觉无变化的 `9`。活动票券保持最新 `scale=0.66`，单独增加基础横向宽度，不能再显示成过短窄条。顶部“已收录活动”和后续“活动时间／日期”摘要整组删除，不留空占位，也不进入编辑参数；活动票券自身的真实日期保留。编辑结果只用于本次目录调位，不写服务端、不改权威活动数据；入口继续受 `import.meta.env.DEV` 与明确参数双重门控。精确写入仍限 `apps/web/src/preview/{candidate-two-preview.tsx,candidate-two-preview.test.ts}` 与 `docs/{product-plan.md,CURRENT_WORK.md}`；不改底图文件、纪念册内页、其他页面、后端／协议、Git、部署或生产。
- 2026-08-28 铃野纪念册目录点名返工：前一版把「全部／节日」做成正文里的普通横排按钮，未落实辛玥所说的“放在上面的书页边缘”；标题仍偏在左上且保留无意义横线，目录构图也未按参考图重排。辛玥进一步逐字澄清：分类不是带尖尾、吊绳或书签轮廓，而是参考图左侧现有的方形分类块整体挪到主体册页上边缘，两个正方形横排，一个深紫、一个浅紫，不进入正文流；层级必须是方形分类块在后、册页边缘遮住分类块下沿、册页正文在前，不能用高层级把分类块压成漂浮按钮。辛玥最终决定重新生成不含本子的通用底图：底图只允许深紫夜色、花、丝带、星光与留白，不得画本子、纸页、方框、书签、文字或活动内容；主体纸页、页沿遮挡、分类方块与活动票券全部由 HTML/CSS 独立控制。本轮只返工纪念册目录；七夕真实字段、正式图片、目录到内页交互、Live 空态和具体活动内页内容不变。前一张带假书签与整本空白纸的生成稿已废弃且未写入项目。精确写入限于 `apps/web/src/preview/{candidate-two-preview.tsx,candidate-two-preview.test.ts}`、`apps/web/public/lingye/memorial/memorial-album-backdrop-v1.jpg` 与 `docs/{product-plan.md,CURRENT_WORK.md}`；不改纪念册内页、铃野地图、铃野共行、后端／协议、Git、部署、生产或其他页面。
- 2026-08-27 铃野纪念册 UI 最新要求：辛玥要求目录不是只换底图，而是照参考图复刻完整层级：顶部独立标题与副题、下方整本冷白淡紫册页、册页顶部真实收录信息、分类纸签、纵向编号活动票页，以及票页内左侧编号／年份、中部活动类别／活动名／真实日期、右侧正式活动横图与“查看回忆”。实际截图确认当前侧边纸签挤压票页、标题过大、活动名折成两行并导致层级混乱；按辛玥最新决定，分类纸签改到册页上方横排，标题缩小，票页铺宽，`七夕活动` 固定一行，重新分配编号／正文／图片宽度。目录是所有限时活动的通用入口，通用底图、标题与分类不得绑定七夕；当前 Demo 只填已经确认的 `01／2026`、七夕「灯河有信」、正式窗口 `2026.08.19—08.21` 与正式夜河灯图，不虚构第二期。目录字体使用细宋体书刊层级与克制衬线数字；通用册页底图为顶部紧凑标题区、册页从约上方六分之一开始并占据剩余主体的 `memorial-album-shell-v2.jpg`。具体活动内页按最新参考图落实为顶部活动抬头／日期、正式 v4 主图、正式灯笺纸主题便笺、回顾正文区与正式 v3 错落照片；不照抄参考图中的奖励、操作按钮或不存在的数据，也不生成假照片。精确写入限于 `apps/web/src/preview/{candidate-two-preview.tsx,candidate-two-preview.test.ts}`、`apps/web/public/lingye/memorial/{memorial-album-shell-v2.jpg,qixi-2026-lantern-night.jpg,qixi-2026-lantern-night-alt.jpg,qixi-letter-note.webp}`、`docs/{product-plan.md,CURRENT_WORK.md}`；不改纪念册入口图、铃野共行、后端／协议、Git、部署、生产或其他页面。
- 2026-08-27 业主档案活动纸点名纠正：撤销未经要求的 `72%` 收窄和增高，`Recent Activity` 恢复原宽度、`196px` 最小高与原叠放位置；参考图落实左上专用暖银细线手绘回形针、居中手写标题、每条真实活动前的小爱心标记与点状横线排版，并保留浅粉沙纸纹和柔和撕边。内置生图两次产生烘焙棋盘／白底，未把假透明资源写入项目；最终使用真正透明的专用手绘 SVG，不再复用设置页的写实银色素材。精确写入限于新活动纸回形针资源、Candidate Two 活动纸引用／直接断言及本任务事实文档。活动内容、时间、More／Less、来往图、其他档案内容与数据合同不变；不照搬清单文案或蝴蝶结，不改 Git、部署、生产或辛玥浏览器。
- 2026-08-27 铃野共行往期故事阅读页点名反馈：具体故事改为整页连续绘本纸面，正文容器删除卡片边框、圆角、底色与阴影；只有正式插图保留独立照片卡，并加入克制的纸胶带与星形贴纸。期数／本期标题、段落标题和正文改为更明确的绘本式字号、宽松行距和留白，目录、权威故事内容、翻页逻辑、素材映射与数据合同不变；精确写入限于候选页、直接源码断言与产品／任务事实文档，不改 Git、部署、生产或辛玥浏览器。
- 2026-08-27 农场地块详情点名反馈：地块详情不再贴在场景底部并被帮收条／底部导航遮住，改为在视窗场景内水平、垂直居中，使用 `82cqw` 固定逻辑宽度并提高到帮收条之上；只改地块详情锚点与对应源码断言，不改帮收条、地块、底部导航、数据合同、Git、部署、生产或辛玥浏览器。
- 2026-08-27 业主档案点名反馈：关系图的 `Edit` 纸签保持锚在关系图右侧边线的下半段，并按最新反馈整体向上移动 `20px`，定位收口为 `right: -16px; bottom: 124px`；只改该 `Edit` 纸签定位与对应源码断言，不改关系节点、编辑器、档案内容、其他装饰、数据合同、Git、部署、生产或辛玥浏览器。
- 2026-08-27 铃野共行入口与往期回顾等待视觉验收：入口图标按现有纪念册的正方形像素游戏底牌重画，保留统一题材但放大「铃野共行」文字，地图内显示尺寸已按反馈缩小；往期回顾结构按最新反馈收口为“往期目录 → 点击第一期／第二期 → 该期实际路线逐段翻页”，目录顶部显示「往期故事」，进入具体故事后该目录顶部栏完整隐藏，只保留返回目录、期数和本期标题；已修正顶部栏自身 `display: grid` 覆盖 `hidden` 导致详情页仍显示的问题。每一段权威历史记录都携带对应受控插图 key，料理复现与营业订单等实际发生的中间经过不得省略，同阶段段落可复用该阶段已有正式图；Human 翻页统一只呈现插图、段落标题和实际内容，不显示内部类型、任务进度或完成计数。页面只使用清透象牙白与浅绿色层级，不再保留黄褐厚重纸色，也不展示备选项、票数或“玩家选择了什么”的说明。只读核对已确认 farm 存档和 `publicExpeditionHumanData()` 本来保存／返回完整 `archives.history`，第二期已有 8 张阶段／结局图，第一期已有开场、三段路线与四个结局共 8 张正式图；现有 Human structured read 只给整期一个图且 Demo 手写漏掉了中间经过。权威投影、严格合同、两期 Demo 全路线和逐段受控素材映射已经补齐；farm 直接投影测试、server／web client 与候选页定向测试、protocol／server／web typecheck、限定 Biome 均通过，独立窄屏预览已核对目录、第二期入口及做饭经过页，未操作辛玥现有浏览器。精确写入范围为权威 farm worktree 的 `dist/server/together-structured.js` 与直接测试，main 的 `packages/protocol/src/index.ts`、`apps/server/src/{farm-lingye-client.test.ts,registration-auth.test.ts}`、`apps/web/src/auth/lingye-client.test.ts`、`apps/web/src/preview/{candidate-two-preview.tsx,candidate-two-preview.test.ts}`、版本化入口图标与第一期 8 张等比例压缩图 `apps/web/public/lingye/{lingye-together-game-icon-*.png,together/river-*.webp,together/ending-*.webp}`、`docs/{product-plan.md,CURRENT_WORK.md}`。新字段只读最近往期实际路线，farm 投影即过滤 `choice` 并丢弃参与者／奖励等内部字段；不改剧情正文、任务／选择／推进／结算／保存机制、其他铃野页面、Git、部署或生产。
- 2026-08-26 熔炼／商店数量角标与流光原野事件本批已由辛玥确认并推送：数量角标统一压在素材／商品整张卡片右上角，熔炼同种素材可连续选择，三页商店点击商品卡累加；流光原野公共事件按权威时间显示最近最多 10 条及北京时间月日／时分。运行代码与候选预览测试共 9 个文件提交为 `a02e5638dace2e1e93c8dd39588e5913d88160e6`，`origin/main` 已与该 SHA 一致；提交未包含共享文档、服务端、旧农场、其他游戏或其他窗口改动，未部署或操作生产。
- 2026-08-26 农场木牌信息收口完成：木牌现在只渲染 `GET /api/farm/field` 的真实农场名和门牌；佩戴称号与欢迎语已从木牌 props、DOM 和专用样式中移除，但仍保留在各自权威数据和设置入口，不改合同。独立季节／土地环境状态、木牌贴图、尺寸与位置均未改。精确改动限于 `apps/web/src/farm/{page/chrome.tsx,page/farm-field-content.tsx,farm-page.css,farm-page.test.tsx}`、`docs/{product-plan.md,CURRENT_WORK.md}`；farm source 41/41、web typecheck、四文件限定 Biome 与限定 diff-check 均通过；未操作 Git、push、部署、生产或浏览器。
- 2026-08-26 部署前一致性修复已推送：多数 farm mutation 成功后统一等待 generation-aware `refreshField()` 并强制重读已经加载的 catalog／kitchen／ranch，页面刷新也重读已打开资源，设置目录重新加载时同步重置初始化门闩；市场交易会刷新已打开的料理库存，帮收继续使用最新权威 field revision。Bell refresh 现在先批量过期该居民超过 24 小时的购物请求、取消对应 pending wake 并发送一次 cancel，再列出仍有效的 wake；购物请求路由在读取当前商店前先按 resident + idempotency key 查询既有请求，相同稳定购物车 body 直接重放首次成功，不同 body 返回幂等冲突。所有 field 重读收口到同一个 AbortController + generation 入口，旧响应不能覆盖新刷新。10 个对应文件提交为 `dd5ae4eb5d039b7e23f48786745dfba1c852d2d4`，`origin/main` 已与该 SHA 一致。最终复核：farm interaction 29/29、Bell／购物请求 11/11、session route 1/1、web／server typecheck、10 文件限定 Biome 与 cached diff-check 均通过；未改 field hash、玩法、视觉或 PWA，未部署、操作生产或浏览器。
- 2026-08-26 拆分发布事实：12 个 Human UI façade／叶子／source-test 文件已独立提交为 `4b3fa94350501a121f44c1d716735ba41135b632` 并 push `origin/main`；远端 `origin/main` 已与该 SHA 一致。提交前 source 197/197、interaction 24/24、web typecheck、12 文件 Biome 与 cached diff-check 通过；共享 `CURRENT_WORK` 与其他窗口脏改动未进入提交，未运行生产 build、部署或生产验证。
- 2026-08-26 第五批自审护栏完成：只在 `apps/web/src/farm/farm-page.test.tsx` 新增一条直接读取 root 的 façade 契约测试，精确锁住 `import "./farm-page.css"` 与 `export { FarmFieldContent } from "./page/farm-field-content"`；实现、CSS、其他测试与既有断言均未改。最终 source 197/197、单文件 Biome 与限定 diff-check 通过；未重复运行不受影响的 interaction／typecheck，未操作 Git、浏览器、构建、部署或生产。
- 2026-08-26 第五批前端结构拆分完成：实时对象确认在主仓；`farm-page.tsx` 已从 1630 行收为 14 行稳定 façade，只保留 CSS、DEV 工具编辑器开关、`FarmPage` 与 `FarmFieldContent` 兼容导出。新 `page/farm-field-content.tsx` 1026 行承接六个顶层 lazy scene/panel 与完整场景／购物编排，新 `page/live-farm-page.tsx` 619 行承接完整权威读取、动作提交、AbortController／requested resource set、preview no-op、同 UUID retry、resource replacement 与 settings 初始化门闩；两段分别与迁移前源码逐行对照，除相对路径和 `LiveFarmPage` 新增导出外零差异，叶子无反向 façade import。`farm-page.test.tsx` 只增加两条 source fixture 并把既有断言指向真实源码归属，40 个 farm source 测试、断言数量与 interaction test 均未增删。主协调者独立复核 source 196/196、interaction 24/24、web typecheck、四文件限定 Biome、tracked diff-check、行数和反向依赖均通过；interaction 仅输出既存 Vite native-config 未来提醒。未运行生产 build 或浏览器，未改 CSS、视觉、DOM／className、文案、数值、panels/scenes/dev/assets/auth/client/protocol/server、Git、部署或生产。前端职责拆分至此收口，后续不再为了行数继续切小组件，除非出现新的真实变更热点或功能边界。
- 2026-08-26 第四批前端拆分完成：`farm-page.tsx` 已从 3536 行降至 1630 行，六个 `farm/page/**` 叶子共 2055 行承接共享模型／retry、页面 chrome、帮收与牧场收取反馈、居民详情局部状态、料理方式／工具／权威食材映射／prep／picker／真实与预览回执；root 继续独占顶层 lazy scene/panel、`FarmFieldContent`／`LiveFarmPage` 的全部 state/effect/callback/request、AbortController／requested set、visited hidden 挂载、三类 cart 与同 UUID retry、settings 初始化门闩和最终 DOM 编排。`farm-page.test.tsx` 只增加 6 条 source fixture 并修正 5 处源码归属，断言、数量和 interaction test 未改；主协调者独立复核 source 196/196、interaction 24/24、web typecheck、8 文件限定 Biome、无叶子反向 façade import与 tracked/untracked whitespace，均通过。未运行生产 build 或浏览器，未改 CSS、视觉、文案、数值、client/protocol、Git、部署或生产。下一批若继续收缩 1630 行 root，必须先只读区分 882 行场景／购物编排与约 560 行 Live authority adapter，继续保留 stale request、AbortController、幂等 attempt 和 resource replacement 边界，不创建通用万能 hook。
- 2026-08-26 第四批前端 façade 单一集成范围：第三批已严格只新建 `apps/web/src/farm/page/{model.ts,chrome.tsx,action-feedback.tsx,ranch-resident-detail.tsx,cooking/{model.ts,prep-overlay.tsx}}`，共 6 文件／2055 行；共享模型／retry、页面 chrome、帮收／牧场收取反馈、居民详情局部状态、料理模型／食材映射／prep／picker／真实与预览回执均保持原 JSX／类型／逻辑，仅有 Biome 换行和 picker option 类型归属变化，新叶子无反向 `farm-page.tsx` import，web typecheck 与 6 文件限定 Biome／whitespace 通过。现在只允许单一集成人 C 修改 `apps/web/src/farm/farm-page.tsx` 与 `farm-page.test.tsx`：从 root 删除已迁出的定义并显式 import 新叶子，保持顶层 lazy scene/panel、`FarmFieldContent`／`LiveFarmPage` 的全部 state/effect/callback/request/AbortController／requested resource set、visited hidden 挂载、cart／retry attempt／UUID、settings 门闩、DOM 顺序和 `FarmPage` export；source test 只给 `readFarmSources()` 增加 6 个路径，并在源码归属变化导致截取失败时把对应读取来源改到精确叶子，不修改任何行为断言、期望数量或 interaction import。禁止改新叶子、CSS、panels/scenes/dev/assets/auth/protocol、视觉／文案／数值／请求、interaction test、Git、浏览器、部署或生产；验收为 root 行数、source 196/196、interaction 24/24、web typecheck、8 文件限定 Biome、无叶子反向 façade import 与限定 diff-check。
- 2026-08-26 第三批前端叶子精确范围：用户已授权继续拆当前最大前端文件；实时 `main` HEAD 仍为 `17654f9506db9bd5b8a01fb5f610fc7dc2a831b8`，`farm-page.tsx` 3536 行且当前同语义写入只属于本票，拆前 source 196/196（其中 farm 源码边界 40 项）、interaction 24/24、web typecheck 通过。只读审计确认本批先保留 `FarmFieldContent`／`LiveFarmPage` 的全部共享 React state、visited scene 挂载、购物车／UUID retry attempt、settings 初始化门闩、authority resource、AbortController／requested set 与 13 个 action adapter，不抽通用 hook。并行小工 A 只新建 `apps/web/src/farm/page/{model.ts,chrome.tsx,action-feedback.tsx}`，机械迁出 action/resource/cart/draft 类型与初始化／resource routing／retry 纯函数、返回刷新／铭牌／环境／scene tabs／余额／工具栏、field harvest 与 ranch collection 的控制／receipt／notice；小工 B 只新建 `apps/web/src/farm/page/ranch-resident-detail.tsx` 与 `apps/web/src/farm/page/cooking/{model.ts,prep-overlay.tsx}`，机械迁出居民投影／布局／详情局部 action state，以及料理方式／工具／分类／权威食材映射／raw ref／prep picker／真实与预览回执／sprite。所有新叶子不得 import `farm-page.tsx`，不得修改 root、CSS、现有 panels/scenes/dev/assets/client/protocol/test；保持 JSX、className、DOM 顺序、局部 state、React identity、lazy 顶层稳定性、always-mounted hidden overlay、文案、数值和请求行为。新叶子语法／typecheck 可导入后，再由单一集成人另批只修改 `farm-page.tsx` 与 `farm-page.test.tsx` 的 source fixture／截取来源，interaction 测试调用方式与断言不得改；禁止浏览器、Git、部署或生产。
- 2026-08-26 第二批结构拆分完成：`farm-page.tsx` 已从 4047 行降至 3536 行；新 `farm/dev/farm-tool-layouts.ts` 207 行作为正式工具栏与编辑器唯一布局事实源，新 `farm/dev/farm-tool-editor.tsx` 338 行承接 `#farmTools=` hash 读取／导出、pointer 拖拽、滑杆、重置和完整临时编辑 UI。`farm-page.test.tsx` 只增加新源码 fixture 并保持原边界断言顺序。主协调者独立复核 source 40/40、interaction 24/24、web typecheck、4 文件定向 Biome 与 tracked/untracked 限定 diff-check，均通过；未改 CSS、正式视觉数值、协议、client/server、素材、Git、部署、生产或浏览器。
- 2026-08-26 第二批结构拆分精确范围：功能接线已收口且 farm 页面拆前 source 40/40、interaction 24/24、web typecheck 通过。并行小工 C 只修改 `apps/web/src/farm/farm-page.tsx`、新建 `apps/web/src/farm/dev/**`，并在确因源码归属变化时最小修改 `apps/web/src/farm/farm-page.test.tsx`；只迁出临时 `#farmTools=` 工具布局编辑器及其 hash／layout／拖拽私有 helper，正式工具栏继续复用同一权威布局数据。必须保留正式 DOM、className、坐标值、逻辑画布、编辑器 hash 合同和交互；不得修改 CSS、场景、面板、协议、client/server、素材、文案、视觉数值、其他测试、Git、部署、生产或浏览器。验收限于 source、interaction、web typecheck、定向 Biome 与限定 diff-check。
- 2026-08-25 购物车合同纠偏：辛玥已明确，农场／牧场购物车只负责“一车一次 Bell 通知”，TA 收到 `【📢来自铃野的通知】\n你的人类{human_name}想要你给她买{shop_name}的{items_text}。` 后自行调用现有 `farm.buy`／`farm.buy-companion` 等权威工具购买。多加的 settlement protocol／client／farm adapter／MCP 分支和社区数据库逐项结算状态机均已撤销；请求编号只在 Doorbell 后台用于同一购物车 POST 幂等与 wake 关联，不进入通知正文、Bell payload、浏览器成功合同或 Human 页面。当前只保留 Human session 创建请求、持久化一份购物车快照、一次 wake、24 小时过期和浏览器同键重试。不得改现有 farm 购买工具、玩法规则、视觉、其他 Prompt、Git、部署、生产或浏览器。
- 2026-08-25 最后两项批准／施工范围：辛玥已确认 90 道食谱采用本任务整理的八种料理方式映射；烤／蒸／油炸分别要求逐户已持有的烤炉／蒸笼／炸锅，炒与煎保持独立 `method_id` 但共用基础锅，当前没有可靠油炸配方时不强塞菜谱。农场／牧场购物车只提交“一车一次购买通知、一次 Bell wake”，料理台仍由 Human 直接购买。唯一新增模型可见通知逐字为 `【📢来自铃野的通知】\n你的人类{human_name}想要你给她买{shop_name}的{items_text}。`，其中商店名只映射为“农场商店／牧场商店”，多件商品以顿号连接，不显示内部请求编号。TA 收到通知后自行调用现有 `farm.buy`／`farm.buy-companion` 等权威工具；Doorbell 不新增 canonical 购买 op、逐项决定回传、结果查询或第二套结算 dispatcher。精确写入范围：main 只保留 Human session 创建请求、后台幂等快照、一次 Bell wake、浏览器同键重试与直接测试；料理方式按既有 authority 迁移。不得改现有 farm 购买工具、未批准 Prompt、其他 Bell producer、料理价格、素材／坐标、其他 Task Ticket、Git、部署、生产或浏览器。
- 2026-08-25 当前施工收口：可由现有 farm authority 证明的浏览器链已经接齐并复核无遗漏：field／catalog／ranch／kitchen／bulletin／流光原野／铃野共行读取；帮收；牧场居民／收取／装饰／派遣／抓捕／金币往来；料理库存动作、食材／每日食谱／烤炉／蒸笼／炸锅整车购买、商店刷新及 2～5 项真实库存烹饪；原创植物、全部设置、探险、作物收藏、熔炼、邻里留言和全服集市跨户原子购买／接受换物。90 道食谱方式／工具迁移和农场／牧场一次 Bell 购物通知均已接通；通知只让 TA 使用现有购买工具，不追踪结果。购物通知数据库／Bell 9/9、session route 59/59、browser client 2/2、农场源码 196/196、真实交互 24/24、MCP runtime 8/8、protocol／server／web typecheck及 web 生产构建 189 modules 均通过；仅有既存主包大于 500 kB 提醒。未 commit、push、部署、访问生产或操作浏览器。任务继续保持施工且整体视觉未验收，不删除本小节。
- 2026-08-25 续工／第二批权威料理接线：辛玥已明确要求继续施工直至全部接完，本票恢复为施工。当前先收口已经存在的 cook authority，不重写料理规则：farm 只在 `/Users/doraemon/Downloads/doorbell-commons-farm-qixi/dist/server/doorbell-internal.js` 与对应 resource route 定向测试接固定 `POST /internal/doorbell/human/kitchen/cook`；main 只修改 `packages/protocol/src/{farm-kitchen-cook.ts,index.ts}`、`apps/server/src/{farm-kitchen-cook-client.ts,farm-kitchen-cook-client.test.ts,registration-auth.ts,registration-auth.test.ts,index.ts,app.ts}`、`apps/web/src/auth/{kitchen-cook-client.ts,kitchen-cook-client.test.ts}`、`apps/web/src/farm/{farm-page.tsx,farm-page.interaction.test.tsx,farm-page.test.tsx}` 及必要的既有拆分 façade 类型。两份 client 改为从 `@doorbell/protocol` 包入口读取合同，消除当前 TS6059；浏览器只提交 2～5 个 raw item refs、当前 kitchen inventory revision 与 header UUID，成功以权威 kitchen resource 和 receipt 更新页面并显示真实结果，失败不扣料、不伪造 `method_id` 或工具判定。不得改锅具坐标、方式视觉、食谱规则、价格、其他页面、其他 Task Ticket、Git、部署、生产或浏览器。
- 2026-08-25 第三批跨户集市接线范围：复用已经完成并通过直接测试的 `replaceFarmsAtomic` 与 `handleHumanCrossFarmMarketAction`，不重写价格、手续费、库存或换物规则。farm 小工只修改 `dist/server/{farm-catalog-structured.js,doorbell-internal.js}`、必要的既有集市／resource route 直接测试，把全服普通货架与真实 Human 换物单做无副作用结构化投影，并让既有 `POST /internal/doorbell/human/market/action` 按 `buy／barter-accept` 调用双户原子 action、其余动作继续走单户 authority；main server 小工只修改 `packages/protocol/src/{farm-catalog.ts,farm-market-action.ts}`、对应直接 fixtures、`apps/server/src/{farm-market-action-client.ts,farm-market-action-client.test.ts}`，严格承接跨户 receipt、买卖双方门牌／revision 与换物列表。主窗口后续独占 browser client、session route、React 买入／接受按钮与共享 root 接线。不得新增 listing ID 给普通货架、不得把单户连续保存冒充原子成功、不得修改 Bell／Prompt、其他玩法、Git、部署、生产或浏览器。
- 2026-08-25 第四批料理工具权威范围：已确认产品合同固定烤炉 `800`、蒸笼 `1,200`、炸锅 `1,600` 银币，且料理台由 Human 通过现有整车购买直接结算；当前 farm 仍缺逐户持有字段。farm 小工只修改 `dist/server/{kitchen-structured.js,kitchen-purchase-action.js}` 与它们最直接的既有测试，把三项稳定工具 ID、固定价格和逐户已拥有集合纳入 kitchen 投影与现有 clone／revision／UUID 整车购买，未拥有可买一次、重复购买拒绝、扣款不足零变更、成功一次保存；既有农场缺字段按“付费工具均未拥有”读取，不回填或改写，首次真实购买时才持久化。主窗口后续再扩 protocol/client/React 购物车。不得给 90 道食谱猜 `method_id`、不得把场景分类当配方方式、不得改已确认价格、素材、其他玩法、Git、部署、生产或浏览器。
- 2026-08-25 跨户集市浏览器收口范围：farm 全服货架／换物投影、双户 internal dispatch 与 main 严格协议／server client 已分别完成；现在一个小工只在既有 `RegistrationAuth` 与 `/api/farm/market/actions` 链补 `buy／barter-accept` 的真实 Fastify 回归，只有发现当前映射缺字段时才定向修改 `apps/server/src/{registration-auth.ts,registration-auth.test.ts,app.ts}`，不改其他 route。另一个小工只修改 `apps/web/src/auth/{market-action-client.ts,market-action-client.test.ts}`、`apps/web/src/farm/panels/farm-action-panels.tsx` 与必要的现有 interaction/source 断言：对他户普通货架显示购买、对他户真实换物单显示接受，提交当前 market revision 与 header UUID，网络未知用同键重试，成功／state conflict 重新读取 catalog，不本地结算；固定范围滚动与现有视觉结构不重做。不得碰 Bell、其他面板、价格规则、Git、部署、生产或浏览器。
- 2026-08-25 播报真实来源收口范围：当前留言与牧场通知已经是真实只读投影，任务和成熟提醒仍被标成 unavailable。farm 小工只修改 `dist/server/bulletin-structured.js` 与直接测试：成熟提醒优先复用已经完成的 Human field 纯投影／同一权威地块状态，任务只读取存档中已经存在且可无副作用证明的进行中任务槽；禁止调用旧时间推进、领取、刷新、随机、保存或前端复算。若某一来源在真实存档中不存在，保持该来源 unavailable 并报告精确字段缺口，不伪造。main／React 只在 farm 严格输出确有新增字段时再同步既有 bulletin schema／fixtures，不增加分类、分页、足迹或 Bell 唤醒。
- 2026-08-25 第一批完成：`shop-panel.tsx` 已从 1695 行收为 252 行稳定 façade，新 `panels/shop/{model,shared,cooking-shop,ranch-shop}` 承接类型／目录与购物车模型、公共 UI、料理商店和牧场商店；`tool-panel.tsx` 已从 1850 行收为 205 行稳定 façade，新 `panels/tools/{types,common,backpack-panel,remote-panels,smelting-panel,settings-panel,original-plant-creator,cooking-recipe-catalog}` 承接对应职责。主协调者发现并仅修正 8 条旧源码归属断言的读取 fixture，未删改正则、计数或行为期望。独立验收：source 39/39、interaction 17/17、web typecheck、15 文件定向 Biome、限定 diff-check 均通过；未改 CSS、视觉、协议、服务端、素材、Git、部署或生产。
- 2026-08-25 第一批测试归属收口：主协调者独立验收确认定向 Biome 与 web typecheck 通过，但 `farm-page.test.tsx` 有 8 条源码归属断言仍只读取拆分前的 `farm-page.tsx`／两个旧面板 façade，因实现已迁入叶子模块而失败。精确追加写入 `apps/web/src/farm/farm-page.test.tsx`，只允许把这些既有断言的 source fixture 指向对应 `panels/shop/**`、`panels/tools/**` 或聚合后的完整面板源码；不得改断言语义、期望值、运行组件、CSS、业务代码或交互测试。修正后重跑该 source test、现有 interaction test、web typecheck、定向 Biome 与限定 diff-check。
- 2026-08-25 “拆屎山”接管／第一批精确写入范围：旧窗口已经安全停手，本票由当前任务继续施工。并行叶子一仅修改 `apps/web/src/farm/panels/shop-panel.tsx` 并新建 `apps/web/src/farm/panels/shop/**`；并行叶子二仅修改 `apps/web/src/farm/panels/tool-panel.tsx` 并新建 `apps/web/src/farm/panels/tools/**`。两者都必须基于当前实时脏文件机械拆职责，保留原 export、DOM、className、文案、会话状态、请求时序和行为；不得修改 CSS、`farm-page.tsx`、共享测试、协议、client/server、素材、模型文案或 farm worktree。主协调者独占 Task Ticket 与最终验证；未获 commit／push／部署／浏览器授权。
- 2026-08-25 交接／停工：辛玥已将后续施工转交新的“拆屎山”任务，本票立即停止继续写入，不再开启新批次或小工。停工前最后完成的是两处点名视觉修正（农场木牌仅保留身份信息，季节／土地阶段拆为独立视窗状态条；流光原野“今日动物踪迹”正式态恢复一行四项）以及叮咚播报的 farm 只读投影、internal／session 读取链和 React 顺序滚动列表；对应播报投影 2/2、farm resource route 1/1、server／browser client 4/4、播报 UI 定向 1/1、候选预览 14/14、农场视觉定向 1/1、protocol 与 web typecheck、限定 Biome／diff-check 已通过，未操作浏览器，视觉仍未由辛玥本人确认。明确未完成：权威料理 cook 仅有 farm action 和 main 独立 protocol／client 文件，尚未接 internal／session route 与 React，且当前 server typecheck 被 `farm-kitchen-cook.ts` 的 protocol 源文件落入 server `rootDir`（TS6059）阻断；跨户集市仅完成 farm 双户原子写入与 action，尚未接 main protocol／client／route／React；农场／牧场 Bell 购物车仍因模型可见逐字文案及 producer 合同未批准而保持阻塞。没有 commit、push、部署、生产访问或浏览器操作。
- 2026-08-25 最新点名视觉修正结果：农场木牌已只保留身份信息，真实季节与土地阶段迁到独立视窗环境条；farm 引擎与 field 合同均无可显示的权威天气字段，因此没有混入社区家园天气或假天气。流光原野正式态已与编辑态统一为一行四项，图片和名称共用同一整组变换。候选预览定向 14/14、农场对应定向 1/1、五文件限定 Biome 与 scoped diff-check 通过；web typecheck 当前只被并行新增的 bulletin client 尚未接 protocol root export 阻断，本批两处文件没有类型错误。未操作浏览器或宣称视觉通过，等待辛玥本人查看。
- 2026-08-25 最新点名视觉修正范围：流光原野“今日动物踪迹”正式非编辑态恢复与已确认编辑态相同的一行四项逻辑网格，图片与名称继续作为同一整体等比例缩放，不改动物顺序、素材、坐标或编辑器；农场木牌只保留权威农场名、门牌、存在时才显示的称号与欢迎语，真实季节和土地阶段拆到木牌外的独立视窗环境状态条。当前 `GET /api/farm/field` 没有农场天气字段，社区家园天气属于另一套权威状态，本批不混用、不把季节冒充天气；以后农场合同返回真实天气时再进入独立环境条。精确写入限于 `apps/web/src/{preview/candidate-two-preview.tsx,preview/candidate-two-preview.test.ts,farm/farm-page.tsx,farm/farm-page.css,farm/farm-page.test.tsx}` 与本任务对应产品决定；不改动物内容、场景坐标、数据合同、后端、其他页面、Git、部署、生产或浏览器。
- 2026-08-25 权威烹饪现状与接线范围：farm 旧 Human 的 2～5 个 raw `items` 烹饪链已由新 `kitchen-cook-action` 在 clone 上复用 `kitchenCook／checkTitles`，成功一次保存并返回真实 kitchen resource，失败零变更、同 UUID 重放；直接测试 4/4、料理组合回归 21/21 通过。90 道食谱及其 32→38→64→90 历史均没有 `method/tool` 字段，投影也只能标记 `not_persisted`，所以本轮不把前端方式选择伪装成后端判定。继续只新增独立 cook protocol、server/browser client 与直接测试，主窗口再接 internal/session route；React 正式启用仍等待 method/tool 权威迁移，不传一个会被后端忽略的假 `method_id`。
- 2026-08-25 跨户集市阻塞已定位并继续施工：farm 当前 `replaceFarm()` 会每户立即 `save()`，旧 `buyFromMarket／humanBarterAccept` 顺序修改双方且无事务，不能直接包装。按“全部接完”授权把 farm 写入范围精确扩到 `dist/store.js`、新 `dist/server/market-cross-farm-action.js` 与一份直接测试：新增一次完整 world 暂存写入的 `replaceFarmsAtomic`，磁盘 rename 成功后才提交双方内存映射，失败恢复买家、卖家和相关 UGC；随后只在该原语上接普通购买／接受换物、revision 与 UUID 幂等。不得改玩法价格、库存规则、其他 store API、main、React、文档外语义、Git、部署或生产。
- 2026-08-25 剩余权威接线并行范围：按辛玥要求立即拆给三个 `gpt-5.6-luna`／`max` 小工，且不让小工重新翻整票或扩散施工。料理小工只核对 farm 现有 90 道食谱、旧 Human 烹饪链与锅具状态证据；只有存在可证明映射时才补权威 cook action，不猜 `method_id` 或迁移值。播报小工只从现有 farm 任务、成熟地块和留言／牧场通知做无副作用结构化投影，并新增独立 protocol／server/browser client 文件，不碰共享 root、route、React 或文档。跨户集市小工只核对并复用现有双户原子保存能力完成普通购买与接受换物；若没有可证明的双户原子提交原语则零写入并返回精确阻塞，不用连续单户保存冒充成功。主窗口后续只接三批共享 protocol root、internal/session route、React 与事实文档。农场／牧场 Bell 购物车因模型可见逐字文案仍未审批，本轮不偷偷写 Prompt 或假唤醒。
- 2026-08-25 牧场动物动画回归已修复：随机平面游走继续由外层 roamer 的 Web Animation 驱动，动物贴图内层重新绑定既有 `farm-ranch-step` 纵向步态，两种 transform 已分层且不再互相覆盖；没有调整动物坐标、活动范围、速度、素材、详情或玩法。该行为定向检查 1/1、web typecheck 与限定 Biome／diff-check 已通过；未操作 Git、部署、生产或浏览器，仍等待辛玥本人视觉查看。
- 2026-08-25 Human 熔炼纠偏与接线已完成：旧 Human `/craft` 原本就把三项素材 ID 交给 `engine.craft()`；新结构化 action 只增加当前 `farm-smelting-v1` revision、UUID 幂等与严格 receipt，不重写玩法。Live catalog 返回真实素材库存与 revision，React 恰好选中三项后提交稳定 ID；farm 在 clone 上复用同一 `craft()` 完成重复数量校验、扣料、隐藏配方／权威随机、种子发放和任务事件，成功一次保存，同键重放不重复扣料。方案与索引中“Human 熔炼权限未确认／只读提示”的旧状态已纠正。farm action／internal route 3/3、server client 1/1、browser client 1/1、React 熔炼交互 1/1、农场源码 39/39、面板读取 2/2、全交互 17/17、protocol／server／web typecheck 与 172 modules build 已通过；未操作 Git、部署、生产或浏览器。
- 2026-08-25 当前收口状态：方案中可由现有权威能力直接证明的 React 写入口已经接齐。探险、牧场居民动作／收取／装饰／派遣／待结算收取／金币转入转回、单户集市浏览／上架／下架／换物挂单撤单、邻里留言、原创植物、全部设置字段、料理商店整车购买／食材刷新、作物图鉴收藏、Human 熔炼，以及料理库存的食用／喂宠物／系统回收／摆摊／鱼获出售／财宝回收均走独立 revision、UUID 幂等与 farm 权威结果，不在前端复制规则。作物图鉴和料理库存从通用工具包拆成按打开加载的行动面板／料理库存 chunk；当前生产构建生成 172 modules、43 张内容哈希图片，首页不预载这两组 chunk，`approved-source` 不进入构建输出。农场源码边界 39/39、面板读取 2/2、真实交互 17/17、protocol／server／web typecheck 与 Vite build 已通过；只保留既存主包大于 500 kB 提醒。仍被真实合同阻塞的是权威料理 `method_id`／工具持有迁移和三件工具购买、农场／牧场购物车 Bell 请求、跨户市场原子购买／接受换物，以及播报任务／成熟和更完整邻里／牧场历史数据；不得伪造。当前无 commit、push、部署、生产或浏览器操作授权，整体视觉仍等待辛玥本人一次性验收。

- 2026-08-25 素材 manifest／hash 收口范围：以现有 `apps/web/src/farm/farm-asset-manifest.ts` 的稳定语义 key／内容 ID 为唯一运行索引，新增构建时覆盖检查，比较 manifest 声明、实际运行引用和受控 farm 内容目录；每个当前可见 ID 必须是正式资源、明确 `fallback` 或明确 `missing`，禁止按中文名、数组下标或 sprite 序号猜图。把 manifest 当前实际引用的 farm 运行图片迁入 Vite 可处理的资源域并产出内容 hash URL；不删除原 public 文件、不追改未知外链，`approved-source` 先移出构建公开目录且保留可恢复源文件位置。只修改 farm asset manifest／其直接测试与构建脚本／必要 package 接线和本任务事实文档，不改场景布局、玩法数据、后端、其他社区素材、Git、部署或生产。
- 2026-08-25 邻里留言接线范围：复用旧 Human 留言路由当前已经存在的目标农场、访问／留言开关、消息写入和通知语义，先抽成唯一 farm authority，再新增独立结构化 message action 与直接测试；旧 route 同步调用同一 authority，不在 adapter 复制或绕过社交总闸。请求只接受目标门牌、正文、当前 neighborhood revision 与 UUID 幂等键；不能接受发送者身份、显示名、时间、点赞或通知内容。涉及发送方与目标方时必须两户 clone 后原子保存，失败双方零变更；成功返回真实留言 receipt 与最新 neighborhood resource。main 后续接独立 protocol、service/browser client、session route、自己／他人开放留言板发送 UI 与交互测试；不加入足迹、假留言、自动回复、Prompt 或模型唤醒。
- 2026-08-25 正式入口与统一 PWA 收口范围：继续施工已确认的 `/lingye/farm` 正常社区路由，社区入口只导航到该页面地址，旧 `/api/farm/ui*` 保持迁移代理且不再承担 React 导航；直接访问正式路由时仍先完成现有 Human session 检查，认证后只加载 FarmPage，返回时恢复社区地址。同步在社区前端唯一入口新增一套 Web App Manifest 与单一 Service Worker：导航请求 NetworkFirst，带内容 hash 的同源构建资源／受控静态资源 CacheFirst，所有 `/api/**` 严格 NetworkOnly，不注册 Background Sync，不缓存任何权威响应。本批只修改 `apps/web/{index.html,vite.config.ts,package.json}`、新增社区级 PWA 构建／注册文件，定向修改 `apps/web/src/{app.tsx,main.tsx,preview/candidate-two-preview.tsx}` 及直接测试、必要的根测试接线与本任务事实文档；若静态素材内容 hash 需要迁移，只处理 `farm-asset-manifest.ts` 已登记运行资源并保留稳定语义 key，不删除未知直链资源。不得停用旧 UI、改 nginx／部署／生产、提交或推送。
- 2026-08-25 食材刷新接线范围：farm 权威动作已经按本节全量收口范围完成；为形成严格端到端链，farm 精确追加 `dist/server/doorbell-internal.js` 与现有 resource route 直接测试，只接固定 `POST /internal/doorbell/human/kitchen/shop/refresh`，不改其他 internal action。Doorbell main 精确新增 `packages/protocol/src/farm-kitchen-shop-refresh.ts`、`apps/server/src/farm-kitchen-shop-refresh-client.ts` 与直接测试、`apps/web/src/auth/kitchen-shop-refresh-client.ts` 与直接测试，并定向接入现有 protocol root、server composition／session route、`apps/web/src/farm/{farm-page.tsx,farm-page.interaction.test.tsx,farm-page.test.tsx}`、料理商店面板与本任务事实文档；浏览器只提交当前 `shop_revision` 和 header UUID 幂等键，不提交身份、次数、价格、余额或新货架。
- 2026-08-25 食材刷新投影兼容：同一批允许定向扩展 `packages/protocol/src/farm-kitchen.ts` 的 `daily_shop` 严格 schema 与直接 fixtures，承接 farm 已返回的 `refresh_window_id／refresh_used_count／refresh_remaining_count／refresh_limit／next_cost_coins／can_refresh／refresh_reset_at`；available 必须全量有效，unavailable 只保留后端可证明的 nullable 状态，前端不自行计算下一价格或跨日重置。
- 2026-08-25 牧场装饰接线范围：farm 已持有装饰的 `place/unplace` 权威 action 已按本节范围完成。为接成端到端链，farm 精确追加 `dist/server/doorbell-internal.js` 与现有 resource route 直接测试；Doorbell main 允许小工只新增独立 `packages/protocol/src/farm-ranch-decoration-action.ts`、`apps/server/src/farm-ranch-decoration-action-client.ts` 与直接测试、`apps/web/src/auth/ranch-decoration-action-client.ts` 与直接测试，共享 protocol root、server composition／session route、React 牧场背包／场景和事实文档仍由主窗口接线。请求只携带稳定 `decoration_id`、`place/unplace`、当前 ranch revision 与 header UUID 幂等键；不接受坐标、价格、余额，不开放 Human 商店购买。
- 2026-08-25 全量收口授权：施工／辛玥要求不再逐模块视觉审批，先把当前方案中仍缺的纯前端、结构化读取和已有权威动作全部完成，再统一自审与一次性视觉验收。施工继续按独立小批保持真实边界：farm 发布线只新增或修改各能力自己的权威函数、结构化 adapter／strict internal route／直接测试，Doorbell main 只修改对应 protocol、service/browser client、session route、React 面板／场景、直接交互测试与本任务事实文档；每批失败零变更、成功一次保存，前端不复制规则。当前并行核对料理商店金币刷新、权威料理所缺 `method_id`／工具持有迁移，以及牧场旧 Human 已有但新 UI 尚未接回的派遣／抓捕／装饰／金币回传入口；共享 route、React 与文档只由主窗口接线。食材手动刷新已经确认无法复用旧 `refreshKitchenShop`：旧函数跨日会同时重抽食材和食谱、清空购买计数且没有手动次数／费用；本批精确扩展 farm 写入到 `content/cooking.json`、`dist/{engine,content}.js`、`dist/server/{kitchen-structured,kitchen-shop-refresh-action}.js` 与直接测试，新增由 farm 持有的北京时间日计数、100～1000 农场金币费用和只刷新轮换食材的权威动作，保留当日食谱、库存、购买计数与失败原子性，不在 adapter 复制随机规则。料理历史追证已经确认 32→38→64→90 道版本始终没有 `method_id`，每户也没有锅具持有字段或旧存档迁移策略；因此现阶段不得把分类或图片猜成方式，只先完成不依赖该猜测的真实能力。牧场下一批只接已经确认且现有 Human 权威函数可证明的已持有装饰摆放／收回：farm 精确写入新增 `dist/server/ranch-decoration-action.js` 与直接测试，复用 `ranchPlaceDecoration／ranchUnplaceDecoration`，不开放产品已经排除的 Human 配饰／装饰直购，不碰 AI 购物铃或跨农场派遣结算；main 对应独立 protocol、service/browser client、session route、React 牧场背包／场景与直接测试仍由主窗口接线。后续按方案依次收口原创植物、其余设置、探险、集市、邻里、播报与资源治理；Human 熔炼已在后续批次按旧 `/craft` 权威接回。尚未确认的农场／牧场 Bell 模型可见逐字文案及任何新增玩法数值不得由本任务猜定；其余可从当前权威引擎与已确认产品决定直接证明的能力先全部完成。共享脏工作区中其他 Task Ticket 的文件与语义区域继续保留，不读取范围外 diff、不 reset／revert／整理。当前无新的 commit、push、部署、生产访问或浏览器操作授权。
- 2026-08-25 原创植物接线范围：复用 farm 现有 `designCrop` 作为唯一玩法权威，不从 React 或 adapter 复制设计费、起步种子、稀有度、生长时间、ID 生成或全服上限。farm 精确新增 `dist/server/original-plant-action.js` 与直接测试，并在 `dist/server/doorbell-internal.js`／resource route 定向测试接固定 internal POST；adapter 只负责严格五字段、门牌、专用 revision、UUID 幂等、clone farm、全局 UGC 快照回滚、一次保存和结构化 receipt。为让 Human 取得合法前置版本，定向修改 `dist/server/farm-catalog-structured.js` 及其直接测试，把同一 `farm-original-plant-v1` revision 作为 catalog 顶层只读字段返回，不复算玩法。Doorbell main 精确新增 `packages/protocol/src/farm-original-plant-action.ts`、定向扩展 `packages/protocol/src/farm-catalog.ts` 与 fixtures、server/browser clients 与直接测试，再由主窗口接 protocol root、server composition／session route、创造表单与交互测试。browser 只提交 catalog 返回的当前 revision 与五个设计字段，幂等键只走 header；成功后以权威 receipt 更新结果并重新读取 field/catalog/neighborhood，不伪造称号或额外奖励。
- 2026-08-25 当前收口结果：料理食材手动刷新、牧场已持有装饰摆放／收回、原创植物创建三条 authority action 已完成 farm internal、Doorbell session/browser route 与 React 交互闭环。原创植物的专用 revision 由 catalog 顶层返回，浏览器网络结果未知只复用原五字段、revision 与 UUID 重试；成功只展示权威作物、费用、起步种子与余额并重读 field/catalog。牧场装饰成功整体替换 ranch resource。protocol build、server/web typecheck、server/browser clients 28/28、React 交互 14/14、源码边界 39/39、session route 44/44 通过；尚未 commit／push／部署／访问生产，整体任务继续施工。
- 2026-08-25 下一批设置范围：把旧 Human route 已有的 `aiName／humanName` 与 `social.visit／steal／water／message` 校验和写入抽成可复用 farm 权威函数，再让现有 `dist/server/farm-settings-action.js` 调用同一函数；旧 route 同步改为复用，不在 adapter 复制直接存档写入。farm 只定向新增设置 authority 模块、修改旧设置 route 对应语义区域、扩展 settings action 与直接测试；main 只扩展既有 settings protocol/client/route/React 逐字段保存与直接测试。保持 clone、revision、UUID 幂等、一次 replaceFarm；不改文案、玩法、其他旧 route 或生产。
- 2026-08-25 下一批探险范围：复用 `dist/expedition.js` 的 `expEnter／expExplore／expRoll／expChoose／expSetCharm／expRetreat` 作为唯一权威，新增独立 `dist/server/expedition-action.js` 与直接测试；动作必须在 clone 上执行、以完整旅程／库存／RNG 相关 revision 和 UUID 幂等保护、成功一次保存并返回完整最新 catalog resource，浏览器不提交随机结果、奖励或余额。随后由主窗口接 strict internal route、protocol、server/browser client、session route、现有探险面板与交互测试；不得复制随机、地图、事件或奖励规则。
- 2026-08-25 探险／集市前置版本接线范围：为让浏览器只能提交权威当前版本，分别把 `farm-expedition-v1` 与 `farm-market-v1` revision 从各 action 内抽成只读纯投影并追加到 catalog 顶层严格合同；不改变 catalog data 内容、不执行推进／刷新／结算／保存。同步接 fixed internal action routes、Doorbell clients/session routes 与现有面板；集市只开放单户原子可证明的浏览、上架、下架、换物挂单与撤单，跨户购买／接受换物继续返回明确不可用，不伪造原子结算。
- 2026-08-25 下一批牧场往来范围：复用 `dispatchRanchRaid／catchRanchRaid／ranchRemit／farmSendRanch`，新增独立 ranch interaction action 与直接测试，分别承接派遣、抓捕、牧场金币回传及农场金币转入；dispatch/catch 涉及双方农场时必须隔离两户 clone、以双方相关状态 revision 和 UUID 幂等保护、全部成功后原子提交或全部零变更，不能只保存一边。请求只接受现有权威函数真正需要的稳定动物／目标门牌／金额，不接受补偿、概率、费用、余额或结果。main 后续接独立合同和 UI：派遣从派遣面板发起，抓捕进入具体来访动物详情，金币往来进入牧场明细入口，不恢复已撤掉的全局管理条。
- 2026-08-25 第六批真实写操作：施工／料理台食材与每日食谱的 Human 整车购买已在本地完成，不再沿用单项 adapter。浏览器一次只提交当前 `shop_revision`、整车条目和 UUID 幂等键；farm 在 clone 上逐项复用 `kitchenBuy` 的真实货架、限额、已解锁与银币校验，任一项失败整车零变更，全部成功才一次 `replaceFarm`，返回逐项 receipt、总银币、余额和完整新 kitchen resource。同 key 同 payload 重放，同 key 异 payload 冲突；畸形 revision 在结算前按无效请求拒绝，坏余额在任何购买／保存前按 farm unavailable 拒绝。React 成功后整体替换权威 kitchen、清空购物车并显示真实件数／支付额；网络结果不明时主确认键、数量变化和返回目录后的加购全部锁定，只能复用原购物车、版本与 key 重试；货架变化／状态冲突会清车并重取权威商店，不保留无法移除的旧条目。farm action／internal route 定向 11/11、server route＋client 45/45、browser client 5/5、React 交互 9/9、farm 源码边界 39/39、protocol typecheck／build、server typecheck、14 文件限定 Biome、Vite 生产构建（145 modules）与两条发布线 scoped diff-check 通过；构建只保留既存主包大于 500 kB 提醒。普通 web typecheck 仍只被范围外飞行棋测试既有 TS2345 阻塞，没有本批 farm 文件错误。三件付费工具仍缺逐户持有存档迁移，权威烹饪仍缺 90 道食谱 `method_id` 与方式唯一性升级，本批没有猜方式、直写工具状态、开放假工具购买或假料理成功。当前批次未提交、未推送、未部署、未访问生产，整体任务仍不完成。
- 2026-08-25 当前修复批次：施工／五个验收缺口已收口。帮收成功会强制重取此前已经加载的 farm catalog、kitchen 与 ranch，右上角刷新也会重取 field 及全部已加载附属资源；catalog 每次权威刷新都会重新初始化设置草稿，旧请求被 abort 后即使晚返回也不能覆盖新状态，网络结果不明的帮收重试继续复用同一幂等键。field 内部结构化或非 JSON `502` 均映射为合同不可核验，其他 `5xx` 保持服务不可用。web 已建立正式 test script 并进入根 `npm test`，保留 143 条源码边界测试，同时新增 4 条真实 React 交互测试覆盖收获后库存重取、手动刷新、同键重试和晚到响应；`farm-tools` 编辑器只在 Vite DEV 构建开放，生产 JS 不包含编辑器分支。Glimmer／Together 成功合同新增严格 `subject.farm_doorplate`，farm 返回权威门牌，Doorbell client 强制与当前绑定门牌一致。根 `npm test`、server 定向 52/52、farm 两项 projector 2/2、协议构建、server typecheck、限定 Biome、Vite 生产构建（144 modules）与两边 scoped diff-check 已通过；构建只保留既存主包大于 500 kB 提醒。普通 web typecheck 仍只被范围外飞行棋测试既有 TS2345 阻塞，输出没有本批 farm 文件错误。farm `440381a` 与 Doorbell main `004c013` 已分别推送，未部署或访问生产，整体任务仍不完成。
- 2026-08-25 当前写操作收口：继续复用旧 farm 权威函数，没有重写牧场规则。牧场居民详情已经接通投喂、升级、改名、置顶／取消置顶、穿戴／取下配饰和切换已解锁异色；成功响应返回真实扣款、等级、剩余次数、名称、置顶、配饰或外观结果，并整体替换最新 ranch resource，未知／损坏／重复目标在调用权威动作前拒绝。牧场当前有可收产出时显示场景顶部紧凑“一键收取”，`POST /api/farm/ranch/collect` 只接受当前 revision 与 UUID 幂等键，farm 复用原有收取、还债、料理柜与自动回收链，React 只展示真实逐项 receipt，不恢复底部全局管理条。设置面板已经接通农场名、欢迎语和佩戴／卸下称号的逐字段保存，成功整体替换 catalog resource；小机昵称、你的昵称与四个社交开关因旧引擎没有对应权威写动作继续只读，不直写存档。对应 farm `440381a` 与 Doorbell main `004c013` 已分别推送，未部署或生产验证，整体任务仍不完成。
- 2026-08-24 第五批真实写操作模式／范围：并行准备牧场当前可收产出的权威一键收取合同。只允许新增 farm 独立 collection adapter／直接测试，以及 main 独立 protocol、server client、browser client／直接测试；共享 index、internal route、Doorbell session route、React 场景入口和事实文档只由主窗口接线。请求不接受动物列表、欠款金额、产物估价、去向或客户端余额，只带当前 ranch revision 与 UUID 幂等键；farm 在 clone 上复用现有收取／料理库存／自动回收／还债权威链，失败零变更、成功一次原子保存并返回逐项真实 receipt 与完整新 ranch resource。同 key 同 payload 重放，未知结果由上层复用同 key；不恢复此前被撤掉的底部全局管理条，前端入口位置待主窗口按现有场景结构接入。不得改玩法、Prompt、旧 Human HTML／CSS、其他任务文件、Git、部署、生产或辛玥现有浏览器。
- 2026-08-24 第四批真实写操作模式／范围：在牧场居民动作闭环收束后继续接已经确认的农场设置逐字段保存。只允许新增 farm 独立 settings action adapter／直接测试，以及 main 独立 protocol、server client、browser client／直接测试；共享 protocol index、farm internal route、Doorbell session route、React 设置表单和本任务事实文档仍由主窗口接线。每次请求只提交一个允许字段、当前 catalog revision 与 UUID 幂等键，Human key／门牌只由 Doorbell session 派生；farm 必须复用现有权威设置动作，在 clone 上执行并一次原子保存，成功返回完整新 catalog 资源，未知结果重试同一幂等键。称号佩戴若现有引擎是独立权威动作，可纳入同一 dispatcher 的独立 action，不得让前端整对象覆盖或直接写存档。不得改 Prompt、玩法规则、旧 Human HTML／CSS、其他任务文件、Git、部署、生产或辛玥现有浏览器。
- 2026-08-24 第三批真实写操作模式／范围：继续施工已经由辛玥明确要求接回的现有权威能力，先并行准备三条互不重叠的薄合同：料理台 Human 直接购买、料理台权威烹饪、牧场具体动物详情动作。三名 `gpt-5.6-luna`／`max` 小工只允许新增各自独立的 farm adapter／定向测试，以及 main 侧独立 protocol、server client、browser client 与直接测试；共享 `packages/protocol/src/index.ts`、farm `dist/server/doorbell-internal.js`、Doorbell session routes、React UI 和事实文档仍只由主窗口接线。动作必须复用现有 farm 权威规则、原子保存、真实扣款／扣料／产物／失败语义与稳定实例身份，不能在前端或 adapter 复制玩法；先写能在旧实现上失败的定向测试。农场／牧场购物车的“喊 TA 来买”仍等待 Bell 唤醒、幂等与部分失败合同，Human 设置保存、熔炼、留言和市场写入也不在本批擅自开放。不得修改旧 Human HTML／CSS、`dist/{engine.js,game.js,store.js,server.js,web.js}`、Prompt、其他任务文件、Git、部署、生产或辛玥现有浏览器。
- 当前批次模式／状态：施工／牧场、料理台和其余农场目录三条独立真实只读闭环已在本地接通。farm 新 projector 分别安全投影权威牧场余额／居民／产出／持有物／派遣／商店，料理余额／工具／混合实例库存／已知食谱／每日货架，以及农场商店／背包／图鉴／探险／熔炼／设置／播报／邻里／集市；不调用旧 view 的推进、初始化、刷新、结算、随机消费或保存。Doorbell 严格 session 路由为 `GET /api/farm/{ranch,kitchen,catalog}`，浏览器不能提供身份。React Live 按进入页面或打开面板读取，不回退 Demo，已消费真实余额、居民、库存、菜单、播报、邻里和同一市场；旧日料理货架、未知牧场拥有／库存状态及未知 ID 均保持不可用。三批仍只读，购买、料理、收取、投喂、升级、派遣、熔炼、设置保存、留言、市场及其他写结算均未开放。
- 当前批次证据读取／精确写入范围：读取本任务事实源、共享 main 现有 protocol／farm clients／session routes／React farm shell 与独立 farm worktree 中直接负责 Ranch、Kitchen、目录／库存／市场／设置／公告的最小源码和内容文件；不读取玩家存档、凭据、生产请求或范围外 diff。三名 `gpt-5.6-luna`／`max` 小工只新增各自不重叠的 farm `dist/server/{ranch-structured,kitchen-structured,farm-catalog-structured}.js`、对应定向测试、main `packages/protocol/src/{farm-ranch,farm-kitchen,farm-catalog}.ts`、`apps/server/src/{farm-ranch-client,farm-kitchen-client,farm-catalog-client}.ts` 及直接测试、`apps/web/src/auth/{ranch-client,kitchen-client,farm-catalog-client}.ts` 及直接测试；共享 `packages/protocol/src/index.ts`、farm `dist/server/doorbell-internal.js`、Doorbell `apps/server/src/{registration-auth.ts,registration-auth.test.ts,app.ts,index.ts}`、React `apps/web/src/farm/**`、`apps/web/src/app.tsx` 与事实文档只由主窗口最终接线。不得修改旧 HTML／CSS、`dist/{engine.js,game.js,store.js,server.js,web.js}`、玩法／概率／结算／存档、Prompt、斗地主、飞行棋、UNO、其他任务文件、Git、部署、生产或辛玥现有浏览器。
- 当前批次下一动作／验证：本批 projector＋dispatcher 10/10、server client／session route 44/44、browser client／farm UI 52/52、protocol／server typecheck、29 文件限定 Biome、直接 Vite build（137 modules）与 scoped diff-check 已通过；`doorbell-internal.js` 语法通过并确认不存在重复 `body` 声明。普通 web typecheck 只被范围外飞行棋测试既有 TS2345 阻塞，输出没有 farm 错误。下一阶段继续按独立 action 合同接真实写操作，并完成正式 `/lingye/farm` 路由、面板／资源进一步拆分、内容 hash、manifest 覆盖、社区统一 PWA 与公开源文件清理；整体任务和视觉验收仍不标完成。
- 模式／状态：施工／farm `/internal/doorbell/human/field/read`、Doorbell `GET /api/farm/field`、React Live 读取，以及旧 Human UI“一键帮 TA 收”的新结构化动作链均已在本地完成。浏览器 `POST /api/farm/field/harvest-assists` 只提交空业务 body、同一次尝试的 UUID 幂等键与当前 field revision；farm adapter 在 clone 上复用原有成熟推进、季节收获、`humanHarvestAll`、社交通知、称号检查和一次原子保存，成功返回真实 receipt 与完整新 field，React 只整体替换权威资源并展示作物、品质、金币／银币、掉落、季节事件和新称号，不本地结算。普通／奇幻仍只在成功收获 receipt 中揭晓。前端第一批四场景拆分和第二批 bulletin／tool／shop 面板拆分已通过技术检查；按工具继续细分、正式 `/lingye/farm` 路由、内容 hash、manifest 覆盖检查、社区统一 Service Worker／PWA 与 `approved-source` 退出公开目录仍未完成。改动已进入 Git，当前未部署或生产验证；旧 `/api/farm/ui*` HTML 代理不得作为数据源。
- field 真实数据硬边界：独立 farm 当前对普通／奇幻地块只持久化 `seedType／growTicks／progress／ripe／waterCount`，具体作物由收获时的 `rollCrop()` 才揭晓；只有限定／原创地块保存可解析的 `limitedId`。因此结构化 field 始终返回真实 `seed_type`，并用 `identity_state + nullable crop_identity` 表达当前是否已有权威 ID／名称／类别；adapter 不提前 roll，前端不按种子类型猜作物。现有顶层 opaque field revision 已扩为收获安全摘要：基于投影成熟后的完整隐藏 farm 状态、当前日、季节和规则版本生成，并排除持久化幂等 receipt；同一幂等键先于 revision 校验重放原 receipt，陈旧 revision、无成熟地块、次数耗尽、幂等冲突或保存失败均零变更。
- 作物揭晓时点确认：辛玥明确保留当前“收获时才揭晓”玩法，不把普通／奇幻作物改为成熟时抽取。普通／奇幻地块即使已经成熟，field 接口仍只返回真实 `seed_type`、成熟状态、进度／时间与 `identity_state: hidden`，`crop_identity` 保持 `null`；限定／原创因播种时已有稳定 `limitedId`，继续返回真实作物身份。React 不显示猜测名称或专属作物图，收获 receipt 才承接普通／奇幻的真实揭晓结果。
- Doorbell 第一批精确写入范围：新增 `packages/protocol/src/index.ts` 的 field read browser／internal request、严格 success/error schema 与导出类型；新增 `apps/server/src/farm-human-client.ts` 及定向测试，使用既有 farm API base URL、service token 与 timeout 调用固定 `/internal/doorbell/human/field/read`；定向修改 `apps/server/src/{registration-auth.ts,app.ts,index.ts}` 及直接测试，让服务端只从当前 Human session 派生 `farm_human_key + expected_farm_doorplate` 并暴露 `GET /api/farm/field`。第一刀只读，不修改旧 `FarmDirectoryClient` 的 public/HTML 责任，不接 React，不增加任务摘要或写操作；普通／奇幻地块的 nullable 身份、严格 upstream schema、502 合同错误、503 可用性错误与退群清会话按现有边界测试。不得修改当前其他任务占用的 `apps/server/src/{config.ts,config.test.ts,doorbell-farm-op-registry.ts,mcp-runtime.test.ts}`、farm runtime、Git、部署或生产。
- 模式／状态：施工／农场右侧“创造”入口与原创植物设计表单已经落地；邻里“原创作物”继续只负责全服公共浏览与发现。“创造”使用名称、可选拉丁名、描述、播种文案、收获文案五个已确认字段，`demo=full` 保存当前 React 会话草稿，Live 只读；底部使用正常玩家文案“完成设计”，在结构化创建、设计费与起步种子结算合同接入前保持禁用，不扣费、不发种子、不伪造创建成功。首次复用图鉴图标的方向已按辛玥反馈撤销，现已新增独立 `panel.tool.create` 稳定 key 与 `apps/web/public/farm/ui-icons/create-plant.png`：第二版为画架、植物图样和短画笔的 192×192 RGBA 透明像素图，约 29 kB；首轮铅笔／画笔／嫩芽堆叠稿不再接线。精确写入为 `apps/web/public/farm/ui-icons/create-plant.png`、`apps/web/src/farm/{farm-page.tsx,farm-page.css,farm-page.test.tsx,farm-asset-manifest.ts}`、`docs/{CURRENT_WORK.md,product-plan.md,farm-human-ui-game-migration-proposal.md}` 的本任务对应段落；不改后端、旧农场、邻里现有图标、其他页面、Git、部署或生产。农场定向测试 31/31、web typecheck、四文件限定 Biome、web build（115 modules）通过；构建只保留既存主包大于 500 kB 提醒。隔离预览已确认入口、五字段、按钮禁用态与新图标接线存在，没有操作辛玥现有浏览器标签；图标与面板视觉仍由辛玥本人确认。
- 模式／状态：施工／设置面板已按辛玥确认的正式前端条目完成替换：不增加分类页签，单一纵向表单中的“农场名和称呼”包含农场名／小机昵称／你的昵称，另有欢迎语、佩戴称号；“社交开关”把谢绝来访／访问作为同一个二态来访开关，另列偷菜、帮浇水、留言三枚开关。内容在既有固定面板范围内纵向滑动。当前仍无结构化设置读取／保存合同，因此 `demo=full` 只编辑三页共享的当前 React 会话草稿，Live 保持只读；不发请求、不持久化、不生成假称号或假保存结果。精确写入为 `apps/web/src/farm/{farm-page.tsx,farm-page.css,farm-page.test.tsx}`、`docs/{CURRENT_WORK.md,product-plan.md,farm-human-ui-game-migration-proposal.md}` 的本任务对应段落；不改后端、协议、旧农场、其他面板、Git、部署或生产。农场定向测试 31/31、web typecheck、四文件限定 Biome、web build（115 modules）通过；构建只保留既存主包大于 500 kB 提醒。邻里当前只完成外壳和三项空态的事实另行继续收口；本轮整体仍等待辛玥本人视觉验收。
- 模式／状态：施工／辛玥已明确确认三件付费料理工具的正式银币价：烤炉 `800`、蒸笼 `1,200`、炸锅 `1,600`。三个数字已经写入料理商店“工具”目录及对应产品／迁移事实源：未拥有项显示银币图标与权威价格，demo 已拥有的烤炉仍只显示“已拥有”；Human 购买合同尚未接入，因此工具不能入购物车，不模拟购买、扣银币或解锁。精确写入限于 `apps/web/src/farm/{farm-page.tsx,farm-page.test.tsx}`、`docs/{CURRENT_WORK.md,product-plan.md,farm-human-ui-game-migration-proposal.md}` 的本任务对应段落；没有改 CSS、工具贴图、料理坐标、后端、旧农场、其他页面、Git、部署或生产。农场定向测试 30/30、web typecheck、两文件限定 Biome、web build（115 modules）与限定 diff-check 已通过；构建只保留既存主包大于 500 kB 提醒。技术检查完成，等待辛玥本人视觉验收。
- 模式／状态：施工／辛玥本轮只点名农场木牌文字可读性。首轮浅色加单向阴影仍会融入木纹，当前已经按实页反馈加强为更亮的象牙奶油色 `#fff8dc`，并从同一控件缩放单位派生四向细深棕描边与轻柔下投影，使农场名、`门牌` 与门牌编号都从亮暗木纹中分离；没有增加底框，也没有改木牌贴图、尺寸、位置、文案、overview 数据、其他页面或其他控件。精确写入限于 `apps/web/src/farm/{farm-page.css,farm-page.test.tsx}` 与本任务小节。农场定向测试 30/30、web typecheck、两文件限定 Biome、web build（115 modules）与限定 diff-check 已通过；构建只保留既存主包大于 500 kB 提醒。隔离 348×701 实页确认农场名与门牌计算色均为 `rgb(255, 248, 220)`，四向描边均生效且文字保持完整宽度；没有操作辛玥现有浏览器标签。技术检查完成，等待辛玥本人视觉验收。
- 模式／状态：施工／辛玥追加纠正木牌、付费料理工具与低价方向。农场资料已从 CSS 假木纹改为真正透明背景的像素木牌贴图 `apps/web/public/farm/ui/field-plaque.png`，overview 的 `farm_name／farm_doorplate` 继续用实时 DOM 文字覆盖，不烤字、不补假资料；生成原图 1774×887、958789 bytes，项目运行图压为 768×384、255733 bytes 的 RGBA PNG，只压缩本轮新增素材。木牌已移到独立视窗 UI 层并只从既有统一控件比例派生尺寸，不随场景 cover 裁切或形变。上一轮料理台“已解锁／未解锁覆盖层”已经撤销：已经拥有的工具按原贴图正常出现且没有状态标签，未拥有工具不进料理台场景和左右切换；料理商店新增“工具”栏，`demo=full` 只把烤炉作为已拥有，蒸笼／炸锅显示待购买目录与 `—` 价格，不加入购物车、不模拟购买或解锁，Live 在结构化目录接入前不显示示例付费工具。辛玥已确认三件锅具整体要更便宜，三个精确银币数仍待提供；Human 购买合同也尚未确认。农场／牧场／料理台／邻里继续保留各页会话内面板、分类和滚动位置。精确写入限于 `apps/web/public/farm/ui/field-plaque.png`、`apps/web/src/farm/{farm-asset-manifest.ts,farm-page.tsx,farm-page.css,farm-page.test.tsx}`、`docs/{CURRENT_WORK.md,product-plan.md,farm-human-ui-game-migration-proposal.md}` 的本任务对应段落；不改其他素材、料理工具贴图与确认坐标、食材／食谱目录、overview 合同、后端、旧农场、玩法结算、其他页面、Git、部署、生产或辛玥现有浏览器。定向测试 30/30、web typecheck、三文件限定 Biome、web build（115 modules）与限定 diff-check 已通过，构建只保留既存主包大于 500 kB 提醒；隔离实页已核对 348×701、1280×720、390×844，宽屏时发现并修正百分比内边距挤空文字的直接问题，三种视口的木牌均完整在视窗内且文字有实际宽度。料理工具商店保持三列、无加号，实测烤炉为已拥有，蒸笼／炸锅为 `—`；料理切换连续八次不包含蒸／油炸，也没有场景状态标签。技术检查完成，等待辛玥本人视觉验收。
- 模式／状态：施工／牧场动物商店本轮已按辛玥实页反馈收口：动物／宠物分类、三列、无分页与固定中部纵向滚动保持不变；原先会压缩叠行的自适应行改为完整固定商品格节奏，每只动物等比例收进自己的正方形格，名称留在格内底部，价格或已拥有留在格外下方。独立购物车加号已删除，未拥有商品点击整格直接加入购物车，已拥有商品不重复加入并仍可查看已有详情。没有修改动物素材与已确认调位参数、料理食材商店、其他弹窗、场景、数据、后端或协议。农场定向测试 26/26、web typecheck、三文件限定 Biome、web build（115 modules）和限定 diff-check 均通过；构建只保留既存主包大于 500 kB 提醒。隔离实页已在 1280×720 与 348×701 核对三列不叠压，窄屏列表为 `303px` 固定视区内纵向滚动（内容 `622px`），点击鸭子后购物车由 0 变 1，目录内加号数量为 0；隔离标签和临时视口已清理，未刷新或点击辛玥现有标签。技术检查完成，等待辛玥本人视觉验收。
- 模式／状态：施工／本轮按辛玥点名继续收口料理台、农场背包和牧场场景 UI。料理台保持八种既有工具贴图与已确认坐标，尤其没有替换 `drink-mixer.png` 或修改 asset manifest；底部八枚方式键已删除，方式改由锅具两侧的视窗 UI 正圆键切换，标签固定在五格食材框上方，锅具下方固定 `放入食材／烹饪`。`放入食材` 打开固定 `60cqw` 高的底部弹窗，复用料理商店五类与四列素材格，demo 只保存五格备料草稿；食谱只有在草稿逐项满足配方数量时才显示禁用的 `一键制作` 占位，Live 不显示假食材、假可制作状态或假结算。农场背包只保留 `种子与药水／素材／其他` 三类，不改权威库存与结算。牧场此前错误的底部“管理”条已完整移除：不显示居民／可收占位，不暴露不可视欠款，不提供全局一键收取、抓捕或金币回传；抓捕等具体动物动作待真实动物场景接入后从点击动物打开的详情弹窗进入。顶部返回／刷新继续属于视窗 UI 层；本轮已撤销此前各自使用 `vmin`／`clamp()` 计算尺寸、描边、阴影和按压位移的错误做法，每枚按钮现在只从同一个独立视窗控件尺寸等比例派生整套固定正圆几何，并用同值锁定最小／最大宽高，不读取场景背景的 cover／裁切尺寸。农场帮收条、余额槽、地块、牧场背景与素材、邻里、商店、右侧工具栏和底部导航未改；不更新整体完成索引、不删除任务票。本轮农场定向测试 24/24、web typecheck、三文件限定 Biome、web build（114 modules）均通过，构建只保留既存主包大于 500 kB 提醒，5203 返回 200；预览继续保留，视觉仍等待辛玥本人确认。未修改旧农场引擎、`old-vps/farm/**`、后端合同、其他页面、Git、部署、生产或辛玥现有浏览器。
- 邻里顶部三项页签返工已完成技术检查：左右内边距、三格中心与选中内嵌层已对齐 `neighborhood-shell.png`；页签行高度已改为由外壳宽度推导的固定设计比例，文字恢复显示。按辛玥追加决定，选中页签内面复用页面既有柔和草绿渐变，选中文字为浅奶油色；位置、尺寸和未选中态不动。底部四项导航、邻里正文、背景、右侧工具、素材、数据、后端、协议和其他页面未改。农场定向测试 20/20、两文件限定 Biome 与 5203 页面 200 通过；上一轮同一 CSS 高度修正后的 web typecheck、web build（113 modules）也已通过，构建仅保留既存主包大于 500 kB 提醒。技术检查完成，等待辛玥本人实页视觉验收。
- 素材 manifest 当前状态：前端现已通过一个类型化清单解析场景页签、右侧工具、叮咚播报、邻里外壳、三类作物阶段图、农场商店演示素材、八种料理方式与 18 种牧场动物／宠物素材；页面数据只保留稳定素材 key 或动物 ID，不再内嵌公开路径或 `spriteIndex`。本轮未改视觉、布局、素材文件、后端或协议；定向回归 20/20、web typecheck、三文件限定 Biome、web build（113 modules）与未跟踪三文件的逐文件 diff-check 均通过，5203 页面和 manifest 模块返回 200；构建只保留既存主包大于 500 kB 提醒。固定 URL 仍是迁移期交付方式，内容 hash／预缓存／PWA 没有被冒充为已完成。
- 素材 manifest 续施工：邻里一体化外壳确认后，辛玥明确要求继续按最新版迁移方案施工；本轮先完成不依赖新后端合同的前端素材身份收束。新增 `apps/web/src/farm/farm-asset-manifest.ts`，把当前正式入口实际消费的场景图标、页面工具、料理方式、作物通用阶段图、商店演示素材和牧场动物图集按稳定 `asset_key／entity_id／visual_state／status／usage` 登记，并让 `farm-page.tsx` 通过清单取图；牧场动物不再在商品数据里携带 `spriteIndex`，改由稳定动物 ID 命中 manifest frame。当前公共目录固定 URL 仍是迁移期交付方式，本轮不冒充已经完成内容 hash／PWA；不移动或重画素材，不改邻里外壳、场景构图、按钮位置、数据、后端、协议、旧农场或其他页面。精确写入扩展为 `apps/web/src/farm/{farm-asset-manifest.ts,farm-page.tsx,farm-page.test.tsx}` 与本任务小节；只跑这三文件的定向测试、typecheck、限定 Biome、web build 和限定 diff-check。
- 牧场商店图鉴调位已按辛玥确认的 URL hash 收束：18 种动物／宠物的最终 `x/y/size` 数值已逐项原样固化到 `RANCH_ANIMAL_LAYOUTS`，一次性 `editor=ranch-animals` 的 query 分支、hash 解析、拖动／滑杆 UI、专用 CSS 与专用测试均已删除；旧编辑地址现在只进入普通页面，不再进入编辑器。名称固定在正方形图鉴格底部，价格／已拥有状态独立留在方块外；动物／宠物分类、九宫格分页、详情、无分割线、数据、素材、其他页面、后端与协议未动。详情返回按钮的可见文案已从“‹ 返回”收成单个“‹”，并保留不可见的“返回牧场商店”无障碍名称；返回行为、按钮尺寸／位置、其他弹窗和视觉未动。农场定向测试 19/19、限定 Biome、web typecheck 与限定 diff-check 均通过；技术检查完成，整体视觉任务仍等待辛玥本人验收，不标完成。
- 邻里页面重做：辛玥已明确确认背景 `exec-cc40254f-466e-42df-a04e-d23f0205dda7.png` 可用，项目内继续固定使用 `apps/web/public/farm/scenes/neighborhood-background.png`，不重新生成、不改构图。首轮上层 UI 被辛玥否定：它错误做成了悬浮弹窗，深色外壳、绿色选中块、中央空图标和示例的整页层级无关，也与前面农场页面的奶油纸面、浅蜂蜜木边视觉不一致，因此该首轮视觉不成立。当前邻里上层已经替换为整页层级：背景场景保留在页面上段，`排行榜／留言板／原创作物` 三项宽页签紧接场景下沿，选中内容区从页签下方连续铺到底部导航上方；点选哪个只显示哪个栏目。每个栏目自己的内容区使用分页而不是内部长滚动，分页控制固定在内容区底部；当前没有结构化合同，因此只显示诚实空页 `1/1` 且上一页／下一页禁用，但禁用只影响行为和箭头提示，按钮底面继续保持与其他页面一致的绿色，不再由全局透明度洗成灰色；不生成假页数、假排行、假留言或假原创作物。不得保留悬浮外壳、卡片套卡片、黑重描边或自创绿色选中态。其他农场页面、底部四项导航、料理工具位置、背景素材、后端与协议不动。农场定向测试 19/19、限定 Biome、web typecheck 与限定 diff-check 通过；技术检查完成，视觉由辛玥本人验收。
- 牧场商店／派遣纠正：辛玥撤销单独“动物图鉴”入口；目录归入牧场商店并分成“动物／宠物”两栏，`demo=full` 按 `3×3` 每页 9 个正方形缩略格展示当前 `old-vps/farm/content/animals.json` 的 16 种权威动物和 `pets.json` 的 2 种权威宠物。缩略格在图标下显示名称和价格，演示中明确标记已拥有的条目只显示“已拥有”、不再显示价格；点击后在同一商店弹窗切换为详情，小尺寸“返回”回到原分类／分页。详情只渲染内容文件真实存在的字段，缺失介绍整行不出现；山羊因原图集下缘混入羊驼耳朵，当前只为山羊改用修正后的独立透明 `goat-codex.png`，其他动物继续使用原图集，未重画或拆分。牧场原“动物图鉴”工具位改为“派遣”；现有农场引擎已有动物派遣能力，但 Doorbell 侧尚无结构化读取／操作合同，因此当前面板只显示诚实未接入状态，不伪造任务、目标、奖励、记录或成功。正式 Live 的牧场商店仍保持真实未接入状态。精确写入范围扩展为现有农场前端三文件、项目内稳定动物／派遣／熔炼素材、本任务小节和 `docs/product-plan.md` 对应已确认入口归属；不修改后端、协议、旧农场内容／素材或其他页面。
- 功能弹窗卡片与分页纠正：辛玥指出“一件内容一张卡片”是所有功能弹窗的统一内容规则，不是农场商店特例；同时明确长列表使用方案既有分页，不得由前端擅自改成内部滚动。当前有真实视觉条目的农场商店改为每页四张纵向紧凑的独立卡片，底部使用与顶部页签完全同色的圆形绿色上一页／下一页按钮与页码，并将分页整体轻微下移；切换分类回到第一页，不出现滚动条或卡片下方的大块无意义空白。其他功能面板继续使用同一独立卡片与分页边界，当前没有结构化条目的页面只显示一张紧凑诚实空状态卡，不伪造条目或页数。弹窗纸面按辛玥指定参考图改为浅奶油色旧纸材质：只保留克制的中心亮、边缘略深渐变和细纸纹，不使用棕色／焦糖色厚重牛皮纸；具体条目不使用圆角硬描边或立体下坠阴影，只以浅纸片底色差和间距区分。新增项目材质 `apps/web/public/farm/ui/panel-parchment.png`（640×640，约 503 kB），5203 返回 200；定向测试 17/17、限定 Biome、web typecheck、web build 与限定 diff-check 通过，构建只保留既存主包大于 500 kB 提醒。弹窗外框、分类、商品素材、文案、价格、其他场景和数据不变；技术检查完成，等待辛玥本人刷新实页视觉验收。
- 模式／状态：施工／邻里页此前分别贴入顶部页签、正文框并在底栏前收口，造成上下两处明显断层；该拼接方案已撤销。首张机械直线式一体外壳候选也未接入。当前运行时只使用三枚圆润页签、正文和底部延伸共用连续软边的一体透明外壳 `apps/web/public/farm/ui/neighborhood-shell.png`；旧顶部页签图和正文框图已从运行时引用移除，真实页签按钮、栏目文字、诚实空状态和分页仍由 DOM 覆盖，外壳底部额外延伸 `3.8cqw` 并由既有底栏覆盖接缝。辛玥已经在 5203 实页明确回复“OK”，本项视觉确认通过并固定，不再调整。定向测试 19/19、限定 Biome、web typecheck、web build、限定 diff-check 与 5203 页面／素材 200 均通过；构建仅保留既存主包大于 500 kB 提醒。未改邻里背景、底栏视觉、动物、地块、料理工具、数据合同、后端或其他页面；整个农场升级任务仍有其他未完成项，因此本 Task Ticket 保留施工状态。
- 右侧功能弹窗本轮收束范围：辛玥要求不要继续逐项补一项汇报一项，先按 `docs/product-plan.md` 最新农场 Human UI 方案把农场、牧场、料理台当前侧栏中所有入口的弹窗结构一次补齐，再统一列出需要她本人审阅的视觉部分。当前只使用方案已经确认的页面归属和库存边界：牧场为商店／背包／派遣／集市／设置，动物目录与详情进入牧场商店；料理台为商店／背包／食谱／集市／设置；料理背包明确区分商店食材、牧场产物、鱼获与料理，牧场背包只承接配饰、装饰等保留持有物。没有对应结构化合同的面板只显示场景专属分类和诚实空状态，不生成商品、库存、动物、食谱、摊位、设置值或操作成功；不改已确认的弹窗外框、背景、地块、侧栏图文参数、底栏、后端、协议、旧农场或玩法。
- 农场其余功能弹窗第一批返工：第一轮只接入了农场页 `背包／作物图鉴／集市／探险／熔炼／设置` 六个面板的内部栏目，却继续复用旧 `.farm-tool-panel` 外框，辛玥实页指出“根本没重新画框”，该轮不视为弹窗视觉完成；她进一步指出旧 70×96cqw 面板过小、栏目拥挤。当前保留已经核对的真实分类与空状态边界，但用 CSS 重新绘制并放大整块右侧固定比例弹窗：面板改为 78×118cqw，深浅双层游戏边框、奶油纸面、顶部绿色页签和一体化关闭键都由同一面板比例生成；内容区同步扩容，不缩小土地、右侧工具图标或栏目文字来硬塞。功能弹窗与叮咚播报层级均为本页最高的 `z-index: 80`，明确覆盖右侧工具、底栏与顶部控制键；尺寸、位置和行为未随层级调整。不使用新贴图，此前生成的两张弹窗候选仍未复制进项目、未被引用。第一批当时不跨牧场／料理台；本轮已由辛玥追加授权按最新方案补齐这两个页面的既有工具弹窗，写入仍限定为 `apps/web/src/farm/{farm-page.tsx,farm-page.css,farm-page.test.tsx}` 与本任务小节，不改后端、协议、旧农场、地图、场景背景、菜单图标或其他页面。

- 顶部返回／刷新比例纠正曾错误把屏幕 UI 收入固定 862×1825 背景画布，随后又用 contain 缩放整张画布，导致非同宽高比视口出现两侧留白、按钮和弹窗随背景一起缩进。辛玥实页明确纠正：背景与屏幕 UI 不是同一裁切层，按钮不得绑定在背景上。当前已撤销 `.farm-game__canvas`、`ResizeObserver` 和固定画布缩放包装；返回／刷新、加载／错误态、右侧工具、弹窗和底栏重新直接挂在全视口 `.farm-game__shell`，按视口容器单位统一缩放并锚定屏幕边缘，按钮行为、图标造型和热区不变。
- 农场缩放当前唯一方向：场景背景继续以 862×1825 原始比例 cover 铺满视口并允许自然裁切；土地保留在场景构图层。返回／刷新、右侧工具、弹窗和底栏属于独立屏幕 UI 层，不随背景裁切、不因背景比例产生留白。禁止再次固定背景、用 contain 留空、把按键绑回背景画布或逐个添加 viewport 像素补丁。已确认土地内部布局、工具图文参数、素材、按钮行为和编辑器页面不变；窄屏、宽屏、高屏与浏览器缩放只核对技术比例，视觉仍由辛玥本人实页确认。
- 顶部按钮当前纠正：两枚按钮已经移出固定背景画布，此前尺寸、边距、描边、阴影与按压位移仍分别使用多组 `vmin`／`clamp()`，并没有形成一枚固定形状后整体等比例缩放的控件。本轮只保留一个由独立视窗容器宽高共同决定的控件尺寸；按钮横纵、最小／最大宽高、边距、描边、阴影和按压位移全部从该尺寸按固定比例派生，设置 `flex: 0 0`、`aspect-ratio: 1` 和 `box-sizing: border-box`，SVG 只按按钮自身 48% 缩放。它只锚定视窗边缘，不读取背景 cover／裁切尺寸；背景、土地、侧栏、弹窗、底栏与按钮行为未动。
- 料理底部食材选择器追加收口：辛玥实页指出当前五枚食材分类键过厚、四列格内贴图过大，并确认每格左上角显示库存数量。本轮只压低该选择器的分类键、缩小格内食材贴图并新增左上角纯文字数量；当前 overview 没有料理库存，因此 `demo=full` 只能显示 `×—`，Live 继续没有示例条目，待结构化库存字段接入后再显示权威数量。弹窗固定滑动范围、五类、四列、点击放入五格、素材身份和其他料理布局不动。
- 料理食材弹窗底部形态已经按辛玥实页反馈调整：弹窗底边保持 `bottom: 0`、横向贴住视窗两边、覆盖底部导航、下方直边并从 `translateY(100%)` 向上滑出；高度由不足以遮住锅具的 `60cqw` 提高到 `80cqw`，顶部进入锅具区域。全宽外壳不再沿用悬浮框的 `10cqw` 左右内缩，内容改用 `4cqw` 横向内边距，让五类与四列格铺开；格子变宽时贴图占比从 `74%` 同步收为 `64%`，保持贴图实际视觉大小并增加留白。内容仍只在弹窗自身固定范围内滚动，数量、选择行为、素材身份与底部导航本身未动。定向测试 24/24、web typecheck、两文件限定 Biome、web build（114 modules）与 5203 页面 200 均通过，构建只保留既存主包大于 500 kB 提醒；视觉仍等待辛玥本人确认。
- 料理假结果样式已按辛玥实页反馈完成本轮技术检查：上一版 `50cqw` 纸面过小、料理图占比过大，还遗漏了现有 farm 的稀有度与结算信息。只读核对 `old-vps/farm/dist/web.js` 当前结果结构，确认真实结果包含成品图、按 `N/R/SR/SSR/SP` 着色的稀有度标、料理名、锁定牧场金币与银币回收值、条件性的“新食谱已解锁”、关闭键和“收进料理柜”，并不显示 `×1`。当前 `demo=full` 固定使用 `SR` 的 `番茄炖牛肉` 作为视觉样例，纸面放大到 `66cqw`、成品图缩小到 `18cqw`，并补齐上述元素；没有权威金额时两个回收值只显示货币图标与 `—`，样例解锁标记与收柜按钮只演示排版，点击不写入。辛玥最新指出右上角金色圆形关闭钮过重，本轮只把它收为无圆底、无边框、无阴影的单独棕色 `×`，保留原关闭行为和固定点击热区；弹窗内容、尺寸、成品图、稀有度与其他页面不动。该样例不按所选方式／食材推导，不进入 Live，不表示真实成功，不扣材料、不解锁食谱、不保存或结算；正式结果提示仍只在 farm 返回权威结果后出现。农场定向测试 24/24、web typecheck、三文件限定 Biome 与 web build（114 modules）均通过，构建只保留既存主包大于 500 kB 提醒；写入只限现有农场 React／CSS／定向测试与本任务小节，不复制旧 HTML，不改后端、协议、旧农场引擎或真实存档。技术检查完成，等待辛玥本人视觉验收。
- 料理按键压扁的实页根因已经修正：社区全局 `button { min-height: 44px; }` 曾把本页左右料理切换键从声明的 `38×38` 撑成实际 `38×44`，把食材弹窗关闭键从 `32×32` 撑成实际 `32×44`；同时本页原 `.farm-game button { font: inherit; }` 的选择器优先级高于食材分类自身字号，导致分类文字实际回到 `16px`。本轮用低优先级的本页作用域重置撤销这两个全局覆盖，并为两种圆形键显式锁定各自同一个宽高／最小最大宽高变量；社区其他页面的 44px 通用触控规则未改。5203 实页热更新后量取确认顶部返回／刷新均为 `42.77×42.77`、料理左右键均为 `38×38`、关闭键为 `32×32`，分类文字为 `7.68px` 且矮行高度 `17.77px`；定向测试 24/24、web typecheck、两文件限定 Biome、web build（114 modules）与页面 200 均通过，构建只保留既存主包大于 500 kB 提醒，视觉仍等待辛玥本人确认。
- 右侧功能弹窗当前结果：农场页补齐背包／作物图鉴／集市／探险／熔炼／设置，牧场页补齐背包／派遣／集市／设置，料理台补齐背包／食谱／集市／设置；三页商店继续走同一弹窗入口，`demo=full` 的农场商店保留已核对的视觉预览，牧场商店已接入 16 种动物、2 种宠物的目录／分页／详情视觉，Live 与料理台商店仍为诚实未接入状态。料理背包分类固定为商店食材／牧场产物／鱼获／料理，牧场背包固定为配饰／装饰／其他。邻里首批为排行榜／留言板／原创作物；叮咚播报只列进行中的任务／成熟提醒／最近留言，不含足迹。功能弹窗与播报均为本页 UI 最高内容层 `z-index: 80`，覆盖右侧工具、底栏与顶部控制键；没有新增假库存、假购买、假派遣、假食谱、假动态或假操作成功。动物商店相关定向测试 19/19、限定 Biome 与 web typecheck 通过；视觉由辛玥本人验收。
- 料理台独立食谱弹窗分页已按辛玥最新确认撤销：底部上一页／下一页键与页码已经删除，不再按每页五条切片；五个原有食谱分类保留，当前分类的食谱只在弹窗原本固定的中部内容范围内纵向滑动，不撑开弹窗。真实接入后仍只渲染该户权威已解锁食谱；料理商店每天刷新的两道未知食谱、食材选择器、其他商店和其他弹窗未动。农场定向测试 24/24、web typecheck、三文件限定 Biome 与 web build（114 modules）均通过，构建只保留既存主包大于 500 kB 提醒；已同步更新 `docs/product-plan.md` 与迁移方案对应已确认规则，不改后端、协议、旧农场或真实存档。技术检查完成，等待辛玥本人视觉验收。
- 料理商店食材栏“刷新”键已按辛玥最新反馈缩小：原 `min-width: 11cqw`、`min-height: 34px` 与 `border-radius: 999px` 在窄屏组合成接近大圆的外观；当前改为固定 `12cqw × 6cqw` 的扁平草绿圆角矩形，圆角收为 `1.1cqw`，次数、下次金币价、禁用状态和未来刷新合同边界不变。农场定向测试 24/24、web typecheck、三文件限定 Biome 与 web build（114 modules）均通过，构建只保留既存主包大于 500 kB 提醒；写入限定为现有农场 React／CSS／定向测试与本任务小节，不动分类、食材格、购物车、其他按钮、后端、协议、旧农场或真实存档。技术检查完成，等待辛玥本人视觉验收。
- 农场作物图鉴已按辛玥最新确认补为纯文字目录：只读核对权威 `old-vps/farm/content/crops.json`，当前共有 171 种内置作物，普通 60、奇幻 70、限定 41，每项均有稳定 ID、真实名称、分类与 `N/R/SR/SSR/SP` 等级。`demo=full` 按普通／奇幻／限定三类显示名称和等级，每类内部固定按 `N → R → SR → SSR → SP` 从低到高排列，同等级保留权威内容目录原顺序；不使用作物图片、详情卡或分页，只在弹窗固定内容范围内双列纵向滑动。当前 Doorbell 无图鉴发现／收藏结构化合同，Live 继续显示诚实未接入状态，不把全量目录冒充该户已发现或已收藏。新增静态文字目录只承接权威内容证据，不保存 URL 或素材序号。农场定向测试 25/25、web typecheck、四文件限定 Biome、web build（115 modules）、`5203` 本地预览 HTTP 200 与限定 diff-check 均通过；构建只保留既存主包大于 500 kB 提醒。已同步更新 `docs/product-plan.md` 与迁移方案对应规则，不动原创作物、地块、商店、后端、协议、旧农场或真实存档。技术检查完成，等待辛玥本人视觉验收。
- 牧场首页动物二维巡游已按辛玥最新反馈返工：上一版仍然只是两条固定往返轨迹，纵向偏移很小，实页观感接近固定在某一行，并没有真正承接旧 farm 的 `data-roamer` 机制。当前每一段都在各自动物的安全草地区域内重新随机选择二维 `x/y` 目标，按场景实际尺寸和二维距离用 `transform` 移动，到达后短暂停留再选择下一处，并按横向行进方向转身；鸡和小猫的安全区域均覆盖较宽横向范围与明显纵向范围，避开边界、池塘、右侧视窗工具栏和底栏，不再循环同一条线。轻微步态与 `prefers-reduced-motion` 静止行为保留，运动不写入业务状态。`demo=full` 仍只显示鸡和小猫，Live 仍不生成假动物。本地 355×701 实页连续坐标检查确认 2.2 秒内鸡由 `(91.93, 200.59)` 移至 `(95.37, 215.32)`、小猫由 `(177.48, 308.13)` 移至 `(151.27, 365.29)`，横纵坐标均变化，console 无 warning/error；农场定向测试 26/26、web typecheck、三文件限定 Biome、web build（115 modules）、`5203` HTTP 200 与文档限定 diff-check 均通过，构建只保留既存主包大于 500 kB 提醒。已同步更新 `docs/product-plan.md` 与迁移方案对应视觉边界；不改动物大小、`RANCH_ANIMAL_LAYOUTS`、动物素材、商店、后端、协议、旧农场或真实存档。技术检查完成，等待辛玥本人视觉验收。

- 农场商店分类字号已调整：`种子与药水／今日商店` 的响应式字号由 `clamp(0.48rem, 2.05cqw, 0.62rem)` 提高到 `clamp(0.72rem, 3.1cqw, 0.88rem)`；按钮尺寸、分类名称、布局、商品列表和其他文字均未动。农场定向测试 16/16、web typecheck、限定 Biome、web build、限定 diff-check 与 5203 样式核对均通过；构建仅保留既存主包大于 500 kB 提醒。技术检查完成，等待辛玥本人刷新查看。

- 农场可见开发标签继续清理完成：农场场景右上角“真实概览”标签已删除，加载说明中的同词已改为普通“正在读取当前账号绑定的农场”，页面源码与 5203 开发模块均不再包含“真实概览”。地块、商品、弹窗、尺寸、交互和数据合同未动。农场定向测试 16/16、web typecheck、限定 Biome、web build、限定 diff-check 与 5203 文案核对均通过；构建仅保留既存主包大于 500 kB 提醒。技术检查完成，等待辛玥本人刷新查看。

- 农场商店弹窗开发说明已清理：可见的 `界面演示` 标签与底部 `每 4 小时轮换 · 只读预览 · 不会发起请求` 整行说明均已删除，空出的高度直接归还商品列表；无障碍名称同步改为普通“农场商店／价格”，专用标签与边界样式已删除。两项真实分类、商品名称、价格、限购／随机出现说明、素材、弹窗尺寸与交互均保留，其他场景空状态、后端、协议和玩法未动。农场定向测试 16/16、web typecheck、限定 Biome、web build、限定 diff-check 与 5203 指定文案不存在核对均通过；构建仅保留既存主包大于 500 kB 提醒。技术检查完成，等待辛玥本人刷新查看，任务仍未完成且不登记稳定视觉基线。

- 农场商店分类与药水素材纠正已完成：商店现只分 `种子与药水／今日商店` 两栏；普通种子、奇幻种子与单瓶加速药水归入前者，限定种子、六瓶药水套装与种子配方归入后者。已新增真正透明、无文字且保持原始比例的 `apps/web/public/farm/ui-icons/{speed-potion,potion-set}.png`，单瓶与六瓶套装不再复用通用 `shop.png`；项目图分别为 179×192／192×170 RGBA PNG、37,227／53,523 bytes，5203 均返回 200。价格、每日限购、四小时轮换、种子配方、其他商品、弹窗外形和玩法合同均未改。农场定向测试 16/16、web typecheck、限定 Biome、web build、限定 diff-check 与开发模块引用核对均通过；构建仅保留既存主包大于 500 kB 提醒。技术检查完成，等待辛玥本人刷新查看，任务仍未完成且不登记稳定视觉基线。

- 今日商店配方素材已纠正：现有引擎中的隐藏配方是用于熔出种子的种子配方，不是料理食谱；商店不再复用料理台 `recipes.png`。已新增与现有农场像素素材同画风、无文字的独立 `apps/web/public/farm/ui-icons/seed-recipe.png`，图面使用种子→嫩芽→种子的配方示意，并只替换今日商店“隐藏配方”演示项；料理台食谱图标、配方机制、价格、商店分组、弹窗布局、其他素材和后端均未动。项目素材为 192×192 RGBA 真透明 PNG、52,730 bytes，5203 返回 200 且开发模块已引用新路径。农场定向测试 16/16、web typecheck、限定 Biome、web build、限定 diff-check均通过；构建只保留既存主包大于 500 kB 提醒。技术检查完成，等待辛玥本人刷新查看新素材，任务仍未完成且不登记稳定视觉基线。

- 当前右侧农场商店弹窗已按现有引擎证据纠正：不再用“农场／牧场／料理台”作为商店内分类，农场场景内现为 `种子与药水／今日商店` 两栏；`demo=full` 只演示代码真实存在的普通种子 8、奇幻种子 40、加速药水 50／每日最多 6 瓶，以及每 4 小时轮换且可能出现的限定种子、6 瓶药水套装 250、隐藏配方 500。牧场／料理台点击商店不会混入农场示例，正常 Live 在新读取合同未接入前继续显示真实空状态；没有购买按钮、假请求、假通知或假结算。证据读取严格限于 `old-vps/farm/source-reference/src/{config,engine,types,web}.ts` 的直接商店片段和 `docs/product-plan.md` 对应条目，不读取玩家存档、凭据或生产，也未修改旧农场代码、后端、协议、其他弹窗或场景视觉。技术检查完成，等待辛玥本人刷新查看商店视觉，任务仍未完成且不登记稳定视觉基线。

- 当前田阵列返工：辛玥已确认当前六列田阵列的单块 `12.16cqw`、`0.4cqw` 间距、六行上限、`112%` 底图、`74.96cqw` 阵列宽高与 `45%/46%` 垂直／水平锚点固定，不再调整大小和比例；每户按权威农场真实解锁进度显示不同地块数量，不补齐未解锁占位。该 UI 规则已定向写入 `docs/product-plan.md`；当前共享合同只返回 `farm.plots[]`、没有独立 `unlock_count`，前端继续逐项渲染服务端数组。`demo=full` 本地预览已从 36 格改为 20 格供辛玥查看，正式 Live 数据和合同未动。农场定向测试 14/14、web typecheck、限定 Biome、限定 diff-check 通过，5203 实际送出的模块已核对包含 20 格。技术检查完成，等待辛玥本人刷新后查看，视觉任务仍未完成。

- 限定植物两阶段当前实现：辛玥已分别确认成熟体 `exec-c6d1e745-9302-41ea-b3a8-0fbb591c9f9b.png` 与同品种生长期 `exec-58f6898a-b85c-406c-ad3d-096d2b68aea1.png` 可以；两张已按项目统一规格裁切为 512×512 透明 PNG，并替换 `apps/web/public/farm/crops/{limited-growing,limited-ripe}.png`，压缩后约 151 kB／73 kB。普通／奇幻素材、真实概览映射和其他页面未动。定向测试 13/13、限定 Biome、web build、限定 diff-check 与两张开发资源 200 均通过；构建仅保留既存主包体积提醒。

- 限定植物成熟体确认：辛玥已明确确认抽卡候选 `exec-c6d1e745-9302-41ea-b3a8-0fbb591c9f9b.png` 可以，定为限定植物成熟体，不再修改其造型。下一步只以该确认图为目标补同品种生长期，不添加新的外观描述；生长期仍须由她本人查看后，两阶段素材才一起替换项目内限定植物文件。

- 限定植物提示词再次纠正：辛玥明确禁止前端继续描述或指定限定植物外观，黑金郁金香方向立即作废。下一候选改为“抽卡”：只提供同一项目的普通植物作为画风、体量和像素密度参考，只要求生成一株更稀有的植物及其同品种生长阶段，不指定颜色、花型、叶型或装饰；候选仍不自动替换项目素材。

- 限定植物第三轮纠正：最新候选被辛玥指出仍与奇幻植物使用相同的卷叶、紫色花苞和发光轮廓，因此作废，限定不能只是“奇幻升级版”。下一候选只以普通植物校准像素密度和体量，改用直立长叶、杯状花的黑金郁金香轮廓，深酒红近黑花瓣与克制金边体现稀有；明确禁止卷叶、月牙花苞、紫色主色和发光。候选仍只生成在 Codex 目录，未经辛玥确认不替换项目素材。

- 地块编号与限定植物当前状态：田块左上角常驻数字已删除，只在无障碍标签与点击详情保留真实 `plot_id`。第二套限定植物被辛玥否定为过于突兀，不作为视觉结果；她进一步纠正不应由前端预设香槟山茶、深绿金边等造型。最新候选严格使用简化提示词：“参考同一画风、体量和细节密度，画一株你认为更稀有的植物；生长期与成熟期属于同一品种，透明背景，只画植物。”普通／奇幻素材和真实实页映射均未动；最新两张候选仍在 Codex 生成目录，等待辛玥本人查看确认后才替换项目内限定素材。

- 地块选中框纠正：当前外扩 `outline` 会被田阵列内部滚动边界裁切，辛玥实页已指出。改为完全落在单块田内部、沿地块边缘显示的完整选中描边；不扩大热区、不改变地块状态、点击或滚动边界。

- 田地尺寸当前微调：辛玥查看五列田阵列后确认田块可以略大、现有间距过空；本轮只将五列阵列宽度从 61% 轻调到 63%、列行间距从 1.15cqw 收紧到 0.55cqw，并同步保证六行完整可见，不移动整组锚点、不改植物比例、背景、真实数据或点击行为。

- 最新施工状态：农场田阵列已改为每排 5 块、最多显示 6 行，超过 30 块只在阵列内部滚动；原 CSS 拼接的茎叶果实与空地横杠已删除。普通、奇幻、限定三套植物均已生成独立的生长中／成熟透明 PNG 并保存到 `apps/web/public/farm/crops/**`，六张素材轮廓不同；当前真实概览没有植物类别字段，因此实页只使用普通套，奇幻／限定只保存素材、不按种子名猜测。一次性 `editor=cooking-tools` 已落地，八种料理方式分别保存工具横向位置、纵向位置和大小到 URL hash，刷新可恢复，正常料理页继续读取当前默认参数，等待辛玥调整后再固化。定向测试 13/13、web typecheck、限定 Biome、web build、限定 diff-check 与六张开发资源 200 均通过；构建仅保留既存主包大于 500 kB 提醒。技术检查完成，等待辛玥本人查看田地与调整料理工具，视觉任务仍未完成。

- 模式／状态：施工；设置面板的四项社交状态已经从灰色文字胶囊改为标准滑轨／滑块开关：开启为草绿色滑轨且滑块靠右，关闭为暖灰棕滑轨且滑块靠左，接口未提供值时以居中滑块和 `—` 诚实表达未知；可见状态文字位于滑轨外侧，禁用、键盘焦点和减少动态效果均有独立样式，控件使用固定宽高比例，不绑定场景背景纵向裁切。`demo=full` 继续只保存 React 会话草稿，Live 继续只读，不新增保存、持久化或成功反馈。农场定向测试 31/31、web typecheck、三文件限定 Biome、web build（115 modules）、限定 diff-check 与 5203 页面 200 均通过；构建只保留既存主包大于 500 kB 提醒。技术检查完成，等待辛玥本人视觉验收。一次性工具图标排版编辑器已经在 `apps/web/src/farm/**` 落地，仅由 `editor=farm-tools` 查询参数启用：固定 192×192 逻辑画布，方形工具底框只作预览背景，十一种工具可切换，图标层与文字层可分别拖动，图标大小与文字字号分别调整，单项可重置，每个工具独立参数自动写入 URL hash，刷新后可恢复；正常工具栏仍未套用临时参数。右侧展开菜单内各工具后方的方形底框已降低存在感；底部“农场／牧场／料理台”三项场景切换栏未动。定向测试 9/9、web typecheck、限定 Biome、web build 与限定 diff-check 已通过；隔离 390×844／430×932 实页确认十一项可切换、滑杆和拖动会更新 URL hash、刷新可恢复且无 body 横向溢出，console 无 warning/error。技术检查完成，等待辛玥本人调整并进行视觉验收。已经生成但未接入的商店、背包、作物图鉴、集市、探险、排行、留言和设置稿全部只留在 Codex 生成目录，不复制进项目、不引用；此前误做的华丽空徽章母版及十一个组合稿已从项目目录移入系统废纸篓，可恢复。保留现有工具清单、按钮行为与场景归属，不改土地、料理台、Menu 开合或业务逻辑，任务未完成、未登记稳定视觉基线。外层手机壳铺满当前视窗，场景背景与场景内地块／锅具共享 862×1825 等比例 cover 画布并允许不同手机比例产生少量边缘裁切；底栏、返回键和 Menu 按实际手机宽高自适应，场景内容保持比例与锚点、不被拉伸。农场、牧场、料理台三张正式像素场景和七件料理工具已经接入 Doorbell 内部农场页，真实 `/api/farm/overview` 仍是当前地块数据唯一来源，未接合同的牧场／料理操作没有伪造成功。顶部木牌已删除；七件料理工具底边共用炉架锚点。农场、牧场和料理台都保留各自商店入口，人类端商店只读展示商品，点击“喊 TA 来买”后应通过「铃」通知自己的小机，由小机按农场权威机制实际购买、扣款和结算；当前前端任务没有新增铃生产者或后端合同，也没有伪造已通知或已购买成功。牧场产物结算时直接回收换钱，不进入背包或牧场工具。
- 当前视觉调整：辛玥在编辑器实页确认菜单文字因深色字芯增粗而过粗、影响辨认，同时明确原浅色描边无需改变；本轮只降低编辑器预览与正常菜单工具文字的字重并移除深色增粗阴影，完整恢复原浅色外描边。她进一步确认 192×192 放大画布不能单独代表实页大小；编辑器需在保留放大拖动区的同时增加按当前手机宽度和正常 13.7cqw 菜单格等比例缩放的实页预览，两边读取同一组 URL hash 参数，不改或清空她已调整的位置、尺寸和工具参数。放大画布的原始数值会误导大小判断，因此图标／文字滑杆改为显示并编辑其占完整菜单格的百分比，内部仍换算回同一组 192 逻辑坐标，现有 hash 效果不得跳变。
- 当前定稿动作：辛玥已在一次性编辑器完成十一项工具的图标／文字位置与大小调整，并明确“搞完了”；本轮按她当前 URL hash 中的数值原样固化到正常侧栏，编辑器暂时保留到她查看正常页面后再决定删除。现有 `recipes` 错误复用了 `codex.png`，本轮只把食谱入口换成独立且语义正确的食谱图标，不改其他工具、布局、场景或行为。
- 最新定稿参数：辛玥查看独立食谱图标后再次微调“设置”的文字位置与“食谱”的图标／文字，并明确“ok了”；当前以该次 URL hash 为唯一最新值覆盖正常侧栏，其他九项已固化参数不变。完成覆盖与最小技术检查后交给她查看不带编辑器参数的正常页面。
- 新增熔炼入口：辛玥确认在农场场景侧栏增加与“探险”同级的“熔炼”工具，并使用独立像素图标，同时加入一次性工具编辑器供她调整。当前没有熔炼操作合同，点击只能沿用真实未接入提示，不得伪造熔炼、产物、库存或结算成功；不扩散到牧场、料理台或后端玩法。
- 熔炼最终参数：辛玥已在一次性编辑器调整 `smelting` 的图标／文字位置和大小并明确“ok了”；本轮按她当前 URL hash 的 `iconX 95.8203125`、`iconY 91.18359375`、`iconSize 173.76`、`textX 93.3203125`、`textY 141.98828125`、`textSize 35.52` 原样覆盖正常侧栏，不改其他十二项参数或任何业务行为。
- 牧场背包归属：辛玥确认牧场场景也使用现有“背包”入口，取代另设“牧场仓库”的概念，后续用于查看动物配饰等需要保留的牧场持有物；牧场自动回收换钱的产物仍不进入背包。本轮只把现有背包入口加入牧场侧栏并复用已经定稿的背包图标／文字参数，不新增库存假数据、配饰面板或后端操作。
- 菜单格背景重画：辛玥否定用 `opacity: 0.42` 把旧 `tool-cell.png` 压成半透明的处理；本轮改为重画实色不透明、正方形、存在感更轻的菜单格背景，并恢复完整不透明显示。只替换菜单工具格底图与对应透明度样式，不改十二项图标／文字定稿参数、Menu 开合、底部场景栏、业务入口或其他页面。
- 菜单格材质纠正：第一张实色轻菜单格被辛玥否定为过于平、像空白奶油块，与上方木质 `Menu` 割裂；该版不作为视觉结果。重新按 `tool-menu.png` 的暖木像素材质绘制浅蜂蜜木框、细木纹和奶油内衬，边框比 Menu 薄且存在感低，但保留完整不透明与可辨质感；未获她确认前不登记稳定视觉基线。
- 集市跨场景入口：辛玥确认料理台和牧场也都可以查看集市；本轮把现有同一个“集市”入口加入牧场与料理台侧栏，三处复用同一图标、定稿参数和后续权威页面，不复制集市状态、不新增场景专属集市或假数据。
- 底部场景焦点范围：辛玥指出点击“牧场”等底部场景后，当前 3px 蓝色 `focus-visible` 轮廓沿整块大触控区显示，视觉范围过大。保留整块至少 44px 的可点击热区和键盘可见焦点，但移除整块蓝框，改为只在图标与文字附近提供小范围焦点提示；不缩小按钮、不改底栏比例和场景切换行为。
- 证据读取范围：本任务小节；`docs/{product-plan,farm-human-ui-game-migration-proposal,runtime-architecture,DEBUG_INDEX}.md` 中当前农场 Human UI 目标与真实边界；`apps/web/package.json`、`apps/web/src/{app.tsx,auth/**,preview/**}` 及直接测试；`packages/protocol/src/index.ts` 中现有农场概览合同；`apps/server/src/app.ts` 中 `/api/farm/overview` 的直接路由，仅核对浏览器可用合同；`old-vps/farm/content/{cooking,recipes}.json` 的食材／食谱目录、分类和真实字段；`old-vps/farm/assets/cooking/{ingredient-atlas,ingredient-atlas-2,dish-atlas,dish-atlas-2,fishing-cooking-atlas,odd-dish}.*` 的正式缩略图；`old-vps/farm/dist/web.js` 第 60–115 行仅限食材／料理 Atlas 的稳定 ID-to-frame 映射函数，并扩展只读旧引擎源码／构建产物中直接负责料理商店每日食谱选取、刷新、价格单位和返回字段的最小片段。不得读取或复制旧 Human HTML／CSS，不读取玩家存档、凭据、生产请求或范围外 diff。
- 精确写入范围：本任务小节；辛玥本轮明确授权新增 `AGENTS.md` 的场景型页面统一逻辑画布／等比例缩放规则；定向更新 `docs/product-plan.md` 当前农场 Human UI 工具栏与人类商店购买归属条目；`apps/web/src/app.tsx`；`apps/web/src/auth/{auth-client.ts,auth-client.test.ts}`；`apps/web/src/preview/{candidate-two-preview.tsx,candidate-two-preview.test.ts}`；新建 `apps/web/src/farm/**`；新建 `apps/web/public/farm/**`，其中本轮只修改 `apps/web/src/farm/{farm-page.tsx,farm-page.css,farm-page.test.tsx,farm-asset-manifest.ts}`、新增 `apps/web/src/farm/farm-cooking-catalog.ts`，并新增 `apps/web/public/farm/cooking-catalog/**` 作为从旧引擎既有正式 Atlas 迁入的运行素材；完成后定向更新 `docs/DEBUG_INDEX.md` 并删除本任务小节。不修改 `apps/server/**`、`packages/protocol/**`、`old-vps/farm/**`、`candidate-two-source.ts`、玩法／结算／存档、MCP、Prompt、其他任务脏文件、Git、部署或生产。
- 已知边界与直接证据：当前页面仅展示 `/api/farm/overview` 返回的地块编号、空闲／生长中／成熟、种子类型和浇水次数；辛玥要求删除顶部农场门牌木牌后，农场名与门牌不再占用场景顶部。错误明确区分登录、群资格、资格核验服务、资料缺失、农场不存在、不可公开读取、农场服务不可用、网络和未知响应。没有新增金币、银币、背包、动物、料理、假操作成功或静态贴图；牧场、料理和其他操作只显示尚待对应结构化合同接入的说明。农场页面保持点击后才加载的独立产物：最终本轮构建为约 10.37 kB JS／10.87 kB CSS（gzip 均约 3.25 kB），没有把本页加入社区首屏资源；现有主包仍有大于 500 kB 的既存构建警告，本任务未以 PWA 或阈值修改掩盖。正式烤炉、炖锅、炒锅、油炸锅、蒸炉、甜品工具和饮品工具七张 PNG 均已定向确认带 Alpha 通道；甜品与饮品的原批准源图继续保留，只用于说明确认造型，不作为运行素材。最终定向测试 8/8、限定 Biome、web build 均通过。隔离实页在 390×844 与 430×932 下外壳分别完整铺满当前视窗且均无 body 横向溢出；工具底框分别约 53.43px／58.91px，图标分别约 49.13px／54.18px，图标与文字垂直范围约重叠 5.5px，奶白透明轮廓生效；390 实页确认工具文字实际使用 `font-weight: 900`、四向 0.7px 棕色字芯扩边与四向 1px 奶白外轮廓，console 无 warning/error。
- 本轮固化与技术证据：十一项正常侧栏已逐项使用辛玥最终 hash 参数，不再使用统一默认位置；`recipes` 已从复用 `codex.png` 改为独立透明食谱本图标 `apps/web/public/farm/ui-icons/recipes.png`，保留文字独立图层。定向测试 10/10、web typecheck、限定 Biome、web build 与限定 diff-check 通过；构建仅有既存主包大于 500 kB 提醒。隔离 390×844 与 430×932 技术检查确认外壳分别为 390×844／430×932、body 横向宽度分别为 390/390 与 430/430、菜单实际格宽分别约 53.43px／58.91px、正常侧栏内联参数生效；料理台食谱图标自然尺寸 192×178、透明资源完成加载，console 无 warning/error。技术检查完成，等待辛玥本人查看正常页面并进行视觉验收。
- 熔炼入口技术证据：农场侧栏与一次性编辑器均已新增 `smelting`；独立 `apps/web/public/farm/ui-icons/smelting.png` 为 192×173、38,678 bytes、带 Alpha 的像素熔炼炉／金属锭素材。点击仍只进入现有未接入提示，不产生本地熔炼状态。最新“设置／食谱／熔炼”参数均已覆盖正常侧栏；定向测试 10/10、web typecheck、限定 Biome、web build 与限定 diff-check 通过，构建仅保留既存主包体积提醒。技术检查完成，等待辛玥本人查看正常侧栏并视觉验收。
- 本轮菜单与入口技术证据：牧场侧栏已复用“背包”和“集市”，料理台侧栏已复用“集市”；产品方案同步明确牧场背包取代独立牧场仓库，并继续排除自动回收产物。菜单格已从旧半透明底图改为 192×192、29,316 bytes、带 Alpha 的实色木纹 `tool-cell-textured.png`，运行样式 `opacity: 1`；首张被否定的扁平 `tool-cell-light.png` 不再被代码引用。底部场景整块 3px 蓝色焦点框已移除，键盘焦点改为图标小轮廓和文字下划线，按钮热区及底栏比例不变。定向测试 10/10、web typecheck、限定 Biome、web build 通过，构建仅保留既存主包体积提醒；视觉仍等待辛玥本人查看。
- 邻里与播报结构：辛玥确认底部新增独立第四项“邻里”，排行、完整留言与原创作物归入该页面；右侧原 `Menu` 位置改为与“邻里”不同的独立“叮咚播报”入口，只用于查看进行中任务、已确认成熟提醒和最近留言摘要，不包含足迹。农场／牧场／料理台的场景工具不再通过 `Menu` 点击展开或收起，改为常驻显示；当前没有这些邻里动态的结构化前端合同，本轮只能落入口、信息层级与明确空状态，不得生成假任务、假成熟提醒、假留言、假排行或假原创作物。
- 邻里与播报当前实现：底栏已改为四等分并新增“邻里”；排行、留言与原创作物位于邻里页，三个原场景工具列常驻显示且不再由 `Menu` 展开／收起。右侧原位置使用独立“叮咚播报”入口；播报打开后只列进行中的任务、成熟提醒、最近留言三类明确空状态，没有静态假动态或足迹。新增项目资源 `apps/web/public/farm/ui-icons/{neighborhood,dingdong-bulletin}.png` 均为 192×192 RGBA；首轮过度复杂稿未接入代码。辛玥否定继续加工旧铃铛后，已重新生成一只构图原生顺时针倾斜 45°、背景从生成源开始就真正透明的新铃铛并替换 `dingdong-bulletin.png`（约 17.3 kB）；透明角像素、Alpha 通道与奶油底合成结果均已核对，没有复用机械旋转稿，也没有改变尺寸、位置、文字、页面结构或其他素材。定向测试 11/11 与限定 diff-check 再次通过；此前限定 Biome、web build 通过，本地 5203 预览返回 200，最终视觉等待她本人查看新图。
- 播报调位编辑：现有一次性工具编辑器首项已从废弃的“菜单／旧 Menu 图标”替换为当前“叮咚播报／新透明铃铛”，正常侧栏与编辑器读取同一个 `bulletin` 布局键；无 hash 初次打开时直接继承正常侧栏当前定稿参数，不再跳回统一默认值。其余工具参数、邻里底栏、业务行为和正常页面效果不变。定向测试 11/11、限定 Biome 与限定 diff-check 通过；辛玥可在 5203 的 `editor=farm-tools` 页面调整，完成后按 URL hash 原样固化播报图标与文字参数。
- 播报最终参数：辛玥已在一次性编辑器完成“叮咚播报”调整并明确 `OK`；已按当前 URL hash 的 `iconX 91.953125`、`iconY 77.484375`、`iconSize 211.2`、`textX 97.7265625`、`textY 143.6640625`、`textSize 35.52` 原样覆盖正常侧栏，只改 `bulletin` 一项，不改其他工具或页面。定向测试 11/11、限定 Biome 与限定 diff-check 通过。
- 播报底框修正：辛玥查看正常页后指出“叮咚播报”缺少工具栏底图；本轮只让播报按钮复用现有 `tool-cell-textured.png` 木纹底框，不改她刚确认的图标／文字参数、常驻工具列或行为。
- 四项底栏重画：辛玥指定参考图左下“田地／牧场／料理”场景切换栏的结构，把现有四项做成贴住逻辑画布底边的四等分游戏栏：选中项为奶油色，未选中项为木色，图标与文字位于各自格内；不是悬浮白色面板，也不是四个图标单独漂浮。同步移除当前错误的蓝色图标焦点框，以同画风暖色焦点反馈保留键盘可用性；不改四个入口、场景内容或业务行为。
- 地块结构纠正：辛玥指定参考图中的独立小方田阵列；现有真实地块继续逐格映射，但去掉包住全部地块的大棕框，把小地块缩小并直接落在草地逻辑画布上，避开背景房屋。只改布局与地块外观，不增加地块、不改变真实状态和操作。
- 底栏与地块当前实现：首张深棕底栏已退出引用；实页改用全新文件名 `apps/web/public/farm/ui/scene-tabs-frame-v2.png`（1075×177）的浅蜂蜜木／低饱和卡其四格栏，左右与底部贴住逻辑画布，新文件名同时绕开旧静态素材缓存。选中格使用奶油色内嵌层，键盘焦点为暖色内轮廓，不再包住单个图标。农场地块去掉整组外框，缩为 49% 画布宽的四列独立小方田，并使用新透明贴图 `apps/web/public/farm/ui/plot-tile.png`（256×256 RGBA）代替 CSS 土色渐变；真实地块数量、状态、详情与点击行为不变。定向测试 11/11、限定 Biome、限定 diff-check、开发服务新 CSS 引用和贴图 200 均通过；技术检查完成，等待辛玥刷新实页查看。
- 底栏／地块素材纠正：辛玥实页否定首张四格底栏为过黑、浑浊的深棕色，该稿立即作废；重画时未选中格必须使用参考图的低饱和橄榄棕／浅蜂蜜木色，禁止巧克力深棕。地块不能继续以 CSS 渐变代替素材，本轮新增一张真正透明、可重复复用的独立小方田土块贴图；不重画作物，不改变地块数据。
- 料理工具定位编辑：辛玥已完成一次性 `editor=cooking-tools` 调整并要求关闭该编辑器。正常料理台最终参数已按她最后地址原样固化：烤 `49.39713550883651／66.69322842368047／64.5`，炖 `49.229278975741245／67.18072408991446／45.5`，炒 `48.85234164420485／68.54087925203899／46.5`，油炸 `50.44958726415094／66.1706783369803／50`，蒸 `48.95447270889488／67.35378953650289／63.5`，甜品 `50.42642351752021／66.61279092898349／52`，饮品 `51.07922001347709／68.60751939526557／51`，依次为 `x／y／width`；煎继续与炒共用同一口锅及同一参数。临时查询入口、hash 读写、编辑界面和专属样式已经删除，正常料理台工具切换保留；没有改背景、烹饪方式、资产造型、菜单、地块、底栏或业务逻辑。
- 田阵列与植物素材：辛玥确认田地每排最多 5 块、可视最多 6 行；超过 30 块时只能在田阵列内部继续查看，不能扩出第七行破坏场景。删除当前 CSS 拼接的茎、叶、果实和空地横杠，空地只保留土块；植物素材分成普通、奇幻、限定三套，每套分别绘制生长中与成熟形态，轮廓必须明显不同。当前 `/api/farm/overview` 只有 `seed_type`、没有类别字段，因此本轮只保存三套素材并让无类别实页使用普通套，不按名称猜测奇幻／限定归属；真实种子类型仍以接口详情文字为准。
- 社交开关最新实现：辛玥实页指出首轮未知状态把滑块停在中间，并在滑轨外额外显示 `—`，形成三态仪表而不是正常开关；这两处错误均已撤销。四枚控件现在只显示滑轨与圆形滑块，固定为关时靠左、开时靠右的标准二态结构；未接接口的 `null` 仍通过无障碍名称表达“未设置”，可见形态按关闭位呈现，首次点击直接开启。继续保留键盘焦点、44px 触控热区与 Demo 会话草稿边界，不改设置字段、保存合同或其他面板。农场定向测试 31/31、web typecheck、三文件限定 Biome、web build（115 modules）、限定 diff-check 与 5203 页面 200 均通过；构建只保留既存主包大于 500 kB 提醒。技术检查完成，等待辛玥本人视觉验收。
- 设置空值最新实现：辛玥明确要求未填写的设置项保持真正空白，不使用 `—` 作为输入框、欢迎语或称号空选项的可见占位。`FarmSettingsPanelContent` 内农场名、小机昵称、你的昵称、欢迎语及佩戴称号空选项的横线均已删除，保留现有可见字段标签、真实值、编辑能力与空字符串语义；创造表单等其他面板中的占位未动，没有新增默认文案或假称号。农场定向测试 31/31、web typecheck、三文件限定 Biome、web build（115 modules）、限定 diff-check 与 5203 页面 200 均通过；构建只保留既存主包大于 500 kB 提醒。技术检查完成，等待辛玥本人视觉验收。
- field／帮收批次精确写入范围：farm 发布线只定向修改独立 worktree 内 `dist/server/{human-structured.js,doorbell-internal.js}`，新增 `dist/server/human-harvest-assist.js` 与 `tools/doorbell-human-harvest-assist.test.mjs`，复用旧 Human 一键帮收动作链并提供严格结构化 action；不修改 `dist/{engine.js,game.js,store.js,server.js,web.js}`、Ranch 或 Kitchen。共享 main 只定向修改 `packages/protocol/src/index.ts`、`apps/server/src/{farm-human-client.ts,farm-human-client.test.ts,registration-auth.ts,registration-auth.test.ts,app.ts}`、`apps/web/src/auth/{auth-client.ts,auth-client.test.ts}`、`apps/web/src/farm/{farm-overview.ts,farm-page.tsx,farm-page.css,farm-page.test.tsx}`，以及本任务小节和 `docs/{product-plan.md,farm-human-ui-game-migration-proposal.md,runtime-architecture.md,DEBUG_INDEX.md}` 的对应事实；不修改其他社区页面、斗地主、旧 HTML、生产、部署或 Git。定向验证仅覆盖新 farm action、Doorbell client／route、农场 React 与必要的 protocol／server／web 类型检查、限定 Biome／build／diff；整体任务仍不删除票。

- 第一批四场景资源拆分完成：`farm-page.tsx` 保留共同壳和页面会话状态，新建 `apps/web/src/farm/scenes/{field,ranch,cooking,neighborhood}/**` 四个 lazy 场景模块及直接背景 CSS；初始只访问并挂载农场，牧场／料理／邻里首次进入才加载，访问后继续隐藏挂载以保留分类、面板和滚动位置。农场首屏不再请求 `ranch-background.png`、`cooking-background.png`、`neighborhood-background.png` 或 `neighborhood-shell.png`；`stew-pot.png` 仍会因底部料理页签复用同一大锅具图而请求，本批没有改视觉／素材。大型面板、独立底栏小图标、内容 hash、manifest 覆盖检查、统一 PWA 与公开源文件清理仍未完成。农场定向测试 32/32、web typecheck、11 文件限定 Biome、web build（123 modules、四场景分别生成 JS／CSS chunk）与 diff-check 通过；只保留既存主包大于 500 kB 警告。技术检查完成，等待辛玥本人视觉验收。
- 权威 farm field adapter 已完成读取与一键帮收两条本地结构化链：`/internal/doorbell/human/field/read` 继续保持纯投影；`/internal/doorbell/human/field/harvest-assist` 在 clone 上直接复用旧 Human 完整结算链，并把幂等 receipt 与新 farm 状态通过一次 `replaceFarm` 原子提交，保存失败同时回滚内存和磁盘。普通／奇幻即使成熟仍在 read 中保持 `hidden + null`，只在成功 receipt 首次返回真实作物；限定／原创继续按稳定 ID 解析。action-safe revision 覆盖收获结果依赖的隐藏 farm 状态与时间规则，receipt 自身不参与摘要；同键同请求重放原结果，同键异请求、陈旧状态、无成熟地块和次数耗尽均不推进随机数或存档。Ranch、Kitchen 与农场目录的独立纯读取随后也已接通，但没有增加这些领域的写 action。field 新 action、既有 read／internal 回归合计 3/3 与三个 JS 语法检查通过；未改共享 `old-vps/farm/**`、生产或 Git。

- Doorbell 社区侧 field 读取与一键帮收动作均已在本地接通：`GET /api/farm/field` 与 `POST /api/farm/field/harvest-assists` 都只从当前 Human session 派生私有 key 和预期门牌；浏览器不能覆盖身份、门牌或选择地块。POST 严格要求空 body、UUID `Idempotency-Key` 和带引号的 `If-Match`，成功严格校验真实 receipt、完整 field、revision 与 server time；业务拒绝、状态冲突、幂等冲突、资格／绑定、上游合同和可用性错误保持分离并全部 `no-store`。React Live 已改用 field GET，权威 `can_assist` 为真时开放“一键帮 TA 收”；未知网络结果只允许用原幂等键重试，成功后整体替换响应中的 field，不本地删地、扣次数或加币。定向复验：protocol build、server typecheck、client／route 38/38、前端 auth＋farm 58/58、farm action/read/internal 3/3、限定 Biome与 Vite build（130 modules）通过；完整 web typecheck 的农场新增错误为零，仅被范围外已停斗地主文件既存 TS2322 阻塞。已进入 Git，当前未部署或生产验证。

- 第二批前端面板拆分已完成当前安全边界：只修改 `apps/web/src/farm/{farm-page.tsx,farm-page.css,farm-page.test.tsx}` 并新增 `apps/web/src/farm/panels/**`，把叮咚播报、通用工具面板和商店拆成独立 lazy JS／CSS chunk；商店只在通用工具面板内实际打开时继续加载，field 首屏不预载这三组面板。页面／场景／工具栏与购物车、设置、原创植物等草稿状态仍由共同壳持有，四页访问后继续隐藏挂载，因此关闭、切页和再次打开仍保留既有分类、草稿与滚动位置。为避免跨 chunk 丢图，只把牧场居民详情与料理备料弹窗确实共用的两项 sprite 基础样式留在共同 CSS；没有改视觉、业务文案、路由、素材、manifest、PWA、后端、Git、部署或生产。农场定向测试 33/33、web typecheck、10 文件限定 Biome 与 web build（130 modules）通过；构建产生独立 bulletin／tool／shop JS／CSS chunk，`index.html` 不预载它们，只保留既存主包大于 500 kB 警告。图鉴、熔炼、设置、创造等当前仍共用同一个 tool chunk，尚未达到“每个大型工具各自只加载自己的模块”；后续需在不改变页面状态的前提下继续细分。技术检查完成，等待辛玥本人视觉验收。
- 铃野结构化读取批次：Candidate Two 的流光原野与铃野共行 Live 页面已经在本地接到 farm 权威玩法，不再跳入、嵌入或解析旧 Human HTML。流光原野读取在 farm／world clone 上投影真实状态，不保存、结算或消耗次数；铃野共行保持旧 Human 页面既有的 farm 权威到期推进与一次保存语义，再对白名单 Human 字段做安全投影，Doorbell 不复制剧情状态机或结算。普通旧 `/api/lingye-glimmer`／`/api/lingye-together` HTML 路由暂留迁移兼容；新 UI 使用 `/api/lingye/glimmer`／`/api/lingye/together` JSON，点击后才加载，不预取、不轮询、不回退旧 HTML 或 Demo。本批没有增加 Human 玩法写操作，已进入 Git，当前未部署或生产验证。
- 本批精确读取／写入范围：读取独立 farm worktree 的 `dist/{glimmer.js,public-expedition.js,server/doorbell-internal.js,store.js}`、对应 content／正式阶段图与定向测试；共享 main 读取协议、farm service client／registration auth／app wiring、Candidate Two iframe bridge 与现有定向测试。farm 发布线只新增 `dist/server/{glimmer-structured.js,together-structured.js}`、修改 `dist/server/doorbell-internal.js`，并新增 `tools/doorbell-human-{glimmer,together}-structured.test.mjs`；不改 `dist/{engine.js,game.js,mcp.js,server.js,store.js,web.js,glimmer.js,public-expedition.js}`、内容、玩法、Prompt、HELP 或生产。共享 main 只修改 `packages/protocol/src/index.ts`，新增 `apps/server/src/{farm-lingye-client.ts,farm-lingye-client.test.ts}`、定向修改 `apps/server/src/{registration-auth.ts,registration-auth.test.ts,app.ts,index.ts}`，新增 `apps/web/src/auth/{lingye-client.ts,lingye-client.test.ts}`、定向修改 `apps/web/src/{app.tsx,app.test.tsx}` 与 `apps/web/src/preview/{candidate-two-preview.tsx,candidate-two-preview.test.ts}`；为完整承接 farm 返回的八个受控阶段图 key，保留现有 opening 图并只新增从 farm 正式 JPEG 等比例压缩的 `apps/web/public/lingye/together/{same-kitchen-old-recipe,same-kitchen-undelivered-letters,same-kitchen-service,same-kitchen-final-arrangement,same-kitchen-ending-one-sign,same-kitchen-ending-next-door,same-kitchen-ending-public-kitchen}.jpg`；同步本任务小节和 `docs/{product-plan.md,runtime-architecture.md,DEBUG_INDEX.md,farm-human-ui-game-migration-proposal.md}` 对应事实。不得重画图片、改视觉 CSS／页面坐标、斗地主、其他社区页面、Git、部署、生产或辛玥现有浏览器。
- 铃野结构化读取技术结果：farm 严格 adapter 返回流光原野真实开放状态、今日踪迹、协作、公共事件、57 项异色图鉴及解锁状态、20 项奇遇、概况和 12 项成就；铃野共行只返回当前故事、轮次、阶段、状态、受控插图 key、历史、任务、选择、冷却、结局与线索，不外泄答案、农场 ID、原始参与者／投票、完整奖励或档案内部结构。Candidate Two 父层只在点击两页入口时发起当前请求，并用请求序号／AbortController 防旧响应覆盖；Demo 不请求接口，Live 不读取静态样例。八个受控阶段图 key 均映射到社区内等比例压缩的正式 4:3 JPEG，未知 key 不猜路径。自审补正三处真实边缘错误：重开投票保留实际选项且允许阶段序号为 `null`；冷却与结局正文进入既有当前状态文字位，不再误写成“没有阶段任务”；流光无时间旧事件与损坏的共行选择历史直接丢弃，不再伪造 1970 时间或第 1 步 A。最终定向验证：farm 结构化读取、field／帮收、第二期与异色回归 15/15；Doorbell client／session route 6/6；Web client／父层桥／Candidate Two 19/19；protocol typecheck＋build、server typecheck＋build、13 文件限定 Biome、farm 三文件语法检查、两条 projector 回归及直接 Vite build（131 modules）通过。普通 Web typecheck 仍只被范围外已停斗地主、飞行棋测试及 UNO 的既存 TypeScript 错误阻塞，输出不包含本批目标文件；未修改这些游戏。已进入 Git，当前未部署或生产验证。

## DESIGN-LINGYE-TOGETHER-GLIMMER-UI-STYLE-20260821

- 模式／状态：施工／流光原野与铃野共行视觉结构已经按最新决定落地；辛玥本轮进一步明确要求把两页 Live 直接接回现有 farm 权威状态。视觉稿、逻辑画布、动物坐标、封面与纪念册素材均不返工；本批只清除 Live 静态样例泄漏并接入点击后懒加载的结构化真实数据，Demo 继续独立。
- 证据读取范围：本任务小节；辛玥本轮参考图；`docs/product-plan.md` 中铃野一级入口、铃野共行、流光原野、统一社区前端与迁移暂停边界；`apps/web/src/preview/{candidate-two-preview.tsx,candidate-two-preview.test.ts}` 的当前地图入口、页面注入与定向断言；`old-vps/farm/content/{public-expedition,glimmer}.json`、`old-vps/farm/dist/{public-expedition,glimmer,web}.js` 中只读 Human 数据字段和插图映射；`old-vps/farm/LINGYE_TOGETHER_SEASON_2_OUTLINE.md` 中已确认但尚未开放的第二期标题、阶段结构与插图边界；`old-vps/farm/assets/{lingye-together/same-kitchen-opening-v3.png,glimmer/map-scene.webp}` 的既有正式素材。铃野共行玩法结构沿用 `lingye-together` 设计合同；不读取生产玩家数据、凭据、Prompt、模型可见文案或范围外任务 diff。
- 精确写入范围：既有视觉设计与 CSS 均保持不动；本任务仅配合 BUILD 票定向修改 `apps/web/src/{app.tsx,app.test.tsx}`、新增 `apps/web/src/auth/{lingye-client.ts,lingye-client.test.ts}`，以及修改 `apps/web/src/preview/{candidate-two-preview.tsx,candidate-two-preview.test.ts}` 中两页的严格数据槽、Live／Demo 分流、父层懒加载消息桥与断言。为让 farm 已有八个受控阶段图 key 都能在同一既定封面位显示，只新增七张从 farm 正式 JPEG 等比例压缩的社区运行图，路径为 `apps/web/public/lingye/together/{same-kitchen-old-recipe,same-kitchen-undelivered-letters,same-kitchen-service,same-kitchen-final-arrangement,same-kitchen-ending-one-sign,same-kitchen-ending-next-door,same-kitchen-ending-public-kitchen}.jpg`；不重画、不裁切、不改构图。后端／协议／farm adapter 由 BUILD 票的同一批次负责。不得修改页面坐标、视觉 CSS、`apps/web/src/components/**`、剧情／任务／选择／奖励文案、玩法结算、Prompt、Git、部署或生产。
- 已确认视觉基线：两页都属于同一 Doorbell Commons 铃野体系，保留克制轻按钮与移动端单列阅读节奏，但不强制复制同一页头、卡片或导航结构。铃野共行使用当期真实活动封面／当前阶段图，每一期随实际故事素材变化；封面直接顶到页面顶部，左上只叠轻量返回，页面不显示「铃野共行」四字或「本期封面」标签，右上用无外框小本本 SVG 作为往期故事入口且暂不施工具体展示；正文取消白色圆角卡片、阴影和卡片套卡片，直接排在连续纸页上。流光原野以自身固定地点的深蓝星夜、月色、萤火和远山水面为主视觉，已经融进背景的地点标题就是页头，不再重复显示品牌文字；原野状态直接叠在场景下沿成为轻量夜色 HUD，协作、发现与日志继续留在夜色中，只用文字层级、留白和轻微星光分隔，不使用大暖纸板或同尺寸圆角方块；进入流光原野后隐藏底部社区导航，返回铃野地图时恢复。两页不得做成同一张换色卡片模板。
- 已知边界：铃野共行仍是全服唯一共享剧情状态，AI 是任务行动主体，Human 端只读当前进度、剧情、插图、前情与已确认档案；流光原野仍是铃野独立地点页。Live 改接 farm 新结构化只读合同后，不解析旧 HTML、不生成假动物／任务／公共事件／图鉴／剧情／选择／奖励或结算，也不以 Demo 兜底；没有数据时显示诚实空态，错误时保留当前页面壳并显示读取失败。`demo=full` 继续只由 `window.__doorbellCandidateDemo` 驱动现有视觉样例，不发真实请求。第二期《同一间厨房》是否成为当前线上期完全取决于 farm 实际返回，不由前端静态模板决定。
- 纪念册边界：纪念册属于铃野地图界面的独立只读限时活动档案入口，不放在铃野共行正文内，也不是新的一级页面、奖励中心或农场存档副本。地图徽章现在可点击；`demo=full` 进入地图内只读子页面，只展示已确认的 2026 七夕活动名「灯河有信」、年份与主题句，并可返回铃野地图。不得编造参与者、私人灯笺、奖励领取、完成度或结算，没有结构化详情合同前不提供假详情跳转或操作按钮；Live 保持诚实空态。
- 当前视觉改动：流光原野背景中的“流光原野”已改为有起伏笔势、星屑和叶枝穿插的梦幻手绘标题；重复品牌抬头已删除，原野状态已经写进场景。上一轮把下方三块内容合并成一张连续暖纸板仍然是在用卡片兜底，辛玥明确否定；本轮删除整个白色大容器，让协作、今日动物踪迹、奇遇发现与日志直接排在深蓝夜色上。右上返回地图控制同步去掉突兀的白色圆章、阴影和可见“地图”文字，只保留小号月金色线稿地图图标。按旧农场 `glimmerTracks()` 与 `ranchSprite()` 证据，demo 的今日动物踪迹改为蜜桃鸭、枫糖火鸡、晨雾月光蚕三张正式异色图，原本无权威条目的假白尾鹿已经删除；三图只证明纯前端版式，不模拟捕获，Live 仍为空态。铃野共行封面直接顶到页面上缘，封面可见标题和封面标签已删，正文区改为连续分隔排版；原正文内往期列表与七夕纪念册样张均已移除，往期入口改为封面右上小本本 SVG。铃野地图纪念册徽章已从禁用改为可点击，Demo 进入最小只读子页面并可返回地图；最新徽章由内建 ImageGen 从零重画为正方形一体化透明游戏徽章，中性纪念册主体、白色贴纸轮廓和内嵌「纪念册」文字牌共用同一张 PNG，文字牌覆盖图标下半部，页面不叠加 HTML 标签文字，也不包含河灯、月桥或七夕挂饰；地图显示宽度按辛玥反馈从 64–72px 收至 52–60px，保持 1:1 且不移动锚点。
- 技术验证：本轮候选预览定向测试 13/13 通过；web typecheck 通过；限定 Biome 通过；web build 通过（115 modules，仅保留现存主包大于 500 kB 警告）；限定 `git diff --check` 通过。新纪念册 PNG 为 256×256、RGBA、88,774 bytes，透明通道与准确「纪念册」文字已检查；三张正式动物裁图均为 200×200 RGBA，分别为 29,433／40,940／31,311 bytes。5203 对纪念册与三张动物资源均返回 200，本地预览保持运行；不接管或操作辛玥已有预览标签。
- 纪念册入口最新反馈：当前铃野地图徽章被代码与测试明确写成 `disabled`，因此不是遮挡或点击热区问题；该实现沿用了此前“只落入口、具体页面暂不施工”的旧边界。辛玥现已明确要求纪念册能够点击。徽章现已在 `demo=full` 中接入地图内的只读纪念册子页面，展示方案已经确认可用于版式证明的 2026 七夕「灯河有信」活动名、年份与主题句，并提供返回铃野地图；不展示私人灯笺、参与者、奖励、完成度或结算，不提供假详情按钮，Live 在结构化档案合同接入前仍不生成档案内容。精确写入限于本任务小节、`docs/product-plan.md` 对应纪念册决定及 `apps/web/src/preview/{candidate-two-preview.tsx,candidate-two-preview.test.ts}`；不改图片、农场、后端、协议、其他铃野页面、Git、部署或生产。
- 流光原野栏目最新反馈：当前“今日动物踪迹／今日发现／原野日志”使用固定左右两列，栏目眉题与标题被挤在左窄列、正文或动物列表落在右列，窄屏下标题会断成两行并形成不必要的表格感。辛玥明确要求标题不与正文并排，并指出下方内容不应继续套用这套过度设计。重新核对旧农场 `uiGlimmer()` 后，真实结构是栏目标题直接在上，随后紧接动物网格、协作内容或逐条事件，没有左侧眉题栏；本轮据此删除三个栏目额外眉题和条目内部的左右时间栏，统一改为标题独占整行、内容在下一行的简单纵向结构。不改动物图、名称、发现与日志样例、空态、原野背景或数据边界。 候选预览定向测试 13/13、web typecheck、两文件限定 Biome、web build（115 modules）、限定 diff-check 与 5203 页面 200 均通过；构建只保留既存主包大于 500 kB 提醒。技术检查完成，等待辛玥本人视觉验收。
- 流光原野模块完整性最新反馈：辛玥指出“今日发现”不是旧农场正式模块，必须删除；“原野日志”应恢复为真实的“公共事件”并使用时间线 UI；今日动物踪迹在移动端固定两列、形成两行。重新核对旧农场 `uiGlimmer()`，正式 Human 模块还包括异色动物图鉴、奇遇图鉴、我家的原野概况与流光原野成就，当前社区预览均遗漏。本轮已在 `demo=full` 补齐结构，数据只取旧农场已存在的异色 ID／动物名、奇遇名、统计字段和成就定义作为版式样例；Live 继续显示空态，不生成捕获、奇遇、奖励领取或结算成功，不新增图片或接口。动物只保留图片与名称，公共事件只保留时间与事件正文，两处解释性第二行均已删除；异色动物图鉴、奇遇图鉴与流光原野成就已从正文移入右上角折叠栏，“我家的原野概况”留在正文。按最新反馈，左上返回已移除旧圆形按钮继承，直接复用铃野共行的小号 `‹ 返回铃野` 文字视觉；右上已从“原野资料”文字块改为同高度、无底框的资料 SVG 图标，展开面板同步去掉厚重边框、阴影和毛玻璃。两枚控制均保留至少 44px 隐形触控热区与清晰键盘焦点，不改变折叠内容。一次性排版编辑器仍只在 localhost `demo=full&screen=glimmer&editor=glimmer-animals` 开启，不进入 Live。候选预览定向测试 13/13、web typecheck、两文件限定 Biome、web build（115 modules）、限定 diff-check 与 5203 编辑地址 200 均通过；构建只保留既存主包大于 500 kB 提醒。
- 铃野共行背景最新反馈：活动封面与下方连续纸面原先分别使用冷灰绿和暖奶油两套背景，封面底部遮罩在 58% 后完全透明，且封面下方另留 18px 纸面间距，因此形成明显上下断色。现已统一页面、纸面和封面收口为同一奶油纸底，封面最后一段自然渐隐到该底色，原有 18px 正文留白移入首段内部，不再暴露异色接缝；未修改活动封面素材、封面构图、正文内容、流光原野或其他页面。对应决定已同步产品方案；候选预览定向测试 13/13、web typecheck、两文件限定 Biome、web build（115 modules）、限定 diff-check 与 5203 页面 200 均通过，构建只保留既存主包大于 500 kB 提醒。
- 铃野返回控件最新反馈：流光原野不需要显示“返回铃野”文字，铃野共行与纪念册中同类可见返回文案也一并删除。三处现已只保留既有 `‹` 返回符号、原返回功能与无障碍名称，内容正文中的普通“返回”语义、页面路由、触控热区和右上入口均未改变；决定已同步产品方案。候选预览定向测试 13/13、web typecheck、两文件限定 Biome、web build（115 modules）与限定 diff-check 均通过，构建只保留既存主包大于 500 kB 提醒。
- 流光原野一次性编辑器最新反馈：上一轮把 range 滑杆改成无限制数字输入仍然误解了“自由编辑”；辛玥要的是直接在画面中按住每只动物拖动，数字面板方案已经作废并删除。三只动物本体现在各自可直接拖拽，移动不设边界；拖动时只改变该动物相对网格起点的 `x/y`，松手后通过严格校验的本地 Demo 消息把三组坐标写入当前顶层 URL，刷新可恢复并便于读取最终布局。编辑能力继续只在 localhost `demo=full&screen=glimmer&editor=glimmer-animals` 开启，正式页面、Live 数据、动物素材与非编辑态网格均未改变。候选预览定向测试 13/13、web typecheck、两文件限定 Biome、web build（115 modules）、限定 diff-check 与 5203 编辑地址 200 均通过；构建只保留既存主包大于 500 kB 提醒。
- 流光原野未揭晓状态最新反馈：今日动物踪迹最后一项的 Demo 权威字段设为未揭晓，页面只显示一个大 `?`，不提前显示真实动物图片或名称，也不在下方生成重复小问号；揭晓后才按字段显示实际动物，前端不推算时间或自行切换。
- 流光原野动物顺序纠正：上一轮误把第三项晨雾月光蚕当成未揭晓问号，导致真实月光蚕素材消失且问号错误继承 `gaMoth*` 坐标。辛玥已明确四项顺序为蜜桃鸭、枫糖火鸡、晨雾月光蚕、未揭晓问号；前三只继续读取她当前 URL 中各自图片与文字坐标，第三只月光蚕的位置以蜜桃鸭的实际位置及蜜桃鸭到火鸡的距离作为排版参考，第四个问号只占独立末位，不顶替真实动物，也不读取 `gaMoth*`。第四格只显示一个可拖动的大问号，不再生成其下方重复的小问号 caption。
- 流光原野协作标题最新反馈：Demo 已有“三家合力的料理食材”协作任务时，上方仍显示“今晚还没有新的协作／新的全服任务会……”的空态文字，语义直接冲突。本轮删除有任务状态中的这组冗余空态展示，只保留“今日协作任务”、真实任务内容与进度；无任务时仍保留诚实空态，不改任务数据或结算。
- 异色动物图鉴最新反馈：当前右上折叠栏错误复用了今日踪迹的三只样例，并以两列展示；旧农场 `content/glimmer.json` 的权威异色清单实际共有 57 项（16 类生产动物、2 类宠物与巡逻鹅各 3 套外观），对应三张 5×4 透明图集。Demo 图鉴改为全量 57 项并固定一行三项，使用权威 `id/name/set/kindId` 与原图集索引裁格，不生成假名称、不把图鉴项接入今日踪迹拖拽坐标；Live 仍只按未来结构化合同显示实际解锁状态。
- 本轮实现与缩放边界：第三只晨雾月光蚕已经恢复，第四格为独立 `mystery` 大问号且不再生成下方小问号；任务空态补上严格 `[hidden]` 样式，不会再与已有任务同时出现；右上图鉴已经接入全量 57 项和三张权威 WebP 图集并改为三列。一次性编辑器继续保存 292px 设计宽度坐标，四列网格、动物主体、名称、问号与整组拖动偏移统一用踪迹容器 `cqw` 等比例换算，拖动增量再按当前容器比例反算，因此窄屏／宽屏只整体等比，不再把坐标硬绑背景或视窗像素。
- 技术验证：候选预览定向测试 13/13 通过；web typecheck 通过；两文件限定 Biome 通过；web build 通过（115 modules，仅现存主包大于 500 kB 提醒）；限定 diff-check 通过。三张 1000×800 权威 WebP 图集保持原有压缩文件，合计 1,347,248 bytes；5203 当前编辑页与三张图集均返回 200。未操作辛玥已有浏览器标签。
- 动物整组对齐最新反馈：上一轮仍在旧两列网格上分别用偏移量“微调”四项，月光蚕名称基线因此高于蜜桃鸭／枫糖火鸡；辛玥已明确图片与名称属于同一动物整体，不应只移动文字。正式态与编辑态现已统一为真实四列逻辑网格，前三格分别绑定图片和名称，第四格只保留问号；拖动任一已揭晓动物会移动整组，名称在同一网格中自然对齐，四项统一下移。旧编辑坐标打开时按当前排版版本一次迁移，首次继续拖动后写入最新 `gaLayout` 并删除旧名称坐标，避免重复迁移。
- 技术验证：候选预览定向测试 13/13 通过；web typecheck 通过；两文件限定 Biome 通过；web build 通过（115 modules，仅现存主包大于 500 kB 提醒）；限定 diff-check 通过。5203 已重新启动，带辛玥旧坐标的编辑地址返回 200；未操作她已有的浏览器标签。
- 新坐标与资料弹层最新反馈：辛玥在 `gaLayout=4` 下把四项改到 `duck -10.2/-1`、`turkey -9.6/-0.3`、`moth -8.9/-0.8`、`mystery -3.1/0`；当前编辑默认与旧版迁移现已将前三只收成同一 `-9.6/-0.5`，问号保留独立横向视觉中心并只将纵向对齐到 `-0.5`，后续拖动写入 `gaLayout=5`。右上资料弹层已改成完全不透明的 `#07172b` 深蓝实底并明确层级；折叠标题保持 12px，奇遇／成就正文名称降为 9px，状态／奖励／进度保持 8px；栏目和条目之间的内部横线均已删除，只以 6–9px 留白分组。不改图鉴内容、任务、公共事件或其他页面。
- 技术验证：候选预览定向测试 13/13 通过；web typecheck 通过；两文件限定 Biome 通过；web build 通过（115 modules，仅现存主包大于 500 kB 提醒）；限定 diff-check 通过。5203 最新对齐地址返回 200；未操作辛玥已有浏览器标签。
- 资料弹层层级根因与修正：深蓝底已经完全不透明，但资料栏此前位于 `z-index: 2` 的 hero 层叠上下文内，后续正文自身为 `z-index: 3`，因此正文会整体绘制在弹层之上，既形成“透出底层文字”的假象，也会遮挡下方折叠标题的点击。现在资料栏打开时由 `:has()` 将整个 hero 提到 `z-index: 4`，完整压过正文；三个内部 `details` 没有互斥 `name` 或关闭脚本，均可独立点击并同时保持展开，不改变正文、资料内容或折叠初始状态。
- 技术验证：候选预览定向测试 13/13 通过，并明确断言打开态根层级高于正文、三个内部折叠栏均存在且没有互斥 `name`；web typecheck、两文件限定 Biome、web build（115 modules，仅现存主包大于 500 kB 提醒）与限定 diff-check 均通过；5203 最新对齐地址返回 200。未操作辛玥已有浏览器标签。
- 奇遇／成就图鉴真实目录最新反馈：辛玥点名的是右上奇遇图鉴与流光原野成就的目录内容，不是异色动物图鉴或整页 Live 接口。此前 Demo 只写了 3 条奇遇与 3 条成就样例，确实遗漏真实目录；当前已把旧农场 `content/glimmer.json` 的 20 条权威奇遇，以及 `content/titles.json` 按原野奇遇／异色收集／原野协作各 4 级组成的 12 条权威流光原野成就完整映射进 `demo=full`。成就使用真实 `id/field/min/name/reward`，状态与进度只按当前 Demo 的 1 次奇遇／3 种异色／0 次协作派生；奇遇只把当前 Demo 已发生的“流光泉”标为已遇见，其余保持未遇见，不把样例状态漏进 Live，也不解析旧 HTML 或新增后端合同。
- 技术验证：候选预览定向测试 13/13 通过，并逐项断言 20 条奇遇与 12 条成就的 ID、名称、进度、奖励和状态；web typecheck、两文件限定 Biome、web build（115 modules，仅现存主包大于 500 kB 提醒）与限定 diff-check 均通过。未操作辛玥已有浏览器标签；视觉仍等待辛玥本人确认。
- Live 接线结果：既有两页 DOM、CSS、292px 逻辑坐标、动物整体排版、资料折叠、封面位置与返回控制均未返工。流光原野现在按 farm 返回的真实 tracks／cooperation／events／variants／encounters／summary／achievements 更新现有槽位，57 项异色目录无论是否解锁都保留权威条目并只标记状态；铃野共行按当前 `art_asset_key` 切换八张受控正式阶段图，并更新现有故事、阶段、历史、任务、选择、冷却、结局与线索槽位。加载、空态与错误继续使用同一页面壳，不显示 Demo 内容，也不回退旧 HTML。技术链检查已完成，但本地尚未经过辛玥本人新一轮实页视觉确认，整体视觉任务仍不标完成。

## FARM-GLIMMER-INDEPENDENT-HUMAN-SHELL-20260814

- 模式／状态：暂停；等待所有现有玩家完成 Doorbell 迁移并统一停用剩余旧农场链接，届时才重新审定是否施工流光原野独立 Human 页面壳。停止一切 farm 发布线动作。
- 证据读取范围：总规划窗口已同步的迁移后置决定与本任务当前现场；恢复前不继续读取代码、分支、测试或生产状态，不读取玩家存档、生产凭据或范围外任务 diff。
- 精确写入范围：暂停期间仅允许原位维护本任务小节；不得继续修改 `old-vps/farm/**`、社区代码／Schema／代理、其他页面、玩法、内容、入口 URL、测试、Git 或生产服务，不 commit、push、部署或重启。
- 已知事实：旧用户与社区 `GET /api/lingye-glimmer` 薄代理目前共用农场上游 `/ui/<humanKey>/glimmer`；现在切换 `uiGlimmer()` 会同时改变仍使用旧链接玩家的页面并移除其农场全局导航。当前 `old-vps/farm/dist/web.js` 仅存在未接线的本地草稿 `GLIMMER_STYLE` 与 `glimmerPage()`；`uiGlimmer()` 仍调用原通用 `page()`，所以现有渲染、旧用户、玩法、数据与 Doorbell 入口均未改变。草稿未测试、未 commit、未 push、未部署，不属于实现、候选发布或可部署状态。总规划已在 `docs/{product-plan,runtime-architecture,DEBUG_INDEX}.md` 记录相同后置边界。
- 未知／阻塞：明确阻塞条件是“所有现有玩家已经完成 Doorbell 迁移，且剩余旧农场链接已经统一停用”；在总规划确认该条件成立并由辛玥重新授权前不得恢复。
- 下一动作：无；保持暂停。收到迁移完成与重新施工授权后，先重新核对 farm 基线、共享路由边界和视觉方案，再决定继续或丢弃当前未接线草稿。
- 定向验证：暂停时不运行测试或构建；当前唯一有效结论是 `uiGlimmer()` 尚未接线新壳且没有任何发布动作，不能把草稿标记为已实现或已验证。

## QIXI-SILVER-CROPS-20260814

- 模式／状态：施工待办／2026 年七夕活动已于北京时间 2026-08-20 00:00 结束；活动期发布、降价退款与一次性补偿均已由事实索引承接，当前只剩逾期的活动代码收尾，尚未施工、提交、推送或部署。
- 证据读取范围：本小节；`docs/{product-plan,runtime-architecture,DEBUG_INDEX}.md` 的七夕永久成果与生产事实；独立 farm worktree 中七夕活动配置、任务监听、商店／Human 展示和对应定向测试。不得读取玩家身份、存档正文、凭据或其他仓库。
- 精确写入范围：本小节；farm 发布线中只删除已经结束的 `allin`／七夕 `qty`、活动商店提示、任务卡、任务监听、自动提交、退款临时代码与活动期回执分支，并更新直接测试。不得删除或回收玩家已经获得的种子、作物银币结算、图鉴、称号、历史解锁和补偿幂等事实；Git 与生产仍需当轮明确授权。
- 下一动作／阻塞：活动时间条件已经成立，没有产品阻塞；需要单独开工完成 farm 收尾并验证永久成果仍可读取。当前不得把“活动自然结束”冒充为代码已经清理。
