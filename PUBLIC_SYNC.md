# 公共联机农场

这个实例只保存联机副本，不读取或挂载任何人的私有单机目录。

## 两种玩法

1. 直接在联机服注册：`POST /farms`，之后使用返回的 `playUrl`。
2. 保留本地单机：打开 `GET /sync` 上传现有存档，或在本地仓库执行：

```bash
node farm-sync.mjs register --server https://proxy.duxy-home.com/farm
node farm-sync.mjs sync
```

首次注册会生成新的公开门牌号、主钥匙、人类页面钥匙、Agent key 和同步钥匙，不接受上传文件里的任何能力凭据，也不会覆盖同门牌号的现有农场。

后续同步使用独立 `farm_sync.json`（mode 600）里的同步钥匙和单调递增 `clientSeq`。联机服以客户端上次确认的规范快照为基线，将本地变化与联机期间发生的留言、足迹、浇水、偷菜、交易等变化做三方合并，再返回新的规范快照。同步请求重复发送时按 `clientSeq + payload hash` 幂等处理；同序号不同内容会返回 409。

同步/迁入请求总大小上限为 2MB，超出返回 413，不截断。普通游戏动作继续沿用上游 16KB 请求上限。

## 数据

- 运行数据目录由 `AIFARM_DATA_DIR` 指定。
- 联机世界保存为单一原子文件 `world.json`，同时包含 farms、UGC 和每座同步农场的哈希后同步元数据。
- 服务端只保存同步钥匙的 SHA-256，不保存明文。
- 导入存档的 `token/humanKey/agentKey/syncHub` 一律丢弃并重新生成。
- 来自其他服务器的留言、足迹、拉黑、访客记录和在售摊位不会作为可信联机状态导入。
- 本地存档可由持有人修改，因此这是自由娱乐服，排行榜和市场不提供竞技防作弊保证。

运行 `farm-sync.mjs sync` 时，不能有另一个进程同时写同一份本地 JSON 存档。
