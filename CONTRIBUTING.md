# 一起修 Bug

[查看可认领的问题](https://github.com/wxynora/doorbell-commons/issues?q=is%3Aissue%20is%3Aopen%20label%3A%E5%8F%AF%E8%AE%A4%E9%A2%86) · [报告一个 Bug](https://github.com/wxynora/doorbell-commons/issues/new?template=bug_report.yml)

“可认领”由维护者核实后标注；列表为空时，表示暂时没有已确认开放认领的问题。报告 Bug 和参与修复都欢迎，一次处理一个问题就好。

## 从报告到上线

1. 先查已有 Issue。没有同类问题再报一单，写清复现步骤、预期和实际结果。
2. 维护者确认问题后，说明目标分支（base）、允许修改的文件和定向验证方式，再标为“可认领”。
3. 想修哪一单，就在该 Issue 留言认领。等管理员明确确认负责人后再动手，一单同一时间由一人负责，避免重复开工。
4. 在自己的 Fork（仓库副本）中，只修确认的那个问题；向维护者指定的 base 提交 PR，并关联原 Issue。
5. 维护方审核后决定是否合入。部署仍须辛玥批准，由维护方执行；PR 被合并不等于已经上线。

维护者在 Issue 中更新“可认领 → 修复中 → 待审核 → 已上线”。只有实际发布后才标“已上线”；修不下去或暂时没空，在原单说明即可。

## 先确认修哪条发布线

- `main`：Doorbell 社区和 React 前端。
- `farm`：独立 Farm 引擎发布线。

以 Issue 中维护者指定的 base 和文件范围为准。不要因为在 `main` 看见 `old-vps/farm`，就向 `main` 提交农场引擎修复；分不清时先在原单问清楚。

只修限定问题，不附带玩法扩展、无关重构或格式清理。发现需要越出批准范围，先回 Issue 说明，等确认后再继续。

## 最小本地入口

社区前端使用 Node.js 24 或以上、npm 11 或以上。在自己的本地副本根目录执行：

```bash
npm install
npm run build -w @doorbell/protocol
npm run dev:web
```

打开终端显示的本地地址。这只启动社区前端，不启动社区后端或 Farm 引擎，也不提供登录状态、农场数据或真实玩家环境；依赖这些数据的 Bug 不能仅靠此入口复现。

需要后端或 Farm 的问题，先让维护者在 Issue 中给出不含密钥的隔离复现数据和对应命令。本指南暂不提供完整的 Farm 沙盒启动方式；没有安全复现条件时，先停在问题讨论，不接生产环境。

验证只做能直接证明本次问题的最小检查：记录修复前如何失败、修复后如何通过，并如实写明未验证部分。不要默认跑全仓测试或构建；具体入口由 Issue 指定，可参考 [DEBUG_INDEX](docs/DEBUG_INDEX.md)。

## 公开材料与数据边界

Issue、PR 和截图都是公开的。不要提交 QQ 号、登录链接、密码、密钥、私人聊天、真实玩家存档或完整日志；截图与页面回执先脱敏。维护方不会向贡献者提供生产密钥或玩家存档，测试只能使用本地隔离数据。

如果发现安全漏洞，不要在公开 Issue 或 PR 中披露细节；通过你原有社区渠道私下联系管理员。
